# Compatibility Baseline

This directory is retained for older pipeline entrypoints. `best.json` and
`best.md` point to the active record in `results/champion/`; historical results
have moved to `results/history/`.
