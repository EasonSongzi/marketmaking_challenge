# Challenger Pool

Challengers are stored as full-source immutable revisions under
`results/challengers/<challenger-id>/`. Their status, current revision, tuning
attempts, and evaluation history are indexed by `results/strategy-state.json`.
