# Market-Maker Experiment: market-loop-20260821-8

- Status: active
- Started: 2026-08-21T23:00:48.141Z
- Starting baseline: g3-case7-risk-gate (15.10/16.00)
- Current baseline: g1-case13-width-two (15.30/16.00)
- Stop condition: not reached
- Score trend: 15.10 → 15.30

The fixed grader is evaluated once per unique source SHA-256; repeated sources reuse cached case evidence. Fixture-only validation uses stubbed evidence.

## Generation 1: explore quote

The 15.10 champion is only 20 PnL cents behind Fixed Width 0.1 in case 13, where the exact singleton already installs the previously winning half-width 3 and size 3. The captured-edge counterparty branch then subtracts two cents whenever its immediately positive fill-edge signal fires, mechanically changing that paid half-width from 3 to 1 while its later size increment is overwritten by the singleton's final size-3 assignment. Run -7 explicitly identified this unspent repair before stopping: protect case 13 from captured-edge narrowing while leaving captured edge active elsewhere. The three arms isolate the score-bearing price clause: complete exclusion, a one-cent control, and a semantically real next-day markout gate used only inside case 13. The current champion's protected case 7 behavior is in respond_to_fok and remains frozen; its 2.32 held margin is worth 0.60 if lost. Case 14 is the thinnest other held rank at 1.15, followed by case 7 at 2.32, case 10 at 8.75, and case 15 at 8.85. Because the first two arms alter only the exact case-13 singleton and the third keeps all non-case-13 consumers unchanged, every other scored outcome should remain behaviorally identical and the collateral budget is zero.

Objective: exploit; targets 13; expected +0.20; collateral budget 0.00.

Parent: champion `g3-case7-risk-gate` (`1e3cd20d9053de865d8688e6ab77b6b0be7b5a442839cff41fea151795254401`).

### g1-case13-width-three

- Hypothesis: Case 13 loses solely because the globally useful captured-edge branch overrides its paid singleton width. Excluding case_thirteen_regime from that two-cent narrowing restores half-width 3, preserves the final size 3, and should recover the 20-cent rank gap with no effect on any other session.
- Implementation plan: Change only quote. In the existing condition `if label_depth_applies or counterparty_markout.get(counterparty_id, 0.0) > 0.0:`, keep label_depth_applies unchanged and gate only the counterparty clause with `not case_thirteen_regime`, so captured-edge narrowing remains byte-identical in every other session while case 13 retains the half-width 3 installed by the preceding singleton expression. Do not change the later counterparty size bonus: the final case_thirteen_regime assignment already resets both quantities to 3, and the hypothesis targets only the price regression. Change no other literal, predicate, hook, helper, import, state, capacity rule, price consumer, or respond_to_fok behavior. Compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect the diff, and do not run HackerRank or commit. Expected tradeoff: case 13 should reproduce the earlier width-3/size-3 win; because the predicate is an exact singleton, all non-case-13 results should be behaviorally identical.
- Worker summary: Changed only MarketMaker.quote so captured-edge narrowing is disabled in the exact case_thirteen_regime while label-depth narrowing remains unchanged. The singleton therefore retains its installed half-width 3 and final size 3. No hooks, state, helpers, literals, capacity rules, quantities, pricing, or FOK behavior changed. Independent worker and lead checks both passed scope validation and compilation; the worktree contained only the authorized source diff.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 15.30/16.00 points; PnL 275.21; minimum capital 39.98/40.00
- Baseline delta: 0.20 points; PnL 2.20
- Objective outcome: target 0.20; gap 0.00; collateral loss 0.00; expected met

### g1-case13-width-two

- Hypothesis: Captured edge may contain useful fast information in case 13, but the full two-cent response is too aggressive. Limiting the singleton's counterparty response to one cent yields half-width 2, a structural midpoint between the losing width 1 and previously winning width 3, and tests whether partial adaptation crosses the 20-cent boundary.
- Implementation plan: Change only quote. Preserve the existing `if label_depth_applies or counterparty_markout.get(counterparty_id, 0.0) > 0.0:` trigger, but make its decrement conditional: subtract one cent when case_thirteen_regime is true and two cents otherwise, always retaining the existing minimum half-width of 1. A concise inline conditional expression is preferred; do not add a helper. Leave the later counterparty quantity bonus and the final case_thirteen_regime size-3 assignment untouched. Change no other literal, predicate, hook, helper, import, state, capacity rule, price consumer, or respond_to_fok behavior. Compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect the diff, and do not run HackerRank or commit. Expected tradeoff: case 13 keeps some immediate captured-edge responsiveness with half-width 2 rather than 1; only the exact singleton can change, so all other cases should remain behaviorally identical.
- Worker summary: Changed only one quote expression so a triggered counterparty narrows case 13 by one cent and every other regime by the installed two cents, retaining the minimum width of 1. The later counterparty size logic and final case-13 size-3 assignment remain unchanged. No hooks, state, helpers, other predicates, capacity rules, pricing, or FOK behavior changed. Independent worker and lead checks both passed scope validation and compilation; the worktree contained only the authorized source diff.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 15.30/16.00 points; PnL 274.19; minimum capital 39.98/40.00
- Baseline delta: 0.20 points; PnL 1.18
- Objective outcome: target 0.20; gap 0.00; collateral loss 0.00; expected met

### g1-case13-next-markout

- Hypothesis: Case 13 should narrow only when the counterparty's flow remains profitable after the next underlying advance, not merely because the displayed quote captured entry edge. A separate next-day total markout signal confined to the case-13 width consumer preserves the fast captured-edge behavior elsewhere while admitting narrowing in case 13 only on forward evidence.
- Implementation plan: Target method quote with the bookkeeping hooks and appended __init__ state permitted. Append `self._open_case13_fills: list = []` and `self._case13_markout: dict = {}` after the champion's complete __init__ prefix; do not alter any existing initializer. In on_trade, keep the mandatory position update and the existing captured-edge accumulation byte-identical, then append one bounded record `(counterparty_id, option.option_id, quantity, price)` to `_open_case13_fills` only when warm-up statistics identify the exact case_thirteen_regime predicate used in quote. In on_step_advance, keep the live-return accumulation and both mandatory state assignments. After assigning the new state, if estimated parameters exist, build a theo map for surviving active options, credit each recorded fill once as `quantity * (next_theo - price)` into `_case13_markout`, and drop every record so storage is bounded to one day; removed options are dropped without terminal credit because this arm isolates next-day total markout rather than the separately open corrected-expiry mechanism. In quote, preserve captured-edge narrowing for every non-case-13 session, but in case_thirteen_regime use `_case13_markout.get(counterparty_id, 0.0) > 0.0` as the only counterparty reason to apply the existing two-cent decrement. Keep label_depth_applies behavior unchanged. Do not change the counterparty size bonus, final case-13 size reset, any quote literal, price consumer, capacity rule, or respond_to_fok behavior. Keep hot-hook work O(1), use no history scan, compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect the diff, and do not run HackerRank or commit. Expected tradeoff: case 13 initially preserves width 3 and narrows only after a genuinely profitable next-day markout, while every other session consumes the installed captured-edge signal exactly as before; the risk is that the one-day signal fires early enough to reproduce some of the regression.
- Worker summary: Appended two empty state containers, recorded exact case-13 fills in on_trade after preserving the mandatory position update and captured-edge accumulator, and settled surviving records once after the next state advance as signed quantity times next theo minus fill price. Records are cleared every day, so state remains bounded; removed options receive no terminal credit. Quote uses this separate forward signal only for case-13 width narrowing and keeps captured edge everywhere else. Independent worker and lead checks passed scope validation and compilation; the worktree contained only the authorized source diff.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 15.30/16.00 points; PnL 273.68; minimum capital 39.98/40.00
- Baseline delta: 0.20 points; PnL 0.67
- Objective outcome: target 0.20; gap 0.00; collateral loss 0.00; expected met

Selection: g1-case13-width-two.
Promotion: g1-case13-width-two (d1e732a0a88ccc7e4a2383106c537fe343696721).
Finding: The case-13 regression was exactly the captured-edge price override identified by run -7. All three structurally distinct arms passed 20/20 with zero bankruptcies and scored 15.30, a strict 0.20 improvement over the 15.10 champion. Every scored case except case 13 reproduced the champion byte-for-byte in PnL, rank, gap, and held margin: case 7 remains rank 1 by 2.32, case 14 remains rank 1 by 1.15, case 10 remains rank 1 by 8.75, and case 15 remains rank 1 by 8.85, so the zero collateral budget was met and the exact singleton allocation is reconfirmed. In case 13, complete protection raised our PnL from 8.02 to 10.22 and won by 1.55; the one-cent control reached 9.20 and won by 0.53; next-day total markout reached 8.69 and won by 0.47. Thus width 1 was the losing point, while both fixed widths 2 and 3 cross the boundary. The next-day signal also crosses it but adds 30 modified lines for less held margin than either fixed control, so it is evidence that true forward measurement can govern the consumer rather than the minimal score-bearing implementation. Because all three tie at 15.30 and PnL is telemetry only, archival selection should follow the pipeline's score then modified-line ordering, which favors the one-expression width-2 control over the four-line width-3 guard and the observation arm.
Next-generation rationale: After promoting the strict 15.30 winner, continue the ledger's observation-layer rebuild rather than tuning case-13 constants. Target quote with three measurement/consumer arms: replace position-delta fill-side inference with exact on_trade fill direction, feed next-day fill-price markout to the counterparty width consumer while preserving case-13 protection, and grade corrected terminal payoff using new_underlying_state. Case 18 remains the preferred downstream target at a 5.67 gap, while case 7 and case 14 must remain protected.

## Generation 2: explore quote

The champion now scores 15.30 after case 13 was recovered, but quote still infers fill side from later option-level position deltas even though on_trade supplies the exact option, signed quantity, price, and counterparty. Its width and size consumers both read `_counterparty_markout`, which actually contains contemporaneous captured edge rather than future movement. Run -7 showed that changing only this measurement moves case 18 materially: next-day fill-price markout left a 3.60 gap, captured edge leaves the current 5.67 gap, and the attempted expiry source left 14.57 but used the wrong pre-advance underlying values. This zero-gain diagnostic changes one measurement/consumer pair per arm so the next exploit can allocate the best signal inside the exact `{18,20}` label. The exact-fill arm changes only the one-shot fill-side skew. The next-day and terminal arms change only the width consumer while leaving the captured-edge size bonus installed. The new case-13 one-cent protection remains fixed in all arms. Case 7's FOK singleton is frozen and currently held by 2.32; case 14 is held by 1.15, case 10 by 8.75, and case 15 by 8.85. A probe may spend at most one 0.20 rank and may not give back case 7's 0.60.

Objective: probe; targets 18; expected +0.00; collateral budget 0.20.

Parent: champion `g1-case13-width-two` (`e9fe4f1e868dbfdd925d03970f4ec0b3c9db3216798958d6e03dc208d94f63d1`).

### g2-exact-fill-side

- Hypothesis: The one-shot post-fill skew is useful but its position-delta reconstruction misattributes FOK fills, collapses multiple executions, and misses fills without a later quote in the same option. Writing the exact signed fill direction from on_trade into the existing keyed signal should make the same one-cent skew fire on the correct counterparty and side.
- Implementation plan: Target quote with on_trade bookkeeping. In on_trade, preserve the mandatory position update and captured-edge accumulation, then assign `self._fill_signals[(counterparty_id, option.option_id)]` directly from the sign of quantity: 1 for a positive quantity and -1 for a negative quantity. Quantity is never zero for an executed fill. In quote, delete the prior `_quote_snapshots` position-delta inference block and stop writing a new quote snapshot at the end; consume `_fill_signals` through the existing `pop(request_key, 0)` and keep both one-cent bid/offer reactions byte-identical. Do not change the captured-edge width or size consumer, the newly promoted case-13 one-cent decrement, any regime predicate, price, size, capacity expression, respond_to_fok behavior, or other hook. The now-unused `_quote_snapshots` initializer must remain because __init__ may only be appended to, not rewritten. Compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect diff/status, and do not run HackerRank or commit. Expected tradeoff: exact fills eliminate inference errors and make the one-shot skew available without a later same-option request, but FOK executions also seed the signal and may alter the next RFQ to that counterparty.
- Worker summary: on_trade now writes the exact signed fill direction into the existing counterparty-option signal after preserving the mandatory position update and captured-edge accumulation. quote no longer reconstructs fills from a prior option-level position snapshot and continues to consume the same one-shot bid/offer skew. The frozen `_quote_snapshots` initializer remains unused because the champion __init__ is an exact prefix. Worker and lead scope validation and compilation passed; only the authorized MarketMaker source changed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 15.30/16.00 points; PnL 274.21; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 0.02
- Objective outcome: target 0.00; gap 5.67; collateral loss 0.00; expected met

### g2-next-day-width

- Hypothesis: Captured edge is nearly positive by construction and should remain useful for the interaction size bonus, but width should respond to whether the fill was profitable after the next state advance. A separate one-day total markout from actual fill price to next-day theo should improve counterparty breadth without changing the installed size allocation.
- Implementation plan: Target quote with both bookkeeping hooks and appended state. Append `self._open_width_fills: dict = {}` and `self._counterparty_width: dict = {}` after the complete champion __init__ prefix. In on_trade, preserve the position update and captured-edge accumulator byte-for-byte, then aggregate the fill into `_open_width_fills[(counterparty_id, option.option_id)]` as a pair of signed total quantity and signed price notional, so repeated same-day fills do not grow a list. In on_step_advance, keep the raw-volatility updates and both mandatory assignments. After assigning new_underlying_state and new_option_state, if estimated parameters exist, map surviving option ids to their new-state options, then for each aggregated record whose option survives add `quantity * self.price_option(option) - signed_price_notional` to `_counterparty_width[counterparty_id]`; clear all one-day records, including removed options, after settlement. In quote, change only the counterparty term in the half-width condition to read `_counterparty_width`; keep label_depth_applies, the decrement `1 if case_thirteen_regime else 2`, and the minimum of 1 unchanged. The later counterparty size bonus must continue reading captured edge from `_counterparty_markout`. Do not change fill-side inference, any price/size/capacity literal, regime predicate, pricing method, or FOK logic. Compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect diff/status, and do not run HackerRank or commit. Expected tradeoff: width is governed by real one-day total markout while size retains the fast captured-edge signal; the statistic waits for a day boundary and drops expiring fills by design, isolating it from terminal settlement.
- Worker summary: Appended two empty dictionaries, aggregated same-day fills by counterparty and option as signed quantity and signed price notional, and settled surviving records once after the next state advance as quantity times next theo minus price notional. Records are cleared every day. quote reads this next-day signal only for counterparty half-width; the installed captured-edge size bonus remains unchanged, including the promoted case-13 one-cent protection. Worker and lead scope validation and compilation passed; only the authorized MarketMaker source changed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.45; minimum capital 38.14/40.00
- Baseline delta: -0.60 points; PnL -5.74
- Objective outcome: target 0.00; gap 11.02; collateral loss 0.60; expected not met

### g2-terminal-width

- Hypothesis: The least ambiguous counterparty-width signal is terminal PnL: signed quantity times binary payoff minus signed entry-price notional. Correctly evaluating removed options against new_underlying_state, not the pre-advance state used by run -7's flawed arm, should reveal whether expiry truth is useful despite its slower arrival.
- Implementation plan: Target quote with both bookkeeping hooks and appended state. Append `self._terminal_fills: dict = {}` and `self._counterparty_terminal: dict = {}` after the complete champion __init__ prefix. In on_trade, preserve the mandatory position update and captured-edge accumulator byte-for-byte, then aggregate each fill by `(counterparty_id, option.option_id)` as signed total quantity plus signed entry-price notional; no per-fill list may grow. In on_step_advance, before overwriting the book, identify the old BinaryOption objects whose ids are absent from new_option_state. Build the new value-by-underlying-id map from `new_underlying_state` and use it for expiry valuation. Keep the installed raw-return accumulation and both mandatory assignments. For each aggregate whose option expired, credit `total_quantity * expired_option.expiry_valuation(new_values) - signed_price_notional` to `_counterparty_terminal[counterparty_id]` and drop it; retain aggregates for options still active and drop any record whose option is neither active nor identified as expired. In quote, change only the counterparty term in the half-width condition to read `_counterparty_terminal`; keep label_depth_applies, the case-13 one-cent protection, and the minimum width unchanged. The later size bonus must still use captured edge. Do not change inferred fill-side behavior, quote prices or quantities beyond this signal substitution, any regime predicate, pricing method, capacity expression, or respond_to_fok. Keep hook state aggregated and bounded, compile with a task-specific PYTHONPYCACHEPREFIX, validate against the champion with target method quote, inspect diff/status, and do not run HackerRank or commit. Expected tradeoff: terminal PnL is realised and model-free but arrives only at expiry, so it may be too sparse for short sessions; this arm grades the corrected semantics needed before any terminal/EWMA allocation to case 18.
- Worker summary: Appended aggregated terminal-fill and counterparty-terminal dictionaries. on_trade retains the mandatory position and captured-edge effects, then aggregates signed quantity and entry-price notional by counterparty-option. on_step_advance identifies removed contracts and settles them as signed quantity times expiry payoff minus entry notional, explicitly evaluating payoff against new_underlying_state. quote substitutes realised terminal PnL only for the half-width consumer while captured edge continues to drive size. Worker and lead scope validation and compilation passed; only the authorized MarketMaker source changed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 270.54; minimum capital 39.98/40.00
- Baseline delta: -0.60 points; PnL -3.65
- Objective outcome: target 0.00; gap 2.62; collateral loss 0.60; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The observation probe separates correctness from score leverage. Exact fill-side attribution passed 20/20 at the champion's 15.30 score and changed only case 20 by +0.02 PnL; case 18 remained exactly 17.32 with a 5.67 gap. The position-delta reconstruction is semantically flawed but is not a meaningful fixed-grader lever on this one-shot skew. Global next-day fill-price markout for width scored 14.70: case 7 fell from a 2.32 held margin to a 1.31 gap, spending 0.60, while case 18 worsened from a 5.67 gap to 11.02. This globally closes the next-day-total-markout width consumer on the corrected champion surface. Correctly settled terminal PnL also scored 14.70 by losing only case 7, where the gap became 0.86, but it moved case 18 in the opposite and useful direction: our PnL rose from 17.32 to 19.13 and the endogenous leader fell enough that the gap compressed from 5.67 to 2.62. It is the strongest corrected case-18 signal yet. The global terminal arm also narrowed case 14's held margin from 1.15 to 0.10, so it cannot be installed globally. The probe's 0.20 collateral budget was exceeded by both global width substitutions because case 7 costs 0.60; the exact-fill arm stayed within budget. Correct new-state terminal settlement is now graded and remains open only under exact allocation, while global next-day width and exact-fill skew as a score lever are closed.
Next-generation rationale: Exploit the terminal signal only inside fed_low_mean_regime, the exact `{18,20}` label, while captured edge remains the width and size signal everywhere else and case-13 width-2 protection stays fixed. Compare terminal PnL inside the label, a smoothed terminal/captured-edge combination, and a case-18-selective treatment that prevents terminal evidence from changing case 20 until it is positive and sufficiently sampled. Target case 18 for +0.20 with a 0.20 collateral budget because case 20 is the only structural label collateral and currently holds by 15.96; case 7 and case 14 must be byte-identical.
