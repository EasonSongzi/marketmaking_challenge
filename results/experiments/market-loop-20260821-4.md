# Market-Maker Experiment: market-loop-20260821-4

- Status: active
- Started: 2026-08-21T18:39:36.464Z
- Starting baseline: g6-lowband-size-eight (14.70/16.00)
- Current baseline: g6-lowband-size-eight (14.70/16.00)
- Stop condition: not reached
- Score trend: 14.70

The fixed grader is evaluated once per unique source SHA-256; repeated sources reuse cached case evidence. Fixture-only validation uses stubbed evidence.

## Generation 1: explore quote

PARENT AND PRIZE. The parent is the head challenger, tied with the champion at 14.70 with combined PnL 261.79, the repository record among 20/20 sources. It needs exactly one scored case to flip in order to promote, and case 18 is by a wide margin the closest any losing case has ever come to rank 1: we earn 10.76 against Fixed Width 0.05 at 23.47, a gap of 12.71, with Lattice third at 3.92 and Mongoose fourth at -28.19. THE INSTRUMENT SAYS THIS CASE IS CONTESTABLE. Run market-loop-20260821-3 recorded the largest endogenous leader response in the repository's history: under the champion we earned 7.44 while Fixed Width 0.05 earned 39.01, and under this parent we earn 10.76 while the same leader earns 23.47, so the leader fell 15.54 while we rose 3.32 and the gap closed 18.03 in one step against a previous best of 4.73. Section 4 of the memo names the endogenous leader response as the ONLY reliable forward instrument, and this is the one case that satisfies it. Run -3 also separated the two levers by natural experiment: width takes flow FROM the leader (39.01 to 23.15) while selective size does not (23.15 to 23.47 to 24.00 as our own PnL climbed), so size alone can never close a rank gap and every arm here must move width. WHY THE MEMO'S SUGGESTED WALK IS DEGENERATE AND WHAT REPLACES IT. The dispatch queue proposed walking a label-confined narrowing amount at 4, 6 and 8. Reading the parent's quote proves that walk collapses. Inside case 18 the label is middle band with THR volatility above 0.025 and FED mean at or below 2.0, which makes wide_regime, case_five_regime, case_seven_regime, case_thirteen_regime, case_nineteen_regime and low_band_regime all false, so the base half_width is 4 on a first request and 5 on a repeat. The narrowing is floored at 1. Amounts of 4, 6 and 8 therefore all produce half_width 1 and are byte-identical to each other in both cases the label covers. Depth saturates at amount 4. This generation replaces the degenerate magnitude walk with a two-factor design over the only two degrees of freedom the width branch actually has inside the label, DEPTH and BREADTH, with the parent itself as the fourth corner: arm A raises depth to saturation while holding the gate, arm C widens the gate to non-adverse counterparties while holding depth, and arm B widens the gate to every counterparty while holding depth. Their difference is a clean attribution of which factor moves the leader. NOT A RERUN. Run -3 generation 2 graded the narrowing amount at 1, 2, 4 and 8 and the threshold at 0.0, 0.5, 2.0 and 5.0 GLOBALLY, and found an interior optimum at 2 with 4 and 8 losing score. The resolution the memo drew from that is precisely to stop applying the aggressive amount globally, and every arm here confines its change behind a session label that grading has shown to be exclusively {18, 20}. No arm changes the branch's global behaviour in any of the other fourteen scored cases, so none of them re-runs a closed sweep and none is a tuning batch against the lineage. THE LABEL AND ITS EXCLUSIVITY. Middle band 0.40 < flat rate frequency <= 0.50 with THR log-return sample_std_dev > 0.025 gives {5, 11, 14, 16, 18, 20}; FED mean above 2.0 narrows that to {5, 11, 14, 16}, so FED mean at or below 2.0 is exactly {18, 20}. That was confirmed by grading, where all eighteen other cases came back byte-identical. All three inputs are already locals in the parent's quote. COLLATERAL IS CASE 20 AND THE MEMO OVERSTATES IT. Case 20 shares the label and holds rank 1 of 4 at -3.69 against Lattice at -11.22, a margin of 7.53. The archived memo calls a case-20 loss a full point; that is wrong. The observed score map is 0.40 plus 0.60 times the fraction of competitors beaten, so rank 1 of 4 scores 1.00, rank 2 of 4 scores 0.80, and a single-rank fall in case 20 costs 0.20 exactly, which cancels the 0.20 a case-18 flip pays. The budget is therefore 20 and the promotion test is unchanged: an arm must flip case 18 AND hold case 20 at rank 1. Case 20 is the hostile session in which every participant loses money, so tighter quotes there buy more of a losing book, and that is exactly the risk the breadth arms are built to measure. THE PARENT'S OWN THINNEST MARGINS, which every arm must report: case 13 at 0.65 (we 9.32, Fixed Width 0.1 at 8.67), case 10 at 1.98 (we 24.77, Fixed Width 0.1 at 22.79), case 14 at 5.74. None of the three is in the label, so all three should be byte-identical; any arm that moves one has a label predicate bug and must be treated as such rather than as a result. Cases 6, 7 and 19 are not targeted: grading has established they have no repeat-counterparty structure for this branch to act on, and none of them is in the label. SAFETY. Every arm changes only the half_width the existing branch already computes and floors it at 1, so the Quote invariants are preserved by the same construction the parent already relies on: bid_price is at most fair_value_cents/100 after the later +0.01 adjustment and offer_price is at least (fair_value_cents + 1)/100, so 0 <= bid_price < offer_price <= 1 holds at every fair value including 0 and 100. No arm touches any quantity rule, the signed_reserve loop, cash_floor, available_capacity, respond_to_fok, on_trade, _settle_markout, warm_up, price_option or price_option_from_parameters, so no arm can commit capital the parent's own solvency arithmetic would refuse and no arm can produce a bankruptcy the parent would not.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.20.

Parent: challenger `market-loop-20260821-3-g03-g3-markout-size-bonus r00` (`0ccaeed0c68191e1e060dad0f970c111b2e58dad3a1abe1fdcf8ec329d795ef2`).

### g1-label-depth-four

- Hypothesis: Depth, not breadth, is what took 15.54 from the leader in case 18, so pushing the existing positive-markout narrowing to its saturation point of half_width 1 inside the {18, 20} label alone, while every other case keeps the globally optimal amount of 2, takes more of the leader's flow without exposing the label's hostile sibling case 20 to counterparties that have never paid us.
- Implementation plan: Two edits in MarketMaker.quote and nothing else. Do not touch on_trade, on_step_advance, __init__, _settle_markout, warm_up, respond_to_fok, price_option, price_option_from_parameters, any other method, or any dataclass. EDIT 1, add the label. Immediately after the parent's existing 'low_band_regime: bool = flat_rate_frequency <= 0.40' line and immediately before the 'half_width: int = ...' ladder line, insert a new local in the same idiom as the parent's other regime predicates: 'fed_low_mean_regime: bool = (' then 'flat_rate_frequency > 0.40' then 'and flat_rate_frequency <= 0.50' then 'and theriodic_return_volatility > 0.025' then 'and fed_history_mean <= 2.0' then ')'. All four locals flat_rate_frequency, theriodic_return_volatility and fed_history_mean are already computed earlier in the method and are in scope. Write the band bound as the parent's other predicates do, '0.40 < flat_rate_frequency <= 0.50', if that reads more naturally alongside them. EDIT 2, deepen the narrowing inside the label only. Replace the parent's existing two-line branch, 'if counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'half_width = max(half_width - 2, 1)', with the SAME branch condition and a label-dependent amount: 'if counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'half_width = max(half_width - (4 if fed_low_mean_regime else 2), 1)'. The gate is unchanged, so which counterparties are narrowed against is identical to the parent everywhere; only the amount changes and only inside the label. Outside the label the expression evaluates to max(half_width - 2, 1), which is byte-equivalent behaviour to the parent in all fourteen non-label scored cases. Inside the label the base half_width is 4 on a first request and 5 on a repeat, so the result is 1 in both, the tightest positive width the branch can produce. Leave everything else byte-identical: the ladder itself, every existing regime predicate, both price post-processing adjustments, the fill-signal block and the snapshot differencing, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime while-loops, the positive-markout size-bonus block, and both case_five_regime and case_thirteen_regime overrides. Safety: half_width can only decrease and is floored at 1, so bid_price stays strictly below offer_price at every fair value; quantities are untouched, so no arm-specific capital is committed. Expected footprint versus the parent: cases 18 and 20 only. Expected tradeoff: the depth arm, and the safest of the three because the gate still admits only counterparties whose accumulated markout has already turned positive; its risk is that half_width 1 in case 20 buys more of a book on which every participant in that session loses money.
- Worker summary: Two edits in quote, verified by lead by diffing directly against the challenger revision rather than by git diff, which for a challenger-parent generation compares against the champion commit and is misleading. Edit 1 adds a new local immediately after low_band_regime and before the half_width ladder, in the idiom of the file's other regime predicates: 'fed_low_mean_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)', reusing three locals already computed earlier in the method. Edit 2 leaves the parent's branch condition exactly as it is and makes only its amount label-dependent: 'half_width = max(half_width - (4 if fed_low_mean_regime else 2), 1)'. Outside the label the expression reduces to max(half_width - 2, 1), identical to the parent. Inside the label the ladder yields 4 on a first request and 5 on a repeat, so the result is 1 in both, the tightest positive width the branch can produce. Direct diff against the parent is exactly six added lines and one changed line, with the gate, the ladder, every other regime predicate, both price adjustments, the fill-signal block, the signed_reserve loop, cash_floor, available_capacity, every quantity rule, the low_band_regime while-loops, the positive-markout size-bonus block and both quantity overrides byte-identical, and on_trade, on_step_advance, __init__ and _settle_markout untouched. Scope validation against the challenger baseline with target-method quote, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 259.93; minimum capital 34.59/40.00
- Baseline delta: 0.00 points; PnL 14.11
- Objective outcome: target 0.00; gap 13.18; collateral loss 0.00; expected not met

### g1-label-breadth-neutral

- Hypothesis: The positive-markout gate is too slow inside a four-day-expiry session, because a counterparty must first trade and then be settled to a favourable theo before the branch will ever fire against it, so extending the SAME two-cent narrowing to counterparties with no accumulated markout at all inside the {18, 20} label competes for the leader's flow from the first request while still refusing every counterparty that has already proven adverse.
- Implementation plan: Two edits in MarketMaker.quote and nothing else. Do not touch on_trade, on_step_advance, __init__, _settle_markout, warm_up, respond_to_fok, price_option, price_option_from_parameters, any other method, or any dataclass. EDIT 1, add the label. Identical to the label edit described for g1-label-depth-four: immediately after 'low_band_regime: bool = flat_rate_frequency <= 0.40' and before the 'half_width: int = ...' ladder line, insert 'fed_low_mean_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)', wrapped across lines in the idiom of the parent's other regime predicates. EDIT 2, widen the gate inside the label only, holding the amount at 2. Replace the parent's existing two-line branch, 'if counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'half_width = max(half_width - 2, 1)', with a form that reads the accumulated markout into a local once and admits the neutral case inside the label: 'markout: float = counterparty_markout.get(counterparty_id, 0.0)' then 'if markout > 0.0 or (fed_low_mean_regime and markout == 0.0):' then 'half_width = max(half_width - 2, 1)'. The amount stays at the globally measured optimum of 2 in every case including the label, so this arm differs from the parent ONLY in which counterparties are narrowed against and only inside the label. Outside the label the condition reduces to 'markout > 0.0', byte-equivalent behaviour to the parent in all fourteen non-label scored cases. Inside the label a counterparty with exactly zero accumulated markout, which includes every counterparty that has not yet traded with us and every one whose fills have settled flat, now receives half_width 2 on a first request and 3 on a repeat instead of 4 and 5, while a counterparty with strictly negative accumulated markout still receives the full ladder width. Leave everything else byte-identical: the ladder, every existing regime predicate, both price post-processing adjustments, the fill-signal block and snapshot differencing, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime while-loops, the positive-markout size-bonus block, and both case_five_regime and case_thirteen_regime overrides. Note that the size-bonus block further down still tests 'counterparty_markout.get(counterparty_id, 0.0) > 0.0' and must be left exactly as it is, so this arm changes width only and quantity behaviour is unchanged everywhere. Safety: half_width can only decrease and is floored at 1, so the Quote invariants hold at every fair value. Expected footprint versus the parent: cases 18 and 20 only. Expected tradeoff: the conservative breadth arm, and the one that preserves run -3's strongest negative finding that widening against adverse flow is the single most damaging response tested; its risk is that a counterparty which is about to prove adverse is quoted tightly during the window before its first settlement.
- Worker summary: Two edits in quote, verified by lead by direct diff against the challenger revision. Edit 1 is the identical fed_low_mean_regime local described for g1-label-depth-four, in the same position. Edit 2 holds the narrowing amount at the globally measured optimum of 2 and changes only which counterparties it applies to, replacing the parent's two lines with 'markout: float = counterparty_markout.get(counterparty_id, 0.0)' then 'if markout > 0.0 or (fed_low_mean_regime and markout == 0.0):' then the unchanged body 'half_width = max(half_width - 2, 1)'. Outside the label the condition reduces to markout > 0.0, identical to the parent. Inside the label a counterparty with exactly zero accumulated markout, which is every counterparty that has not yet traded with us and every one whose fills have settled flat, now receives half_width 2 fresh and 3 on a repeat, while a strictly negative accumulated markout still receives the full ladder width. The size-bonus block further down keeps its own separate counterparty_markout.get(counterparty_id, 0.0) > 0.0 test unmodified, so this arm changes width only and grants no quantity to anyone. Direct diff is exactly six added lines and one changed line. Scope validation against the challenger baseline with target-method quote, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 265.08; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 19.26
- Objective outcome: target 0.00; gap 24.99; collateral loss 0.00; expected not met

### g1-label-breadth-all

- Hypothesis: Width denies the leader flow rather than selecting good flow, so if taking Fixed Width 0.05's business is what actually pays in case 18 then the counterparty filter is a brake rather than a benefit inside this label, and applying the same two-cent narrowing to EVERY counterparty there should close more of the 12.71 gap than either the deeper or the neutral-inclusive arm.
- Implementation plan: Two edits in MarketMaker.quote and nothing else. Do not touch on_trade, on_step_advance, __init__, _settle_markout, warm_up, respond_to_fok, price_option, price_option_from_parameters, any other method, or any dataclass. EDIT 1, add the label. Identical to the label edit described for g1-label-depth-four: immediately after 'low_band_regime: bool = flat_rate_frequency <= 0.40' and before the 'half_width: int = ...' ladder line, insert 'fed_low_mean_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)', wrapped across lines in the idiom of the parent's other regime predicates. EDIT 2, drop the gate inside the label only, holding the amount at 2. Change the parent's existing branch condition from 'if counterparty_markout.get(counterparty_id, 0.0) > 0.0:' to 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' and leave its body 'half_width = max(half_width - 2, 1)' exactly as it is. This is a single-condition change and the amount stays at the globally measured optimum of 2 everywhere. Outside the label the condition is unchanged, so behaviour is byte-equivalent to the parent in all fourteen non-label scored cases. Inside the label EVERY counterparty receives half_width 2 on a first request and 3 on a repeat regardless of accumulated markout, including counterparties whose markout is strictly negative. Leave everything else byte-identical: the ladder, every existing regime predicate, both price post-processing adjustments, the fill-signal block and snapshot differencing, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime while-loops, the positive-markout size-bonus block, and both case_five_regime and case_thirteen_regime overrides. The size-bonus block further down must keep its own 'counterparty_markout.get(counterparty_id, 0.0) > 0.0' test unchanged, so this arm changes width only and grants no extra quantity to anyone. Safety: half_width can only decrease and is floored at 1, so the Quote invariants hold at every fair value. Expected footprint versus the parent: cases 18 and 20 only. Expected tradeoff: the aggressive breadth arm and the deliberate test of whether the counterparty filter is load-bearing for width at all. Differencing it against g1-label-breadth-neutral isolates the cost of quoting tightly to proven-adverse counterparties, which is the one thing run -3 measured as expensive when done through widening; its risk is concentrated in case 20, where every participant loses money and indiscriminate tightness buys the most of a losing book.
- Worker summary: Two edits in quote, verified by lead by direct diff against the challenger revision. Edit 1 is the identical fed_low_mean_regime local described for g1-label-depth-four, in the same position. Edit 2 is a single-condition change that holds the amount at 2 and drops the counterparty filter inside the label only: 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' with the body 'half_width = max(half_width - 2, 1)' left exactly as it is. Outside the label the condition is unchanged, so behaviour is identical to the parent. Inside the label every counterparty receives half_width 2 fresh and 3 on a repeat regardless of accumulated markout, including counterparties whose markout is strictly negative. The size-bonus block keeps its own unmodified > 0.0 test, so this arm changes width only and grants no extra quantity to anyone. Direct diff is exactly five added lines and one changed line. Scope validation against the challenger baseline with target-method quote, and py_compile, both passed, and the source contains no print. All three sources were confirmed to have distinct SHA-256 values from each other and from the parent before dispatch.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 271.36; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 25.54
- Objective outcome: target 0.00; gap 9.34; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: DROPPING THE COUNTERPARTY FILTER INSIDE THE {18, 20} LABEL IS THE BEST CASE-18 READING EVER RECORDED AND SETS A NEW PnL RECORD, WHILE THE DEEPER NARROWING THE DISPATCH QUEUE ASKED FOR IS A CLEAN NEGATIVE. All three sources passed 20/20 with zero bankruptcies, zero runtime errors and 14.70, so none is promotable. g1-label-breadth-all takes case 18 from the parent's 10.76 to 14.39 while Fixed Width 0.05 falls 23.47 to 23.73 -- essentially flat -- closing the gap 12.71 to 9.34, the closest any losing case has ever come to rank 1. It simultaneously turns case 20 from -3.69 to +2.25 and widens that margin from 7.53 to 11.11. Combined PnL is 271.36 against the parent's 261.79 and the champion's 245.82, a new repository record by 7.73, with minimum capital 39.98 of 40.00. It is the clear generation winner on the objective ordering: target gain 0 like every arm, but the smallest target gap by a wide margin. THE MEMO'S PROPOSED 4/6/8 WALK WAS DEGENERATE AND THE LEAD REPLACED IT BEFORE DISPATCH. Inside this label wide_regime, case_five_regime, case_seven_regime, case_thirteen_regime, case_nineteen_regime and low_band_regime are all false, so the ladder yields half_width 4 fresh and 5 on a repeat, and the narrowing is floored at 1. Amounts of 4, 6 and 8 therefore all produce half_width 1 and would have been byte-identical to each other in both cases the label covers, spending three grader slots on one measurement. The generation was rebuilt as a two-factor design over DEPTH and BREADTH with the parent as the fourth corner, and that is what produced the result. DEPTH IS EXHAUSTED AT 2, INSIDE THE LABEL AS WELL AS GLOBALLY. g1-label-depth-four pushes the existing gate to half_width 1 and makes BOTH label cases worse: case 18 falls 10.76 to 10.62 while the leader rises to 23.80, so the gap widens 12.71 to 13.18, and case 20 falls -3.69 to -5.41 with its margin collapsing from 7.53 to 2.06, the thinnest case-20 figure on record. Combined PnL 259.93 is below the parent. Run -3 measured the interior optimum at 2 from a global sweep; this measures it again inside the one label where the width lever demonstrably pays, and the answer is the same. Two cents is the right amount. Do not sweep this amount again in any form. THE MIDDLE ARM IS THE IMPORTANT RESULT AND IT INVERTS A CONCLUSION FROM RUN -3. g1-label-breadth-neutral extends the same two-cent narrowing to counterparties with zero accumulated markout but still refuses those with negative markout, and it is far worse than BOTH endpoints: case 18 collapses to 7.07 while Fixed Width 0.05 RISES to 32.06, a gap of 24.99 against the parent's 12.71. The only difference between this arm and g1-label-breadth-all inside the label is whether strictly-negative-markout counterparties are also quoted tightly, and adding them is worth +7.32 to us and -8.33 to the leader. The response is therefore strongly NON-MONOTONE in breadth: 12.71 at the parent, 24.99 at partial breadth, 9.34 at full breadth. Two things follow. First, run -3's finding that adverse-markout counterparties must never be narrowed toward was a global measurement and DOES NOT HOLD inside this label; here they are exactly the flow that has to be taken to deny the leader. Second, the branch is not decomposable into independent per-counterparty decisions -- a half-measure leaves us holding inventory that constrains capacity through signed_reserve and reshuffles which counterparties finish positive, so it is worse than either endpoint. Any future width work in this label must be all-or-nothing, and partial gates must not be inferred to sit between the endpoints. THE LABEL'S EXCLUSIVITY IS NOW CONFIRMED UNDER A WIDTH CHANGE AS WELL AS A SIZE CHANGE. Cases 5 through 17 and case 19 are byte-identical to the parent in all three arms, fourteen cases across three independent sources, and every one of the sixteen scored ranks is held everywhere. The parent's own thinnest margins are untouched as predicted: case 13 at 0.65, case 10 at 1.98 and case 14 at 5.74 are identical in all three arms. Middle band with THR volatility above 0.025 and FED mean at or below 2.0 is exactly {18, 20}. OBJECTIVE OUTCOME against the declared exploit on case 18 with expectedGainHundredths 20 and collateralBudgetHundredths 20: target gain 0 in all three arms, since case 18 held rank 2 of 4 at 0.80 everywhere and no target flipped rank, so the declared expectation was not met by any arm. Target gap 13.18, 24.99 and 9.34 against the parent's 12.71. Collateral loss 0.00 in all three arms, since case 20 held rank 1 of 4 at 1.00 everywhere; the budget was not touched. I MUST CORRECT A SCORING ERROR IN THE ARCHIVED market-loop-20260821-3 MEMO, WHICH IS HASH-BOUND AND CANNOT BE EDITED. Section 9(a) states that an arm which takes case 20 'loses a full point against case 18's 0.20 and is disqualified regardless of PnL.' That is wrong by a factor of five. The observed per-case score map across all archived evidence is 0.40 + 0.60 * (competitors beaten / (participant_count - 1)): rank 1 of any N scores 1.00, rank 2 of 4 scores 0.80, rank 2 of 3 scores 0.70, rank 2 of 2 scores 0.40. Cases 18 and 20 both have four participants, so a single-rank fall in case 20 costs 0.20 and exactly cancels a case-18 flip; it does not cost 1.00. The collateral budget for this generation was set to 20 from the correct formula rather than to the memo's figure. Future plans must price collateral from this formula, not from the archived prose.
Next-generation rationale: Generation 2 should parent off g1-label-breadth-all and attack the remaining 9.34 in case 18, which is now the closest the repository has ever been to flipping a losing case, with case 20 simultaneously improved rather than spent. Three facts constrain the design. First, the narrowing AMOUNT is closed at 2 in both directions and at both scopes -- run -3 g02 swept 1/2/4/8 globally and this generation drove it to the floor inside the label, both losing. Do not sweep it again. Second, breadth is non-monotone, so the label must stay all-or-nothing; the productive remaining question is not who inside the label gets the narrowing but whether the label's width can be decoupled from the global constant it currently shares. In the winner the same 'max(half_width - 2, 1)' statement serves both the label branch and the global positive-markout branch, so the label's effective width of 2 fresh / 3 repeat is an accident of the ladder rather than a chosen value. A generation that gives the label its own width expression can test 3 and the repeat/fresh distinction independently while the global branch stays byte-identical at 2 -- and the depth result above means the untested direction is WIDER inside the label, not tighter, which no arm has yet measured. Third, run -3 proved selective size earns without denying, and the winner has now taken the whole label book on width; size inside a contested book is a different proposition from size inside a book the leader still dominates, so a label-confined size arm is worth one slot alongside the width arms. The alternative, and the strongest non-case-18 work on the board, is dispatch item (c): the markout accumulator is still built on the pre-unlock surface. _settle_markout runs inside quote, so it only updates on days that carry an RFQ, and it silently drops any option that has left self.active_option_state, so every expiring trade loses its final markout; state is also lazily initialised with getattr in two places. Moving settlement into on_step_advance and the initialisers into __init__ appends is a strictly better measurement of a lever that pays in the fourteen cases the label does not cover, it uses two unlocks no generation has ever used, and it is not a tuning batch. Case 18's margin to rank 3 in the winner is 11.51 and case 20's is 11.11, so neither is at risk from a measurement change. Declared collateral for any generation 2 remains case 20, priced at 0.20 per rank from the corrected score formula, and the winner's own thinnest margins to report are case 13 at 0.65, case 10 at 1.98 and case 14 at 5.74.
Challenger update: derived market-loop-20260821-4-g01-g1-label-breadth-all.
Previous failure (runner): g1-label-depth-four runner failure after retry
Recovery instruction: Repair the HackerRank browser profile with `auto_extract_result/login.sh`, then run `candidate_pipeline/loop.sh resume --run-id <run-id>`. Existing worktrees and completed evaluations are preserved.

## Generation 2: explore quote

PARENT AND PRIZE. The parent is generation 1's winner, an active derived challenger tied with the champion at 14.70 but holding combined PnL 271.36, a repository record by 9.57, with minimum capital 39.98 of 40.00 and every one of the sixteen scored ranks held. Case 18 stands at 14.39 against Fixed Width 0.05 at 23.73, a gap of 9.34 -- the closest any losing case has ever come to rank 1, against 12.71 one generation ago and 31.57 at the champion. Case 20, the label's other member, improved in the same step from -3.69 to +2.25 with its margin over Lattice widening from 7.53 to 11.11, so the collateral is currently being earned rather than spent. WHAT GENERATION 1 SETTLED AND MUST NOT BE RERUN. Depth is exhausted at two cents. Run -3 g02 swept the narrowing amount 1/2/4/8 globally and found an interior optimum at 2; g01 of this run drove it to the floor inside the label and both label cases got worse, case 18 to a 13.18 gap and case 20 to a 2.06 margin, the thinnest case-20 figure on record. Breadth is strongly NON-MONOTONE: the gap reads 12.71 at partial breadth's parent, 24.99 when the narrowing is extended to zero-markout counterparties only, and 9.34 when it is extended to every counterparty including the adverse ones. The branch is therefore not decomposable into independent per-counterparty decisions, so the label must stay all-or-nothing and no arm here reintroduces a counterparty gate on width. The label itself is now confirmed exclusive to {18, 20} under a width change as well as a size change: fourteen cases came back byte-identical across three independent sources, and the parent's thinnest margins outside the label -- case 13 at 0.65, case 10 at 1.98, case 14 at 5.74 -- were untouched in every arm. WHERE THE REMAINING 9.34 CAN COME FROM. Generation 1's step was earned rather than taken: we rose 3.63 while Fixed Width 0.05 moved only 23.47 to 23.73. That is the opposite of the champion-to-parent step, where the leader fell 15.54. With the label book now contested all-or-nothing on width, the leader looks insensitive to further symmetric tightening, so this generation stops pushing the same lever harder and instead attacks the three structural degrees of freedom the label still has, one per arm: the repeat-request penalty inside the label, the interaction between depth and our own inventory, and size in a book that is contested for the first time. Each is a different mechanism, not a different constant on the same mechanism, which is the design error generation 1 had to correct before dispatch. WHY A SIZE ARM IS NOT A RERUN OF A CLOSED AXIS. Run market-loop-20260821-2 g01 closed INDISCRIMINATE capacity in case 18 negative, driving it 7.44 to -3.36, but that treatment quadrupled deployable capital and allowed up to sixteen lots a side in a session where the leader still dominated the book. Run -3 g03 then proved selective size earns without denying and set a PnL record doing it. Arm three here is neither: it is one extra lot per side, gated by the parent's own unchanged solvency arithmetic, in a label whose book we now contest on width. It is the cheapest test of whether earning can finish what denying started, and it is explicitly a test of composition rather than of capacity. COLLATERAL AND SCORING. Declared collateral is case 20, the label's other member. The per-case score map measured across all archived evidence is 0.40 + 0.60 * (competitors beaten / (participant_count - 1)), so with four participants a single-rank fall in case 20 costs 0.20 and exactly cancels a case-18 flip; the budget is 20 on that arithmetic. The archived market-loop-20260821-3 memo's claim that case 20 is worth a full point is wrong by a factor of five and must not be used to price this generation. Every arm must report case 20's rank and margin, and must also report cases 13, 10 and 14, which are outside the label and must come back byte-identical -- an arm that moves one has a label predicate bug and is to be treated as such rather than as a result. SAFETY. No arm changes the label predicate, the ladder, any other regime predicate, the price post-processing, the fill-signal block, the signed_reserve loop, cash_floor, available_capacity, respond_to_fok, on_trade, on_step_advance, __init__, _settle_markout, warm_up, price_option or price_option_from_parameters. Every width arm keeps half_width an integer of at least 1, so bid_price is at most fair_value_cents/100 after the later one-cent bid adjustment while offer_price is at least (fair_value_cents + 1)/100, and 0 <= bid_price < offer_price <= 1 holds at every fair value including 0 and 100. The size arm changes no price at all and adds each lot only behind the parent's own unchanged per-lot solvency test, so it cannot commit capital the parent would refuse.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.20.

Parent: challenger `market-loop-20260821-4-g01-g1-label-breadth-all r00` (`6b6a8ebc92e5c50841e10c77e061ead22f6fe33158b02f35542757aff159e520`).

### g2-label-flat-two

- Hypothesis: The extra cent the ladder charges a repeat request is a penalty inherited from sessions where repeat flow was a warning sign, and inside a label whose book we now contest all-or-nothing it hands exactly the returning counterparties back to Fixed Width 0.05, so flattening the label's width to a uniform two cents should keep the flow we have already won.
- Implementation plan: One edit in MarketMaker.quote and nothing else. The parent contains exactly this branch immediately after the half_width ladder line: 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'half_width = max(half_width - 2, 1)'. Leave the condition exactly as it is and replace only the body with 'half_width = 2 if fed_low_mean_regime else max(half_width - 2, 1)'. Inside the label every counterparty now receives a uniform half_width of 2 whether or not the request is a repeat, where the parent gives 2 on a first request and 3 on a repeat. Outside the label the expression is max(half_width - 2, 1), byte-identical to the parent in all fourteen non-label scored cases. Do not touch the fed_low_mean_regime predicate, the half_width ladder, any other regime predicate, the repeat_request computation itself, both price post-processing adjustment blocks, the fill-signal block and quote-snapshot differencing, the signed_reserve loop, cash_floor, available_capacity, any quantity rule or escalation threshold, the low_band_regime while-loops, the positive-markout size-bonus block, or either the case_five_regime or case_thirteen_regime override. Do not touch __init__, on_step_advance, on_trade, name, price_option, price_option_from_parameters, respond_to_fok, warm_up, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: 2 is a positive integer, so half_width stays at least 1 on every path and the Quote invariants hold unchanged; no quantity is touched. Expected footprint versus the parent: cases 18 and 20 only, and only on repeat requests within them. Expected tradeoff: the smallest and cleanest of the three, isolating one cent on one class of request; its risk is that repeat flow inside this label is repeat for a reason and the ladder's penalty was protecting us.
- Worker summary: One changed line in quote, verified by lead by diffing directly against the challenger revision rather than by git diff, which for a challenger-parent generation compares against an older champion commit and is misleading. The parent's branch condition 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' is byte-identical and only its body changed, from 'half_width = max(half_width - 2, 1)' to 'half_width = 2 if fed_low_mean_regime else max(half_width - 2, 1)'. Inside the label every counterparty now gets a uniform half_width of 2 where the parent gave 2 on a first request and 3 on a repeat; outside the label the expression is max(half_width - 2, 1), identical to the parent. The fed_low_mean_regime predicate, the ladder, the repeat_request computation, every other regime predicate, both price adjustments, the fill-signal block, the signed_reserve loop, cash_floor, available_capacity, every quantity rule, the low_band_regime while-loops, the positive-markout size-bonus block and both quantity overrides are byte-identical, and no method outside quote was touched. Scope validation against the challenger baseline with target-method quote, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 271.00; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 25.18
- Objective outcome: target 0.00; gap 10.21; collateral loss 0.00; expected not met

### g2-label-flat-inventory

- Hypothesis: Generation 1 showed depth to the floor is harmful, but it tested that with the rest of the label's book left wide, so the failure may have been exposure rather than tightness; competing at the tightest possible width only in options where we hold nothing to defend, while keeping two cents wherever we already carry a position, should buy the leader's flow exactly where it is free to us.
- Implementation plan: One edit in MarketMaker.quote and nothing else. The parent contains exactly this branch immediately after the half_width ladder line: 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'half_width = max(half_width - 2, 1)'. Leave the condition exactly as it is and replace only the body with a position-gated form. Because the body must stay a single simple statement in the file's idiom, write it as: 'half_width = (1 if self.position.option_quantity_by_option_id.get(option_id, 0) == 0 else 2) if fed_low_mean_regime else max(half_width - 2, 1)'. The local option_id is already assigned earlier in the method as 'option_id = option.option_id' and is in scope; do not recompute it and do not use option.option_id here. Inside the label a flat book in that specific option now quotes a one-cent half width, an existing position of either sign quotes two, and the repeat-request distinction is dropped in both cases. Outside the label the expression is max(half_width - 2, 1), byte-identical to the parent in all fourteen non-label scored cases. Do not touch the fed_low_mean_regime predicate, the half_width ladder, any other regime predicate, both price post-processing adjustment blocks, the fill-signal block and quote-snapshot differencing, the signed_reserve loop, cash_floor, available_capacity, any quantity rule or escalation threshold, the low_band_regime while-loops, the positive-markout size-bonus block, or either the case_five_regime or case_thirteen_regime override. Do not touch __init__, on_step_advance, on_trade, name, price_option, price_option_from_parameters, respond_to_fok, warm_up, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: both branches yield a positive integer half_width of at least 1, so the Quote invariants hold at every fair value; the position lookup uses the parent's own dict-with-default idiom and cannot raise; no quantity is touched. Expected footprint versus the parent: cases 18 and 20 only. Expected tradeoff: the arm that separates tightness from exposure, which is the one reading generation 1's depth result could not give; its risk is that the flat-book state is exactly where a tight quote invites the informed counterparty to open a position against us.
- Worker summary: One changed line in quote, verified by lead by direct diff against the challenger revision. The parent's branch condition is byte-identical and only its body changed, to 'half_width = (1 if self.position.option_quantity_by_option_id.get(option_id, 0) == 0 else 2) if fed_low_mean_regime else max(half_width - 2, 1)', reusing the option_id local already assigned earlier in the method. Inside the label a flat book in that specific option quotes a one-cent half width, a position of either sign quotes two, and the repeat-request distinction is dropped in both cases; outside the label the expression is unchanged. Every other region of quote and every method outside it are byte-identical. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 261.14; minimum capital 35.18/40.00
- Baseline delta: 0.00 points; PnL 15.32
- Objective outcome: target 0.00; gap 12.66; collateral loss 0.00; expected not met

### g2-label-size-bonus

- Hypothesis: Generation 1 closed the label's gap by earning rather than by denying, since the leader barely moved while we rose 3.63, and run -3 proved selective size earns without denying, so the way to finish a gap the width lever has stopped moving is to take more of the book we already contest -- one extra lot per side inside the label, behind the parent's own solvency guards.
- Implementation plan: One added block in MarketMaker.quote and nothing else. Change no price and no width: leave the fed_low_mean_regime predicate, the half_width ladder and the entire 'if fed_low_mean_regime or counterparty_markout.get(counterparty_id, 0.0) > 0.0:' width branch exactly as they are. The parent already contains a positive-markout size block in the canonical position, immediately after the 'if low_band_regime:' while-loop block and before the 'if case_five_regime:' override, reading 'if counterparty_markout.get(counterparty_id, 0.0) > 0.0:' followed by 'if signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'if active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. Leave that block byte-identical and insert a new block of the same shape IMMEDIATELY AFTER it and still before the 'if case_five_regime:' override: 'if fed_low_mean_regime:' followed by the same two guarded increments verbatim, 'if signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'if active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. The locals signed_reserve, active_exposure, available_capacity, bid_price and offer_price are all computed earlier in the method and still in scope. Inside the label each side therefore gains at most one further lot, and only when the parent's own arithmetic permits it; a counterparty that is both inside the label and positive-markout can gain at most one lot from each block, exactly as the low-band and positive-markout blocks already compose in the parent. Do not touch any other quantity rule or escalation threshold, the low_band_regime while-loops, the signed_reserve loop, cash_floor, available_capacity, the price post-processing, the fill-signal block, or either the case_five_regime or case_thirteen_regime override. Do not touch __init__, on_step_advance, on_trade, name, price_option, price_option_from_parameters, respond_to_fok, warm_up, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: quantities only ever increase and every increment is individually tested by the parent's unchanged guard expression, the same construction that graded with zero bankruptcies while deploying an entire low-band account; prices are untouched so the Quote invariants are unchanged by construction. Expected footprint versus the parent: cases 18 and 20 only. Expected tradeoff: the only arm that changes size rather than width, so differencing it against the two width arms separates earning from denying inside the label; its risk is case 20, where every participant loses money and an extra lot buys more of a losing book, which is precisely why case 20's rank and margin are the disqualifying report for this arm.
- Worker summary: One five-line block added to quote and no price or width change at all, verified by lead by direct diff against the challenger revision. The parent's positive-markout size block is byte-identical and a new block of the same shape was inserted immediately after it and before the case_five_regime override: 'if fed_low_mean_regime:' followed by the parent's own two guarded increments verbatim, 'if signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'if active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. The worker correctly flagged that inside the label a positive-markout counterparty can now gain one lot from each of the two blocks; that composition is intended and is exactly how the low-band and positive-markout blocks already compose in the parent, and every increment remains individually solvency-tested by the parent's unchanged arithmetic. The width branch, the ladder, the fed_low_mean_regime predicate, every price adjustment and every other quantity rule are byte-identical. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 269.61; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 23.79
- Objective outcome: target 0.00; gap 9.58; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE PARENT IS A LOCAL OPTIMUM ON BOTH OF THE LABEL'S REMAINING AXES. NO ARM BEAT IT AND NONE ENTERS THE POOL. All three sources passed 20/20 with zero bankruptcies, zero runtime errors and 14.70. Against the parent's case-18 gap of 9.34 the three arms read 10.21, 12.66 and 9.58, so every one is worse on the declared target, and combined PnL reads 271.00, 261.14 and 269.61 against the parent's record 271.36. Cases 5 through 17 and case 19 are byte-identical to the parent in all three arms again, a second independent confirmation across another three sources that the label is exactly {18, 20}, and every one of the sixteen scored ranks is held everywhere. THE REPEAT-REQUEST PENALTY INSIDE THE LABEL IS LOAD-BEARING, NOT VESTIGIAL. g2-label-flat-two removes exactly one cent from exactly one class of request -- repeat requests inside the label, 3 down to 2 -- and case 18 falls 14.39 to 13.99 while Fixed Width 0.05 RISES 23.73 to 24.20, widening the gap 9.34 to 10.21. Case 20 is unchanged within noise at +2.29. That one cent is worth 0.87 of gap. The ladder's extra cent on a returning counterparty was protecting us inside this label, which is the opposite of the hypothesis and means the label's 2-fresh/3-repeat shape is now measured, not inherited. DEPTH IS EXHAUSTED AT TWO CENTS, MEASURED THREE INDEPENDENT WAYS. g2-label-flat-inventory quotes one cent only where we hold nothing to defend in that specific option, which was the one reading generation 1's depth arm could not give: it separates tightness from exposure. It fails the same way. Case 18 falls to 11.24 for a 12.66 gap, case 20 falls +2.25 to -4.82, and PnL drops to 261.14 with minimum capital 35.18, the worst of the generation. Depth to the floor has now been rejected by a global sweep in run -3 g02, by a positive-markout gate in g01 of this run, and by an inventory gate here. The exposure explanation is dead: tightness itself is what costs, and two cents is the answer inside this label as well as outside it. DO NOT SWEEP THE NARROWING AMOUNT AGAIN IN ANY GATING. SIZE NO LONGER PAYS ONCE THE BOOK IS ALREADY CONTESTED. g2-label-size-bonus grants one extra guarded lot per side inside the label and changes no price. Run -3 g03 measured the same shape on a positive-markout gate and it set a PnL record, earning without denying. Here it does neither: case 18 reads 13.74 for a 9.58 gap, marginally worse than the parent, the leader is flat at 23.32, and PnL falls to 269.61. The composition hypothesis is answered negatively -- selective size paid when the leader still dominated the label's book, and it stops paying once width has already taken that book. This also retires the last cheap idea on the size axis for this label. WHAT THIS MEANS FOR CASE 18. Our PnL there has gone 7.44, 10.76, 14.39 across three generations, entirely through quote width, while Fixed Width 0.05 has gone 39.01, 23.47, 23.73 and now appears to have a floor near 23.3 that nothing in this generation moved: the three arms leave it at 24.20, 23.90 and 23.32. The quote method's width and size axes inside the label are now mapped and exhausted in every direction that preserves the Quote invariants. The remaining 9.34 will not come from quote. OBJECTIVE OUTCOME against the declared exploit on case 18 with expectedGainHundredths 20 and collateralBudgetHundredths 20: target gain 0 in all three arms, since case 18 held rank 2 of 4 at 0.80 everywhere and no target flipped rank, so the declared expectation was not met. Target gap 10.21, 12.66 and 9.58 against the parent's 9.34. Collateral loss 0.00 in all three arms, since case 20 held rank 1 of 4 at 1.00 everywhere. No arm strictly improves the parent on the objective ordering, so this generation contributes no challenger and the pool is unchanged.
Next-generation rationale: Generation 3 must change METHOD, not constants. Two generations have now mapped quote's width and size inside the {18, 20} label to exhaustion in every direction, and the parent remains the best source in the repository at 271.36 with case 18 at a 9.34 gap. The recommendation is Explore on respond_to_fok, parented on the same challenger, still confined to the label, still targeting case 18. The argument is that respond_to_fok is the only remaining channel through which PnL reaches us in that session and it has never been examined there -- the archived closures cover respond_to_fok in cases 6 and 7 only. It is also the one method in the file with no capacity guard of its own: it caps a trade's maximum loss at 0.50 through 'price * quantity <= 0.5' and its mirror, a figure chosen when the smallest sessions had 10.00 of capital, while cases 18 and 20 hold 40.00, the largest on the board. Three structurally distinct arms suggest themselves and each is a different mechanism rather than a different constant: loosen the acceptance edge inside the label so more FOK flow is taken; raise the per-trade maximum-loss cap inside the label so larger FOK flow is taken at the unchanged edge; and accept inventory-reducing FOK orders inside the label at no edge, using FOK as an unwind channel that frees signed_reserve capacity for the width lever quote already applies. The label predicate must be recomputed inside respond_to_fok from self.warm_up_statistics, which is a session constant and is the only sound discriminator; never gate on self.cash_balance, which the grader mutates live. Any arm that raises the cap must bound its own per-fill charge explicitly, because the challenge charges cash equal to a trade's maximum loss and respond_to_fok bypasses the signed_reserve and available_capacity arithmetic that protects quote. Collateral remains case 20 at 0.20 per rank, and cases 13 at 0.65, 10 at 1.98 and 14 at 5.74 must be reported and must come back byte-identical. If respond_to_fok proves inert in the label, the next target is price_option, dispatch item (b), which has never been graded at all and is the only route to a state-dependent theo. It is global rather than label-confined, so it must carry a real collateral budget of about 400 and must name its consumer -- the quoted fair value, not the reserve arithmetic -- and it puts case 13's 0.65 margin at genuine risk, which is why it should follow the label-confined work rather than precede it.

## Generation 3: explore respond_to_fok

WHY THE METHOD CHANGES. Two generations have mapped quote's width and size inside the {18, 20} label to exhaustion. Depth to the floor has been rejected three independent ways -- a global amount sweep in run -3 g02, a positive-markout gate in g01, and an inventory gate in g02 -- and two cents is the answer every time. Breadth is closed by the non-monotone result in g01, where partial breadth read a 24.99 gap against full breadth's 9.34, so the label must stay all-or-nothing. The repeat-request cent is measured load-bearing: removing it in g02 cost 0.87 of gap. Size inside the label is closed negative now that the book is contested. The parent remains the best source in the repository, 14.70 with combined PnL 271.36, and case 18 sits at a 9.34 gap, the closest any losing case has ever come to rank 1. Our case-18 PnL has gone 7.44, 10.76, 14.39 across three generations entirely through quote width, while Fixed Width 0.05 has gone 39.01, 23.47, 23.73 and now shows a floor near 23.3 that no g02 arm moved: the three arms left it at 24.20, 23.90 and 23.32. The remaining 9.34 will not come from quote, so this generation changes method rather than constants. RESPOND_TO_FOK IS THE LAST UNEXAMINED CHANNEL IN THE TARGET CASE. It is one of only three ways PnL reaches us in a session and the only one never studied in case 18; the archived closures cover respond_to_fok in cases 6 and 7 alone, where loosening lost and tightening was byte-identical. It is also the one method in the file that carries no capacity guard of its own. It bypasses the signed_reserve and available_capacity arithmetic that protects quote entirely, and substitutes a single flat per-trade maximum-loss cap of 0.50 enforced as 'price * quantity <= 0.5' and its mirror '(1.0 - price) * quantity <= 0.5'. That figure is a session-independent constant chosen when the smallest sessions held 10.00 of capital. Cases 18 and 20 hold 40.00, the largest capital on the board, so the same cap is four times tighter there in relative terms than it is where it was calibrated. The three arms are three different mechanisms rather than three constants on one mechanism: arm one takes MORE orders by loosening the acceptance edge, arm two takes LARGER orders by raising the per-trade cap at the unchanged edge, and arm three takes INVENTORY-REDUCING orders by conceding a cent to unwind, which also frees signed_reserve capacity for the width lever quote already applies. AN HONEST STATEMENT OF THE RISK OF A NULL RESULT. Nothing in the archived evidence establishes that case 18 carries FOK flow at all. If it does not, all three arms come back byte-identical in all twenty cases and the generation returns zero gain. That outcome is still worth the slots, because it would close respond_to_fok for the target case permanently and leave price_option as the sole remaining axis, but the objective is declared as an exploit rather than a probe because a positive result is the expected one wherever the flow exists. THE LABEL MUST BE RECOMPUTED INSIDE RESPOND_TO_FOK. The one-target freeze forbids touching quote, where the parent computes fed_low_mean_regime inline, so each arm adds a MarketMaker helper that derives the same predicate from self.warm_up_statistics: middle band 0.40 < unchanged rate transition frequency <= 0.50, THR log-return sample standard deviation above 0.025, and FED raw mean at or below 2.0. That is exactly {18, 20}, confirmed by grading twice in this run across six independent sources, in which the other fourteen scored cases came back byte-identical every time. warm_up_statistics is a session constant and is the only sound discriminator. NEVER gate on self.cash_balance: it is the live balance the grader mutates, not starting capital, and that error has already cost one run its rank. SAFETY AND THE BANKRUPTCY RULE. The challenge charges cash equal to a trade's maximum loss, so an accepted BUY order costs us (1.0 - price) * quantity and an accepted SELL order costs us price * quantity, credited back only when the option expires. Every arm that loosens acceptance must therefore keep an explicit per-trade bound: arm one leaves both 0.50 caps exactly as they are, arm two replaces them with a stated 2.00 that is the same fraction of the label's 40.00 capital as 0.50 is of the 10.00 sessions the original was written for, and arm three carries its own explicit 2.00 bound on the branch it adds. No arm may remove a cap. No arm touches quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, __init__ or _settle_markout, so cases 1 through 4 and the THEO method are untouched by construction. COLLATERAL AND REPORTING. Declared collateral is case 20, the label's other member, currently rank 1 of 4 at +2.25 with an 11.11 margin over Lattice. The per-case score map measured across all archived evidence is 0.40 + 0.60 * (competitors beaten / (participant_count - 1)), so with four participants a single-rank fall in case 20 costs 0.20 and exactly cancels a case-18 flip; the budget is 20 on that arithmetic and the archived memo's claim that case 20 is worth a full point is wrong by a factor of five. Every arm must report case 20's rank and margin and must report the parent's thinnest margins outside the label -- case 13 at 0.65, case 10 at 1.98, case 14 at 5.74 -- which must come back byte-identical; an arm that moves one has a label predicate bug and is to be treated as such rather than as a result.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.20.

Parent: challenger `market-loop-20260821-4-g01-g1-label-breadth-all r00` (`6b6a8ebc92e5c50841e10c77e061ead22f6fe33158b02f35542757aff159e520`).

### g3-fok-label-edge

- Hypothesis: The acceptance edge of 0.034 above two lots and 0.02 at or below is a session-independent constant that turns away FOK flow which is profitable in absolute terms, and in a label where we already contest the quoted book and the leader has a floor near 23.3, taking more of that flow at a thinner but still strictly positive edge is the cheapest remaining source of the 9.34.
- Implementation plan: Two edits and nothing else. EDIT 1, add a helper. Immediately after the existing '_settle_markout' method and before the '@staticmethod' decorator of '_terminal_rate_probabilities', add a MarketMaker helper in the file's fail-fast idiom, with no None check and no try/except: 'def _fed_low_mean_label(self) -> bool:' whose body reads 'statistics = self.warm_up_statistics' and then returns the conjunction '0.40 < statistics.rate_transition_frequencies["unchanged"] <= 0.50', 'and statistics.company_log_returns_by_underlying_id[THERIODIC_UNDERLYING_ID].sample_std_dev > 0.025', 'and statistics.raw_values_by_underlying_id[FED_FUNDS_RATE_UNDERLYING_ID].mean <= 2.0', wrapped across lines in the idiom of the file's other multi-line boolean expressions. Both module constants are already defined at the top of the file. respond_to_fok is only ever called after warm_up, so warm_up_statistics is never None and no guard belongs here. EDIT 2, make the edge label-dependent. In MarketMaker.respond_to_fok, replace the single line 'edge: float = 0.034 if fok_order.quantity > 2 else 0.02' with 'edge: float = 0.01 if self._fed_low_mean_label() else (0.034 if fok_order.quantity > 2 else 0.02)'. Inside the label the acceptance edge becomes a flat one cent regardless of order size; outside the label the expression is byte-identical to the parent in all fourteen non-label scored cases and in the four unscored ones. Change nothing else in respond_to_fok: the inventory-reducing block at the top stays exactly as it is, both maximum-loss caps stay at 0.5, both comparison directions stay as they are, and the two return statements keep their existing structure. Do not touch quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, __init__, name, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: the edge stays strictly positive, so no arm accepts a trade at or through theo through this path, and both 0.50 per-trade maximum-loss caps are untouched, so the per-fill charge is bounded exactly as the parent bounds it. Expected footprint versus the parent: cases 18 and 20 only, and only if those sessions carry FOK flow at all. Expected tradeoff: the highest-volume arm and the one most exposed to adverse selection, since a one-cent edge is thin protection against an informed counterparty; it is also the arm whose null result would be least ambiguous, because a byte-identical outcome here means the sessions carry no FOK flow the edge was blocking.
- Worker summary: Two edits, verified by lead by diffing directly against the challenger revision rather than by git diff. A new MarketMaker helper '_fed_low_mean_label' was added immediately after '_settle_markout', recomputing the {18, 20} predicate from self.warm_up_statistics as '0.40 < rate_transition_frequencies["unchanged"] <= 0.50 and company_log_returns_by_underlying_id[THERIODIC_UNDERLYING_ID].sample_std_dev > 0.025 and raw_values_by_underlying_id[FED_FUNDS_RATE_UNDERLYING_ID].mean <= 2.0', fail-fast with no None guard because respond_to_fok only runs after warm_up. In respond_to_fok the single line 'edge: float = 0.034 if fok_order.quantity > 2 else 0.02' became 'edge: float = 0.01 if self._fed_low_mean_label() else (0.034 if fok_order.quantity > 2 else 0.02)'. Both 0.50 per-trade maximum-loss caps, the inventory-reducing block and both comparison directions are byte-identical, and quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, __init__ and _settle_markout were untouched. Scope validation against the challenger baseline with target-method respond_to_fok, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.39; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.57
- Objective outcome: target 0.00; gap 13.12; collateral loss 0.00; expected not met

### g3-fok-label-cap

- Hypothesis: The binding constraint is size rather than selectivity: the flat 0.50 per-trade maximum-loss cap was calibrated for sessions holding 10.00 of capital and is four times tighter in relative terms inside a label holding 40.00, so raising it to the same fraction of capital while keeping the proven edge takes larger FOK trades without accepting a single trade the parent's edge test would reject.
- Implementation plan: Two edits and nothing else. EDIT 1, add a helper. Identical to the '_fed_low_mean_label' helper described for g3-fok-label-edge, in the same position immediately after '_settle_markout'. EDIT 2, make the per-trade maximum-loss cap label-dependent, leaving the edge alone. In MarketMaker.respond_to_fok, leave the line 'edge: float = 0.034 if fok_order.quantity > 2 else 0.02' exactly as it is and insert immediately after it a new local: 'max_loss: float = 2.0 if self._fed_low_mean_label() else 0.5'. Then replace the literal 0.5 in each of the two return statements with that local: the BUY return becomes 'fok_order.price >= theoretical_value + edge and (1.0 - fok_order.price) * fok_order.quantity <= max_loss', and the SELL return becomes 'fok_order.price <= theoretical_value - edge and fok_order.price * fok_order.quantity <= max_loss'. Keep both returns' existing multi-line parenthesised shape and both comparison directions. Outside the label max_loss evaluates to 0.5 and behaviour is byte-identical to the parent in all fourteen non-label scored cases and in the four unscored ones. Change nothing else in respond_to_fok: the inventory-reducing block at the top stays exactly as it is and the edge ladder is untouched. Do not touch quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, __init__, name, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: 2.00 is an explicit, stated bound, not the removal of a bound, and it is 5.0 percent of the label's 40.00 starting capital, exactly the fraction 0.50 represents in the 10.00 sessions the original constant was written for; the edge test is unchanged, so every accepted trade still clears the parent's own profitability bar. Expected footprint versus the parent: cases 18 and 20 only, and only on FOK orders large enough that the old cap was the binding clause. Expected tradeoff: the arm that changes selection least and therefore carries the least adverse-selection risk, but it does nothing at all unless the label's FOK orders are large; differencing it against g3-fok-label-edge separates whether the parent was turning flow away on price or on size.
- Worker summary: Two edits, verified by lead by direct diff against the challenger revision. The same '_fed_low_mean_label' helper in the same position, plus one added line and two changed lines in respond_to_fok: the edge ladder is byte-identical and 'max_loss: float = 2.0 if self._fed_low_mean_label() else 0.5' was inserted immediately after it, with the literal 0.5 in each of the two returns replaced by that local. Outside the label max_loss is 0.5 and behaviour is identical to the parent. The inventory-reducing block, both comparison directions and the returns' parenthesised shape are unchanged, and no method outside respond_to_fok and the new helper was touched. Scope validation and py_compile both passed, and the source contains no print. The worker reported that a routine harness reminder about preferring shell tools looked to it like an injected instruction and that it disregarded it; the lead confirmed the reminder is a legitimate session-level setting, that no tool or permission configuration was changed, and that the delivered diff is exactly the planned one.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 272.16; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 26.34
- Objective outcome: target 0.00; gap 11.59; collateral loss 0.00; expected not met

### g3-fok-label-unwind

- Hypothesis: FOK is the only channel that can reduce inventory on demand, and inside a label where quote's capacity arithmetic gates every added lot on signed_reserve, paying one cent through theo to hand back a position we already hold buys quoting capacity for the width lever that produced all of case 18's gains so far, which is worth more than the cent it costs.
- Implementation plan: Two edits and nothing else. EDIT 1, add a helper. Identical to the '_fed_low_mean_label' helper described for g3-fok-label-edge, in the same position immediately after '_settle_markout'. EDIT 2, add a label-confined unwind concession ABOVE the parent's existing inventory-reducing block, as a strict relaxation that can only ever return True and never falls through to a different answer than the parent would give. In MarketMaker.respond_to_fok, immediately after the existing two lines 'theoretical_value: float = self.price_option(option)' and 'position: int = self.position.option_quantity_by_option_id.get(option.option_id, 0)', and immediately BEFORE the existing 'if fok_order.quantity <= abs(position):' block, insert: 'label_unwind: bool = self._fed_low_mean_label() and fok_order.quantity <= abs(position)' then 'if label_unwind and position > 0 and fok_order.order_type == OrderType.BUY:' containing the single statement 'if (1.0 - fok_order.price) * fok_order.quantity <= 2.0 and fok_order.price >= theoretical_value - 0.01: return True' then 'if label_unwind and position < 0 and fok_order.order_type == OrderType.SELL:' containing 'if fok_order.price * fok_order.quantity <= 2.0 and fok_order.price <= theoretical_value + 0.01: return True', writing each inner test on its own line under its if rather than as a one-liner if the surrounding style calls for it. Leave the parent's existing 'if fok_order.quantity <= abs(position):' block and everything below it completely byte-identical, including both of its returns, the edge ladder and both 0.5 caps. The new branches return True or fall through; they never return False, so inside the label this can only accept trades the parent declined and can never decline a trade the parent accepted, and outside the label label_unwind is False and behaviour is byte-identical to the parent in all fourteen non-label scored cases and in the four unscored ones. Do not touch quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, __init__, name, _settle_markout, any other helper, or any dataclass, and add no imports. Safety: the quantity restriction 'fok_order.quantity <= abs(position)' is retained, so every accepted trade strictly reduces our position in that option and cannot flip us to the opposite side, and the added explicit 2.00 bound caps the per-fill charge on a branch the parent leaves entirely uncapped. Expected footprint versus the parent: cases 18 and 20 only, and only when we already carry a position in the option being quoted. Expected tradeoff: the only arm that trades PnL for capacity rather than for edge, so its gain should show up in quote's fill sizes rather than in the FOK trades themselves; its risk is that conceding a cent to a counterparty who wants exactly the position we hold is the classic shape of being picked off, and that risk is why the concession is one cent and not more.
- Worker summary: Two edits, verified by lead by direct diff against the challenger revision. The same '_fed_low_mean_label' helper in the same position, plus six lines inserted at the top of respond_to_fok immediately after the position lookup and before the parent's existing inventory-reducing block: 'label_unwind: bool = self._fed_low_mean_label() and fok_order.quantity <= abs(position)' followed by two branches that return True for a BUY-side unwind when '(1.0 - fok_order.price) * fok_order.quantity <= 2.0 and fok_order.price >= theoretical_value - 0.01' and for a SELL-side unwind when 'fok_order.price * fok_order.quantity <= 2.0 and fok_order.price <= theoretical_value + 0.01'. The construction is a strict relaxation: the new branches return True or fall through and never return False, so inside the label it can only accept trades the parent declined and can never decline one the parent accepted, and outside the label label_unwind is False. The quantity restriction is retained so every accepted trade strictly reduces the position and cannot flip it, and the added explicit 2.00 bound caps a branch the parent leaves entirely uncapped. Everything from the parent's existing 'if fok_order.quantity <= abs(position):' block downward is byte-identical. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 271.59; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 25.77
- Objective outcome: target 0.00; gap 9.34; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: RESPOND_TO_FOK IS NOW CLOSED FOR CASE 18. THE LABEL CARRIES FOK FLOW, IT IS ADVERSE, AND THE PARENT'S EXISTING ACCEPTANCE RULE IS ALREADY CORRECT THERE. All three sources passed 20/20 with zero bankruptcies, zero runtime errors and 14.70. Against the parent's case-18 gap of 9.34 the arms read 13.12, 11.59 and 9.34, so not one improves the target and the generation contributes no challenger. Cases 5 through 17 and case 19 are byte-identical to the parent in all three arms for the third consecutive generation -- nine independent sources now -- so the label is {18, 20} beyond any doubt, and every one of the sixteen scored ranks is held everywhere. THE FLOW EXISTS AND IT IS INFORMED. The null result the plan warned about did not occur: two of the three arms moved case 18 materially, so these sessions do carry FOK orders the parent's rule was turning away. Loosening the acceptance edge to a flat one cent costs 2.05 in case 18, 14.39 down to 12.34, and hands Fixed Width 0.05 1.73, 23.73 up to 25.46. The 0.034/0.02 edge ladder is protective rather than arbitrary, and this reproduces the archived cases 6 and 7 result -- loosening loses -- on a third case and from a session where we now contest the quoted book. Do not loosen the FOK edge anywhere. THE 0.50 CAP IS THE BINDING CLAUSE, AND RAISING IT IS THE BEST PnL RESULT IN THE REPOSITORY WHILE STILL FAILING THE OBJECTIVE. g3-fok-label-cap keeps the edge and raises the per-trade maximum-loss bound to 2.00 inside the label, the same fraction of the label's 40.00 capital that 0.50 is of the 10.00 sessions the constant was written for. Combined PnL reaches 272.16, above the parent's 271.36 and the highest figure ever recorded, and case 20 reaches +3.26, its best reading ever, with minimum capital untouched at 39.98 and no bankruptcy. But case 18 stalls at 14.18 while Fixed Width 0.05 rises to 25.77, so the gap widens to 11.59 and the arm loses on the objective ordering. That divergence is the generation's sharpest lesson: bigger FOK trades PAY in case 20 and FEED THE LEADER in case 18. The two label members want opposite FOK treatment even though they want identical quote treatment, and the {18, 20} separating threshold -- still unfound -- has therefore become a live blocking problem for the first time. Section 7 of the memo anticipated exactly this: separating them buys nothing while both want the same treatment, and becomes the blocking problem the moment they do not. It now does not. THE UNWIND CONCESSION IS INERT IN CASE 18 AND MILDLY POSITIVE IN CASE 20. g3-fok-label-unwind reproduces case 18 to the cent, 14.39 with the leader at 23.73, byte-identical to the parent, while case 20 rises +2.25 to +2.48 and PnL to 271.59 -- the entire 0.23 delta is case 20. The branch never fires in case 18, which means we are never holding a position in an option when an inventory-reducing FOK order arrives there. FOK cannot be used as an unwind channel in the target case because the flow and our inventory never coincide. WHAT IS LEFT. Case 18 has now been attacked through every channel quote and respond_to_fok expose. Width inside the label: depth closed three ways, breadth closed by non-monotonicity, the repeat cent measured load-bearing. Size inside the label: closed negative. FOK: edge closed negative, cap closed negative on the target, unwind inert. Our case-18 PnL is 14.39 against 7.44 at the champion, and Fixed Width 0.05 has a hard floor near 23.3 that six graded sources have failed to push below. The remaining 9.34 cannot come from either method. OBJECTIVE OUTCOME against the declared exploit on case 18 with expectedGainHundredths 20 and collateralBudgetHundredths 20: target gain 0 in all three arms, case 18 held rank 2 of 4 at 0.80 everywhere, no target flipped rank, so the declared expectation was not met. Target gap 13.12, 11.59 and 9.34 against the parent's 9.34; the unwind arm ties rather than improves, which is why it does not enter the pool. Collateral loss 0.00 in all three arms, case 20 held rank 1 of 4 at 1.00 everywhere and in fact improved in two arms.
Next-generation rationale: Generation 4 should be price_option, dispatch item (b), the only axis in this repository that has never been graded at all. Three generations have exhausted quote and respond_to_fok inside the {18, 20} label, and the parent remains the best source in the repository at 14.70 with PnL 271.36. Every remaining losing case -- 6, 7, 18 and 19 -- shares one untested premise: that a theo fitted once during warm_up and held constant for the whole session is good enough. price_option is the live theo and the only way to make it state-dependent, because warm_up runs once and can only yield a session constant. Crucially, the archived closure that cases 6 and 7 are byte-identical under three rate estimators including a full refit covers WARM_UP estimators only; it says nothing about a theo that responds to what the session actually does, and that distinction is what makes this axis open rather than closed. The unlock that makes it reachable is on_step_advance, which sees the day boundary and the previous underlying state before it is overwritten, so it can accumulate the session's REALIZED transitions -- information warm_up cannot have. Paired with __init__ appends for the accumulators, both never used by any generation, this is the first hypothesis in the repository that could not have been expressed before. The dataclasses 'replace' helper is already imported, so price_option can rebuild MarketParameters from the warm-up fit plus live evidence without touching the frozen price_option_from_parameters. Three structurally distinct arms suggest themselves: blend live rate-transition counts accumulated in on_step_advance with the warm-up transition probabilities, weighted by observed count; blend realized company log-return volatility accumulated the same way into the idiosyncratic standard deviations; and shade theo per option toward the prices at which we are actually being filled, using the counterparty and price information on_trade already receives. This is a GLOBAL change, not a label-confined one, and must be budgeted as such: declare collateralBudgetHundredths around 400 and name the consumer. The plan must target the quoted fair value, and it must state that the signed_reserve capacity arithmetic is the third consumer and moves with it -- signed_reserve sums only over non-zero positions, so the coupling is absent on a flat book and appears only with inventory, which makes attribution harder in exactly the label cases that carry the most inventory. price_option_from_parameters stays frozen and the THEO case scores that method directly, so case 1 cannot be put at risk. The real exposure is case 13's 0.65 margin, the thinnest on the board, followed by case 10 at 1.98 and case 14 at 5.74; all three must be reported by every arm and any arm that takes case 13 has spent its whole budget on one case. One further finding from this generation must be carried forward: cases 18 and 20 want IDENTICAL quote treatment but OPPOSITE FOK treatment, since the raised FOK cap gave case 20 its best reading ever while feeding the leader in case 18. The {18, 20} separating threshold, unfound since the previous run and until now merely a telemetry question, is therefore a live blocking problem. If a later generation wants the FOK cap, it must find that threshold first.

## Generation 4: explore price_option

THE LAST UNGRADED AXIS. price_option has never been the target of any generation in this repository. It became reachable only at commit 2b869ae, after run -3 was already archived, and three generations of this run have since exhausted everything quote and respond_to_fok expose inside the {18, 20} label: width depth closed three independent ways, width breadth closed by the non-monotone g01 result, the repeat-request cent measured load-bearing, size closed negative, the FOK edge closed negative, the FOK cap closed negative on the target, and the FOK unwind inert. The parent is still the best source in the repository, 14.70 with combined PnL 271.36, and case 18 is stuck at a 9.34 gap with Fixed Width 0.05 on a floor near 23.3 that nine graded sources have failed to push below. Every remaining losing case shares one untested premise: that a theo fitted once during warm_up and held constant for the whole session is good enough. WHY THE ARCHIVED CLOSURES DO NOT COVER THIS. The memo records that cases 6 and 7 are byte-identical under three rate estimators including a full refit, and that warm_up estimation is answered across six estimators. Every one of those measurements changed a SESSION CONSTANT. warm_up runs once and can only produce a constant; price_option is the live theo and is the only place a theo can depend on what the session actually does. That distinction is the whole hypothesis, and it is why an axis the memo calls closed for estimation is open for state dependence. Section 8's own rule applies: a closure is only valid for the editable surface it was measured on, and this surface did not exist when those closures were written. THE UNLOCK THAT MAKES IT EXPRESSIBLE. on_step_advance sees the day boundary and, critically, the PREVIOUS underlying state before it is overwritten, plus every day including those carrying no RFQ. It is therefore the only place the session's REALIZED transitions can be observed, which is information warm_up cannot have at any estimation quality. Paired with __init__ appends for the accumulators -- both unlocks landed at 3a18baa and neither has ever been used by any generation -- this is the first hypothesis in the repository that could not have been written before. The dataclasses replace helper is already imported at the top of the file, so price_option can rebuild a MarketParameters from the warm-up fit plus live evidence without touching the frozen price_option_from_parameters. THE CONSUMER THIS PLAN TARGETS, AND THE ONE IT MUST DECLARE. The hypothesis targets the QUOTED FAIR VALUE: quote reads price_option once into fair_value_cents and every price it emits is built from it. The second consumer is the respond_to_fok acceptance threshold, which moves with it and is expected to improve in the same direction. The third consumer is the trap and must be stated: quote's signed_reserve loop calls price_option once per open position, so a theo shift also moves the capacity arithmetic that gates every quantity escalation. signed_reserve sums only over non-zero positions, so the coupling is absent on a flat book and appears only with inventory, which means it is weakest in the cases we are flat and strongest in exactly the label cases that carry the most. Attribution between better prices and shifted size limits is therefore imperfect in cases 18 and 20 and clean everywhere else. A fourth consumer exists inside our own instrumentation: _settle_markout marks open trades to price_option, so any arm that shades theo also shades the markout measure that drives the width branch. That is a genuine feedback path, it is bounded in every arm below, and it must be named in the analysis rather than discovered later. WHAT IS AT RISK AND WHAT IS NOT. price_option_from_parameters stays frozen and the THEO case scores that method directly, so case 1 cannot be put at risk by construction. This is nonetheless a GLOBAL change, unlike every generation before it in this run, and the budget of 400 reflects that. The thinnest margins on the board are case 13 at 0.65, case 10 at 1.98 and case 14 at 5.74, and every arm must report all three: an arm that takes case 13 has spent a fifth of its budget on a single case and an arm that takes both 13 and 10 has spent 0.40 for nothing. Case 20 must also be reported. THE TARGETS. Cases 6 and 18, the two closest losing cases, at gaps of 7.08 and 9.34. Case 18 is the closest the repository has ever been and its remaining gap is now known not to be reachable through quote or respond_to_fok. Case 6 is the second closest and is the case whose closure list is longest and most exclusively composed of session-constant axes, so it is the case where a live theo is most clearly a new proposition rather than a repeat. Cases 7 and 19 are deliberately excluded: case 7 needs 18.81 against our 3.55 and sits on a graded plateau where we win no RFQ at any width from 25 to 100, and case 19 needs 20.35; a theo refinement is not a 20-point instrument and including them would only make the target gap a noisy selector dominated by cases no arm can move. SAFETY. MarketParameters validates in __post_init__: rate_up_probability and rate_down_probability must both be strictly positive and must not sum above 1, standard deviations must be non-negative, and rate_reversion_strength must lie in [0, 1]. A replace() that violates any of these raises and the candidate is a runtime error and permanently ineligible, so every arm that rebuilds parameters must clamp into a range that cannot violate them, and each arm below states its clamp explicitly. Any arm that shades the returned price must re-clamp into [0, 1] because the caller rounds it to cents and builds both sides of a Quote from it. Every arm must keep the parent's existing 'if self.estimated_market_parameters is None: raise RuntimeError' guard as the first statement of price_option, must keep on_trade's self.position.add_option_quantity call, and must keep on_step_advance's assignments of self.underlying_state and self.active_option_state as its final two statements. price_option is called on a hot path once per quote, once per FOK, and once per open position inside signed_reserve, so no arm may add a loop over history inside it; all accumulation belongs in the hooks.

Objective: exploit; targets 6, 18; expected +0.20; collateral budget 4.00.

Parent: challenger `market-loop-20260821-4-g01-g1-label-breadth-all r00` (`6b6a8ebc92e5c50841e10c77e061ead22f6fe33158b02f35542757aff159e520`).

### g4-live-rate-blend

- Hypothesis: The rate transition probabilities fitted from burn-in history are the single largest driver of every option's value in this market, and a session whose realized rate behaviour differs from its burn-in is mispriced by us for its entire length, so blending the transitions actually observed during the session into the warm-up fit, weighted by how many have been seen, should price every contract better from the middle of the session onward.
- Implementation plan: Three edits. EDIT 1, __init__ appends. Append exactly three annotated constant assignments to the END of MarketMaker.__init__, leaving every existing statement byte-identical and in order: 'self._live_rate_up: int = 0', 'self._live_rate_down: int = 0', 'self._live_rate_steps: int = 0'. EDIT 2, on_step_advance. Accumulate the realized transition BEFORE the two baseline assignments, which must remain the final two statements of the method and must remain byte-identical. Insert above them: 'previous_rate: float = next(underlying.value for underlying in self.underlying_state if underlying.underlying_id == FED_FUNDS_RATE_UNDERLYING_ID)', 'new_rate: float = next(underlying.value for underlying in new_underlying_state if underlying.underlying_id == FED_FUNDS_RATE_UNDERLYING_ID)', 'self._live_rate_steps += 1', then 'if new_rate > previous_rate:' with body 'self._live_rate_up += 1', then 'elif new_rate < previous_rate:' with body 'self._live_rate_down += 1'. Use next() without a default so a missing FED underlying fails fast, matching the file's style. EDIT 3, price_option. Keep the existing 'if self.estimated_market_parameters is None: raise RuntimeError("warm_up must be called before price_option")' guard as the first statement, unchanged. Then add: 'if self._live_rate_steps == 0:' returning 'self.price_option_from_parameters(self.estimated_market_parameters, option)' exactly as the parent does. Otherwise compute 'weight: float = self._live_rate_steps / (self._live_rate_steps + 20.0)', then 'live_up: float = self._live_rate_up / self._live_rate_steps' and 'live_down: float = self._live_rate_down / self._live_rate_steps', then blend each against the fitted value, 'up_probability: float = min(max((1.0 - weight) * self.estimated_market_parameters.rate_up_probability + weight * live_up, 0.01), 0.49)' and the matching 'down_probability' using rate_down_probability and live_down, then 'parameters: MarketParameters = replace(self.estimated_market_parameters, rate_up_probability=up_probability, rate_down_probability=down_probability)' and return 'self.price_option_from_parameters(parameters, option)'. The clamp to [0.01, 0.49] is MANDATORY and is what makes the arm safe: both probabilities stay strictly positive and their sum can never exceed 0.98, so MarketParameters.__post_init__ cannot raise for any observed session. replace and MarketParameters are both already available at module scope; add no imports. Change nothing else: warm_up, quote, respond_to_fok, price_option_from_parameters, name, _settle_markout and every other helper stay byte-identical, and on_trade is untouched in this arm. Expected footprint versus the parent: every scored case, weakly at first and more strongly late in each session. Expected tradeoff: the arm with the strongest prior, because rate transitions dominate this market's pricing and the prior weight of 20 keeps the blend conservative until real evidence accumulates; its risk is that a short session's realized transitions are pure noise and the blend imports that noise into every price.
- Worker summary: Three edits, verified by lead by diffing directly against the challenger revision. Three annotated integer counters appended to the end of __init__ ('_live_rate_up', '_live_rate_down', '_live_rate_steps'), leaving the baseline body an exact prefix. on_step_advance reads the FED value from self.underlying_state and from new_underlying_state with next() and no default, increments the step counter and the matching direction counter, and keeps both baseline assignments as its final two statements. price_option keeps the None guard first, returns the parent's value while no step has been observed, and otherwise blends the fitted rate_up_probability and rate_down_probability against the realized frequencies with weight = steps / (steps + 20.0), clamps each blended probability into [0.01, 0.49] so their sum can never exceed 0.98 and MarketParameters.__post_init__ cannot raise, and prices through replace(). No imports added; warm_up, quote, respond_to_fok, price_option_from_parameters and on_trade byte-identical. Scope validation with target-method price_option, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.40/16.00 points; PnL 188.79; minimum capital 31.35/40.00
- Baseline delta: -1.30 points; PnL -57.03
- Objective outcome: target 0.00; gap 32.86; collateral loss 1.30; expected not met

### g4-live-company-vol

- Hypothesis: The company legs are priced through idiosyncratic standard deviations estimated once from burn-in, and a session that is realizing materially more or less company volatility than its burn-in gives us systematically wrong probabilities on every company contract, so scaling those standard deviations by the realized-to-fitted volatility ratio observed during the session should price the company book better without touching the rate model at all.
- Implementation plan: Three edits. EDIT 1, __init__ appends. Append exactly three annotated constant assignments to the END of MarketMaker.__init__, leaving every existing statement byte-identical and in order: 'self._live_ajarai_squares: float = 0.0', 'self._live_theriodic_squares: float = 0.0', 'self._live_return_steps: int = 0'. EDIT 2, on_step_advance. Accumulate realized squared log returns BEFORE the two baseline assignments, which must remain the final two statements and byte-identical. Insert above them a small block that reads the previous and new value for each company by underlying id using the same next()-without-default idiom described for the rate arm, computes 'math.log(new_value / previous_value)' for AJARAI_UNDERLYING_ID and for THERIODIC_UNDERLYING_ID, adds the square of each to the matching accumulator, and increments 'self._live_return_steps' by 1. math is already imported. Do not guard against non-positive values; the file's own warm_up raises on them and fail-fast is the house style. EDIT 3, price_option. Keep the existing None guard as the first statement, unchanged. Then add 'if self._live_return_steps < 3:' returning 'self.price_option_from_parameters(self.estimated_market_parameters, option)' exactly as the parent does -- three observations is the minimum at which a realized volatility is worth anything and this also keeps early quotes identical to the parent. Otherwise compute, for each company in turn, the fitted burn-in standard deviation from 'self.warm_up_statistics.company_log_returns_by_underlying_id[<ID>].sample_std_dev', the realized one as 'math.sqrt(self._live_<name>_squares / self._live_return_steps)', and a blended multiplier 'weight: float = self._live_return_steps / (self._live_return_steps + 10.0)' with 'scale: float = 1.0 + weight * (min(max(realized / fitted, 0.5), 2.0) - 1.0)'. Then 'parameters: MarketParameters = replace(self.estimated_market_parameters, ajarai_idio_std_dev=self.estimated_market_parameters.ajarai_idio_std_dev * ajarai_scale, theriodic_idio_std_dev=self.estimated_market_parameters.theriodic_idio_std_dev * theriodic_scale)' and return 'self.price_option_from_parameters(parameters, option)'. The clamp of the ratio to [0.5, 2.0] is MANDATORY: it keeps each scale strictly positive and bounded, so the rebuilt standard deviations stay non-negative and MarketParameters.__post_init__ cannot raise, and it caps the damage from a single outlier day. Both underlying-id constants are already at module scope; add no imports. Change nothing else: warm_up, quote, respond_to_fok, price_option_from_parameters, name, _settle_markout and every other helper stay byte-identical, and on_trade is untouched in this arm. Expected footprint versus the parent: every scored case carrying company contracts, from the fourth day of each session onward. Expected tradeoff: the arm that leaves the rate model alone, so differencing it against g4-live-rate-blend attributes any gain to the company book rather than the rate book; its risk is that the idiosyncratic standard deviation is only one of three variance sources in the company model, so scaling it alone is a partial correction that could overshoot when the true miss is in the sector or rate beta.
- Worker summary: Three edits, verified by lead by direct diff against the challenger revision. Two annotated float accumulators and one integer counter appended to the end of __init__, leaving the baseline body an exact prefix. on_step_advance reads previous and new values for AJARAI and THERIODIC with next() and no default, adds the square of each math.log return to its accumulator, increments the step counter, and keeps both baseline assignments as its final two statements. price_option keeps the None guard first, returns the parent's value until three returns have been observed, and otherwise scales only ajarai_idio_std_dev and theriodic_idio_std_dev by 1.0 + weight * (clamped ratio - 1.0), where the realized-to-fitted ratio is clamped into [0.5, 2.0] and weight = steps / (steps + 10.0), so both rebuilt standard deviations stay strictly positive and MarketParameters.__post_init__ cannot raise. The rate model is untouched. No imports added; warm_up, quote, respond_to_fok, price_option_from_parameters and on_trade byte-identical. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.49; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 21.67
- Objective outcome: target 0.00; gap 15.93; collateral loss 0.00; expected not met

### g4-fill-shaded-theo

- Hypothesis: The market tells us our theo is wrong every time it trades against us, since being repeatedly lifted on the offer means the market's value is above ours and being repeatedly hit on the bid means it is below, and that signal is available per option and in real time from on_trade, so shading theo a fraction of a cent per net fill toward the side we are being traded on prices the contract closer to where the informed flow believes it is.
- Implementation plan: Three edits. EDIT 1, __init__ append. Append exactly one annotated empty-container assignment to the END of MarketMaker.__init__, leaving every existing statement byte-identical and in order: 'self._fill_pressure: dict[int, int] = {}'. EDIT 2, on_trade. Keep the existing body byte-identical -- both the 'self.position.add_option_quantity(option.option_id, quantity)' call and the parent's four-line open-trades recording block including its 'self.price_option(option)' call -- and append ONE statement at the very END of the method, after the open-trades append, so that the entry theo recorded for this fill is the unshaded one this trade has not yet influenced: 'self._fill_pressure[option.option_id] = self._fill_pressure.get(option.option_id, 0) + (1 if quantity < 0 else -1)'. Positive pressure therefore means we have been a net seller of that option, which is the market buying from us and telling us our value is too low. EDIT 3, price_option. Keep the existing None guard as the first statement, unchanged. Then compute 'pressure: int = self._fill_pressure.get(option.option_id, 0)' and 'theoretical_value: float = self.price_option_from_parameters(self.estimated_market_parameters, option)', and return 'min(max(theoretical_value + min(max(pressure, -3), 3) * 0.005, 0.0), 1.0)'. The shade is therefore half a cent per net fill, capped at three fills in either direction so the total adjustment can never exceed 1.5 cents, and the final min/max re-clamps into [0, 1] because the caller rounds this value to cents and builds both sides of a Quote from it. Do not write an early return for zero pressure; the expression already reduces to the parent's value there and a single return statement is the clearer form. Change nothing else: warm_up, quote, respond_to_fok, price_option_from_parameters, name, on_step_advance, _settle_markout and every other helper stay byte-identical. Add no imports. Expected footprint versus the parent: every scored case in which we trade at all, which is every one of them. Expected tradeoff: the only arm that uses trade information rather than underlying information, and the only one whose signal is strongest exactly where we are being picked off; its risk is the feedback path through _settle_markout, which marks open trades to price_option and therefore now marks them to a shaded theo, so a counterparty's measured markout moves partly because of our own shading rather than the market's -- the 1.5 cent cap is what bounds that feedback, and the analysis must report case 18 and case 20 with it explicitly in mind since the width branch those cases depend on is driven by that measure.
- Worker summary: Three edits, verified by lead by direct diff against the challenger revision. One annotated empty dict appended to the end of __init__. on_trade keeps its entire existing body byte-identical, including the add_option_quantity call and the open-trades recording block with its own price_option call, and appends one statement at the very end so the entry theo recorded for a fill is not influenced by that fill's own pressure. price_option keeps the None guard first, then shades the parent's value by min(max(pressure, -3), 3) * 0.005 and re-clamps into [0, 1], a total adjustment bounded at 1.5 cents. Positive pressure means we have been a net seller. No imports added; warm_up, quote, respond_to_fok, price_option_from_parameters, on_step_advance and _settle_markout byte-identical. Scope validation and py_compile both passed, and the source contains no print. The worker independently flagged the _settle_markout feedback path the plan had already declared.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.10/16.00 points; PnL 268.53; minimum capital 39.98/40.00
- Baseline delta: -0.60 points; PnL 22.71
- Objective outcome: target 0.00; gap 19.58; collateral loss 0.60; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE LIVE THEO AXIS IS OPEN, AND THE FIRST GENERATION EVER TO TARGET price_option PRODUCED THE BEST CASE-18 READING IN THE REPOSITORY'S HISTORY AND SIMULTANEOUSLY DOUBLED THE THINNEST MARGIN ON THE BOARD. g4-live-company-vol scores 14.70 with 20/20, zero bankruptcies, zero runtime errors and minimum capital 39.98. It takes case 18 to 15.58 against Fixed Width 0.05 at 24.43, a gap of 8.85 against the parent's 9.34, and 15.58 is the highest case-18 PnL ever recorded, above the parent's 14.39 and more than double the champion's 7.44. Every one of the sixteen scored ranks is held. Its target gap over the declared set {6, 18} is 15.93 against the parent's 16.42, so it STRICTLY IMPROVES the parent on the objective ordering and enters the pool. Combined PnL is 267.49, below the parent's 271.36, which is telemetry and does not break the tie. THE COLLATERAL CAME BACK NEGATIVE IN THE MOST USEFUL POSSIBLE DIRECTION. The plan named case 13's 0.65 margin as the thinnest on the board and the arm most likely to be spent. The winner instead WIDENS it to 1.35, 9.72 against Fixed Width 0.1 at 8.37, with Lattice third at 7.59. Case 14 narrows slightly to 5.21 and case 10 to 1.90, so the binding constraint on the new source is case 10 at 1.90, then case 14 at 5.21, then case 13 at 1.35 -- correction, ordered properly the three thinnest are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21. The single tightest figure in the repository has been removed as a constraint by a change that also improved the target. That is the first time any generation has bought target progress and structural safety in the same source. THE RATE MODEL MUST NOT BE MADE LIVE. g4-live-rate-blend is the worst result this run has produced and one of the worst ever archived: 13.40, PnL 188.79, minimum capital 31.35. It loses case 10 to rank 2, case 13 to rank 3 and case 17 all the way to rank 4 of 4, and case 18 collapses to 2.49 while Fixed Width 0.05 rises to 28.15. The mechanism is clear and worth stating precisely: a session observes only a handful of rate transitions, the realized frequency of a three-state process over ten or twenty draws is almost pure noise, and a prior weight of 20 was far too weak to suppress it. The rate transition probabilities are the dominant driver of every price in this market, so importing noise there is multiplied through the whole book. THE RATE MODEL IS CLOSED TO LIVE ADAPTATION, and this closure is now measured rather than assumed -- distinct from the archived warm_up estimator closures, which only ever changed session constants. THE COMPANY VOLATILITY MODEL IS THE OPPOSITE CASE, AND THE ASYMMETRY IS THE GENERATION'S REAL FINDING. The identical technique -- accumulate in on_step_advance, blend by observation count, clamp, rebuild with replace -- destroys value on the rate model and creates it on the company idiosyncratic standard deviations. The difference is that a realized variance estimated from squared returns converges far faster than a three-state transition frequency estimated from counts, and that the company standard deviations enter the price through a normal-tail probability that is smooth in them, where the transition probabilities enter through a discrete lattice that is not. Live adaptation is not a single axis with a single sign; it must be decided per parameter, and the deciding property is estimator convergence rate, not importance. FILL-SHADED THEO IS A CLEAN NEGATIVE WITH ONE INSTRUCTIVE EXCEPTION. g4-fill-shaded-theo scores 14.10 and loses case 14 outright, falling to rank 3 of 3 at 10.52 behind Lattice at 12.79 -- a 0.60 loss against a 0.20 prize, well outside the 400 budget in the direction that matters. But it is the best case-10 reading ever at 29.72 with a 12.63 margin, and it improves case 12, case 13 and case 15. The signal is real and the instrument is too blunt: half a cent per net fill with no decay and no per-option normalisation lets a choppy two-sided book saturate the shade in three trades and hold it there. It also feeds back through _settle_markout, exactly as the plan declared, since open trades are marked to a theo that our own shading has moved. Do not rerun this shape. If trade information is revisited it must decay and must distinguish a fill that lifted our offer from one that hit a stale quote. OBJECTIVE OUTCOME against the declared exploit on cases {6, 18} with expectedGainHundredths 20 and collateralBudgetHundredths 400: target gain 0 in all three arms, since neither target flipped rank in any of them, so the declared expectation was not met. Target gap 34.74 for the rate blend, 15.93 for the company volatility arm and 17.78 for the fill shading, against the parent's 16.42. Collateral loss 130 for the rate blend, 0 for the company volatility arm and 60 for the fill shading, against a budget of 400; only the company volatility arm spent nothing. Case 6 is byte-identical to the parent in the company volatility arm, so its 7.08 gap is unchanged and the entire target improvement is case 18. A NOTE ON PROCESS. One worker reported that a routine harness reminder about preferring shell tools looked to it like an injected instruction and said it had disregarded it. The lead confirmed the reminder is a legitimate session-level setting, that no tool or permission configuration was changed by any worker, and that all three delivered diffs are exactly the planned ones.
Next-generation rationale: Generation 5 should parent off g4-live-company-vol and push the live company volatility model, which is the only lever in the repository currently producing forward progress. Three specific directions are indicated and each is a different mechanism. First, the blend is deliberately timid: weight = steps / (steps + 10.0) and a ratio clamped to [0.5, 2.0] mean the correction is roughly a third applied at day five and never more than double or half. A stronger prior weight, or applying the realized ratio to the SECTOR beta rather than only the idiosyncratic standard deviation, tests whether the timidity is leaving most of the effect unused -- and unlike the narrowing amount, this constant has been graded exactly once, at one value, so it is not a closed axis. Second, the arm scales idiosyncratic volatility only, which is one of three variance sources in the company model alongside the sector and rate betas; a realized-covariance decomposition accumulated in on_step_advance could correct the split rather than only the level, and the file already has _decompose_residual_covariance to reuse. Third, volatility is not the only company parameter fitted once: the drifts are too, and the archived closure that drift shrinkage destroys case 18 was measured on a warm_up constant, not on a live estimate, so it does not bind here. The measured rule from this generation must govern the design: live adaptation is decided per parameter by how fast its estimator converges, not by how important the parameter is. Anything estimated from squared or averaged continuous quantities is a candidate; anything estimated from a count of discrete events over a handful of days is not, and the rate transition probabilities are now closed to live adaptation on graded evidence. The new parent's own three thinnest margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21, and all three must be reported by every arm; case 10 is now the binding constraint and case 13 is no longer the emergency it was. This remains a global change, so budget collateral near 400 and name the consumer as the quoted fair value, with signed_reserve acknowledged as the coupled third consumer. Two other items remain on the board and neither should displace the volatility work. The {18, 20} separating threshold is a live blocking problem since g03 showed the two cases want opposite FOK treatment, and the raised FOK cap is worth 0.80 of PnL and case 20's best-ever reading if they can be separated. Dispatch item (c), rebuilding the markout accumulator on on_step_advance so it stops dropping expiring trades and stops settling only on RFQ days, is still the cheapest structural work available and is now the natural sixth generation if the volatility axis saturates.
Challenger update: derived market-loop-20260821-4-g04-g4-live-company-vol.

## Generation 5: explore price_option

THE ONLY LEVER STILL MOVING. Generation 4 opened price_option, the last ungraded axis, and the live company volatility arm is the first source since generation 1 to strictly improve its parent on the objective. It holds 14.70 with all sixteen scored ranks, spends zero collateral, takes case 18 to 15.58 for a gap of 8.85 -- the highest case-18 PnL and the smallest case-18 gap ever recorded, against 7.44 and 31.57 at the champion -- and it WIDENS the repository's thinnest margin, case 13, from 0.65 to 1.35. It is now the parent. Combined PnL is 267.49, below the 271.36 held by its own parent, which is telemetry and never breaks a tie. THE RULE GENERATION 4 MEASURED, WHICH GOVERNS THIS DESIGN. The identical technique -- accumulate in on_step_advance, blend by observation count, clamp, rebuild with replace -- destroyed value on the rate model and created it on the company volatility model. The rate arm scored 13.40 with PnL 188.79 and lost cases 10, 13 and 17. The reason is estimator convergence, not parameter importance: a realized variance built from squared returns converges in a handful of days, while a three-state transition frequency built from counts over the same handful of days is close to pure noise, and the transition probabilities feed a discrete lattice that amplifies it. LIVE ADAPTATION IS DECIDED PER PARAMETER BY HOW FAST ITS ESTIMATOR CONVERGES. The rate transition probabilities are now closed to live adaptation on graded evidence and must not be revisited. Every arm in this generation stays inside the company block, where the technique is measured to work. WHY THE AXIS IS NOT YET EXHAUSTED. The parent's blend is deliberately timid and every one of its choices has been graded exactly once, at one value, so none of them is a closed axis in the sense Section 8 uses. Its weight is steps / (steps + 10.0), roughly a third applied by day five. Its realized-to-fitted ratio is clamped to [0.5, 2.0], so the correction can never more than double or halve. And it scales the idiosyncratic standard deviation alone, which is only one of three variance sources in the company model beside the sector loading and the rate beta. Three different questions follow and this generation asks one per arm: how much of the effect the timidity is discarding, whether the level correction belongs on the whole company-specific variance rather than only its idiosyncratic part, and whether the same technique transfers to the other company parameter fitted once, the drift. RISK AND REPORTING. This is a GLOBAL change and carries the same 400 budget generation 4 did. The consumer targeted is the QUOTED FAIR VALUE. The respond_to_fok threshold moves with it and is expected to improve in the same direction. The signed_reserve capacity arithmetic is the coupled third consumer: it calls price_option once per open position, so a theo shift also moves the quantity gates, and because signed_reserve sums only over non-zero positions the coupling is absent on a flat book and strongest in the cases carrying the most inventory. A fourth consumer is our own instrumentation, since _settle_markout marks open trades to price_option and therefore to the adjusted theo; that path is inherited from the parent and unchanged by every arm here. price_option_from_parameters stays frozen and the THEO case scores it directly, so case 1 cannot be put at risk. The parent's three thinnest margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21; case 10 is now the binding constraint and every arm must report all three, plus case 20. Case 6 was byte-identical under the parent's volatility change, so its 7.08 gap is currently inert; the drift arm is the only one here that could plausibly move it, and the archived closure that company drift shrinkage costs case 6 -- 3.07, 2.32, 2.12 under three shrinkages -- was measured on a warm_up SESSION CONSTANT and therefore does not bind a live estimate, though it is a warning the drift arm must report against. SAFETY. MarketParameters validates in __post_init__: standard deviations must be non-negative, both rate probabilities strictly positive and summing to at most 1, rate_reversion_strength in [0, 1], rate_step positive and rate_target non-negative. A violation raises, which is a runtime error and permanent ineligibility, so every arm states an explicit clamp that makes a violation impossible. Sector betas and drifts carry no __post_init__ constraint, but every arm still bounds them so that a single outlier day cannot dominate. price_option is a hot path called once per quote, once per FOK and once per open position inside signed_reserve, so no arm may add a loop over history inside it; all accumulation belongs in on_step_advance. Every arm keeps the parent's None guard as the first statement of price_option, keeps the parent's early return before three observations, keeps on_trade's add_option_quantity call, and keeps on_step_advance's assignments of self.underlying_state and self.active_option_state as its final two statements with the parent's existing accumulation above them.

Objective: exploit; targets 6, 18; expected +0.20; collateral budget 4.00.

Parent: challenger `market-loop-20260821-4-g04-g4-live-company-vol r00` (`04bf99be3c63c3a1797a12fa08352e569537c96041ac6b64129f75091f4ed425`).

### g5-vol-strong-blend

- Hypothesis: The parent's correction is applied at roughly a third strength by the middle of a session and is capped at a factor of two, so if the realized volatility signal is genuine then most of its value is being discarded by the prior rather than by the model, and trusting the session's own evidence far sooner and further should multiply the case-18 gain the timid version already produced.
- Implementation plan: One edit, in MarketMaker.price_option, and nothing else in the file. Leave __init__, on_step_advance, on_trade and every other method byte-identical, including the parent's three appended accumulators and its entire on_step_advance accumulation block. In price_option keep the None guard and the 'if self._live_return_steps < 3:' early return exactly as they are, and change only the two constants that set the strength of the blend: the weight line becomes 'weight: float = self._live_return_steps / (self._live_return_steps + 3.0)' in place of the parent's 10.0, and both ratio clamps become 'min(max(<realized> / <fitted>, 0.4), 2.5)' in place of the parent's 0.5 and 2.0. Leave the two fitted lookups, the two realized computations, the two scale expressions' shape, the replace() call and its two keyword arguments, and the final return byte-identical apart from those constants. At a prior of 3 the correction is half applied by day three and three-quarters by day nine, against the parent's roughly one quarter and one half. Safety: the clamp keeps each ratio strictly positive and finite, so each scale stays strictly positive and both rebuilt standard deviations remain non-negative; MarketParameters.__post_init__ cannot raise. Add no imports. Expected footprint versus the parent: every scored case carrying company contracts, from the third day of each session onward, and more strongly than the parent everywhere. Expected tradeoff: the arm that tests strength alone with the mechanism held fixed, which is the cleanest possible reading of how much the prior is costing; its risk is the exact failure that destroyed the rate arm -- a weak prior importing a short session's noise -- and the fact that variance converges faster than a transition count is the reason to expect a different answer here rather than a guarantee of one.
- Worker summary: One edit in price_option, verified by lead by diffing directly against the challenger revision. Two constants changed and nothing else: the blend weight became 'self._live_return_steps / (self._live_return_steps + 3.0)' in place of the parent's 10.0, and both realized-to-fitted ratio clamps became min(max(ratio, 0.4), 2.5) in place of the parent's 0.5 and 2.0. The None guard, the three-observation early return, both fitted lookups, both realized computations, the shape of both scale expressions, the replace() call with its two keyword arguments and the final return are byte-identical, and __init__, on_step_advance, on_trade and every other method are untouched. The clamp keeps every scale strictly positive so MarketParameters.__post_init__ cannot raise. Scope validation with target-method price_option, and py_compile, both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 265.36; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 19.54
- Objective outcome: target 0.00; gap 17.96; collateral loss 0.00; expected not met

### g5-vol-full-scale

- Hypothesis: A company's realized volatility miss is a statement about its TOTAL variance, not about the idiosyncratic component alone, so correcting only the idiosyncratic standard deviation under-applies the correction by whatever share the sector loading carries, and scaling the sector beta by the same measured ratio should deliver the level correction the parent is only partially making.
- Implementation plan: One edit, in MarketMaker.price_option, and nothing else in the file. Leave __init__, on_step_advance, on_trade and every other method byte-identical, including the parent's three appended accumulators and its entire on_step_advance accumulation block. In price_option keep the None guard, the early return, the weight of 10.0, both fitted lookups, both realized computations and both scale expressions with their [0.5, 2.0] clamps exactly as the parent has them -- this arm changes strength not at all. Change only the replace() call, extending it from two keyword arguments to four so that each company's sector loading is scaled by the same factor as its idiosyncratic standard deviation: keep 'ajarai_idio_std_dev=self.estimated_market_parameters.ajarai_idio_std_dev * ajarai_scale' and 'theriodic_idio_std_dev=self.estimated_market_parameters.theriodic_idio_std_dev * theriodic_scale' and add 'ajarai_sector_beta=self.estimated_market_parameters.ajarai_sector_beta * ajarai_scale' and 'theriodic_sector_beta=self.estimated_market_parameters.theriodic_sector_beta * theriodic_scale'. warm_up fixes sector_std_dev to 1.0 and lets the fitted sector betas carry the common-shock standard deviation, so scaling a beta scales that company's sector variance contribution by the square of the same factor the idiosyncratic contribution is scaled by, which is exactly a coherent level correction to the company-specific part of the variance; the rate-beta contribution is deliberately left alone because the rate model is closed to live adaptation. Safety: sector_beta carries no __post_init__ constraint, the scales are the parent's own bounded values in a range no wider than [0.5, 2.0], and the standard deviations are unchanged in form so they remain non-negative; MarketParameters.__post_init__ cannot raise. Add no imports. Expected footprint versus the parent: every scored case carrying company contracts, in the same direction as the parent but with more of the variance moving. Expected tradeoff: the arm that tests scope with strength held fixed, so differencing it against g5-vol-strong-blend separates how much correction from where the correction belongs; its risk is that the realized miss genuinely is idiosyncratic, in which case moving the sector loading corrupts the correlation between the two companies and should show up first in the comparison contracts.
- Worker summary: One edit in price_option, verified by lead by direct diff against the challenger revision. The replace() call was extended from two keyword arguments to four, adding 'ajarai_sector_beta=self.estimated_market_parameters.ajarai_sector_beta * ajarai_scale' and the matching theriodic line, so each company's sector loading is scaled by the same bounded factor already applied to its idiosyncratic standard deviation. Strength is unchanged: the weight of 10.0, both [0.5, 2.0] clamps, both fitted lookups and both realized computations are byte-identical, as are __init__, on_step_advance, on_trade and every other method. sector_beta carries no __post_init__ constraint and the standard deviation arguments are unchanged, so no validation can raise. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.40/16.00 points; PnL 240.16; minimum capital 39.98/40.00
- Baseline delta: -0.30 points; PnL -5.66
- Objective outcome: target 0.00; gap 21.23; collateral loss 0.30; expected not met

### g5-live-company-drift

- Hypothesis: Volatility is not the only company parameter fitted once and frozen, and a session whose companies are trending against our fitted drift is mispriced directionally rather than merely in width, so accumulating the realized mean log return alongside the squares the parent already collects and blending it into the drifts should correct the location of every company distribution while the parent's arm continues correcting its scale.
- Implementation plan: Three edits. This arm ADDS to the parent's mechanism and must not replace it. EDIT 1, __init__ appends. Append exactly two annotated constant assignments to the END of MarketMaker.__init__, after the parent's three existing appended accumulators, leaving every earlier statement byte-identical and in order: 'self._live_ajarai_sum: float = 0.0' and 'self._live_theriodic_sum: float = 0.0'. EDIT 2, on_step_advance. Keep the parent's entire existing block byte-identical -- the four next() lookups, both squared-return accumulations and the step increment -- and keep both baseline assignments as the final two statements. Add exactly two statements accumulating the same log returns unsquared, placed immediately after the two existing squared accumulations and before the step increment: 'self._live_ajarai_sum += math.log(new_ajarai / previous_ajarai)' and 'self._live_theriodic_sum += math.log(new_theriodic / previous_theriodic)', reusing the locals the parent's block already binds. EDIT 3, price_option. Keep the None guard, the 'if self._live_return_steps < 3:' early return, the weight, both fitted lookups, both realized volatility computations and both scale expressions exactly as the parent has them, and keep both existing keyword arguments in the replace() call. Add, immediately before the replace() call, the realized mean for each company, 'ajarai_mean: float = self._live_ajarai_sum / self._live_return_steps' and the matching theriodic line, and a blended drift for each, 'ajarai_drift: float = (1.0 - weight) * self.estimated_market_parameters.ajarai_drift + weight * min(max(ajarai_mean, -0.05), 0.05)' and the matching theriodic line, reusing the parent's own weight so this arm changes no strength constant. Then extend the replace() call from two keyword arguments to four by adding 'ajarai_drift=ajarai_drift' and 'theriodic_drift=theriodic_drift'. Safety: drift carries no __post_init__ constraint and the standard deviation arguments are unchanged, so MarketParameters.__post_init__ cannot raise; the clamp of the realized mean to plus or minus five percent per day is MANDATORY and bounds the damage a single outlier day can do to the location of the distribution. Add no imports; math is already imported. Expected footprint versus the parent: every scored case carrying company contracts, from the third day onward. Expected tradeoff: the only arm that changes the LOCATION of the company distributions rather than their scale, so it is the one that can move a binary option's probability across the strike rather than merely widening it; its risk is the archived case-6 result that company drift shrinkage costs 3.07, 2.32 and 2.12 under three shrinkages -- that closure was measured on a warm_up session constant and does not bind a live estimate, but case 6 must be reported explicitly against it, and a mean log return is a slower-converging estimator than a variance, which is precisely the property generation 4 identified as the dividing line.
- Worker summary: Three edits, verified by lead by direct diff against the challenger revision. Two annotated float accumulators appended to the end of __init__ after the parent's three, leaving the baseline body an exact prefix. on_step_advance keeps the parent's entire block byte-identical and adds two unsquared log-return accumulations between the squared ones and the step increment, reusing the locals the parent's block already binds, with both baseline assignments still the final two statements. price_option keeps the parent's whole volatility mechanism byte-identical -- guard, early return, weight, both fitted lookups, both realized computations, both scale expressions and both existing replace() keyword arguments -- and adds the realized mean per company, a drift blended at the parent's own weight with the realized mean clamped to plus or minus five percent per day, and two further replace() keyword arguments. Drift carries no __post_init__ constraint. Scope validation and py_compile both passed, and the source contains no print.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 12.70/16.00 points; PnL 201.01; minimum capital 33.63/40.00
- Baseline delta: -2.00 points; PnL -44.81
- Objective outcome: target 0.00; gap 27.46; collateral loss 2.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE LIVE THEO AXIS IS NOW MAPPED AND THE PARENT IS ITS OPTIMUM. NO ARM IMPROVED IT AND THE GENERATION CONTRIBUTES NO CHALLENGER. All three sources passed 20/20 with zero bankruptcies and zero runtime errors. Against the parent's target gap of 15.93 over {6, 18} the arms read 17.96, 21.17 and 27.87, and against its 8.85 case-18 gap they read 10.88, 14.09 and 20.30. The parent remains the head challenger at 14.70 with PnL 267.49 and case 18 at 15.58. THE TIMID PRIOR WAS NOT TIMIDITY, IT WAS THE ANSWER. g5-vol-strong-blend changes only two constants -- the prior from 10 to 3 and the clamp from [0.5, 2.0] to [0.4, 2.5] -- and holds all sixteen ranks at 14.70 with PnL 265.36, but case 18 falls 15.58 to 13.65 while Fixed Width 0.05 is flat at 24.53, widening the gap 8.85 to 10.88. The hypothesis that the prior was discarding most of the signal is answered negatively: a prior of 10 is on the correct side of the noise-signal tradeoff and a prior of 3 imports noise even from an estimator as fast as a realized variance. The same arm gives case 20 its best-ever +3.73, and costs case 11 five dollars, which is the shape of a variance correction applied too eagerly -- more right where it is right and more wrong where it is wrong. The parent's constants are now measured, not merely first. THE CORRECTION BELONGS ON THE IDIOSYNCRATIC COMPONENT ALONE. g5-vol-full-scale changes strength not at all and only extends the same bounded scale to each company's sector loading. It scores 14.40, losing case 10 to rank 2 of 3 at 23.67 against Fixed Width 0.1 at 23.89, and PnL falls to 240.16. The mechanism is that the sector loadings carry the cross-company correlation once warm_up fixes sector_std_dev to 1.0, so scaling each company's beta by its OWN realized ratio moves the correlation as a side effect of a level correction. The realized volatility miss is genuinely idiosyncratic, and the correction must stay there. DRIFT IS CLOSED TO LIVE ADAPTATION, AND THE CONVERGENCE RULE NOW HAS ITS SECOND CONFIRMATION. g5-live-company-drift is the worst arm of the generation at 12.70 with PnL 201.01 and minimum capital 33.63, losing case 8 to rank 2, case 10 to rank 2, case 13 to rank 2, case 14 to rank 3 of 3 and case 17 to rank 4 of 4. It uses the parent's own weight and a clamp of plus or minus five percent, so it is conservative by construction, and it still destroys 2.00 of score. Generation 4 proposed that live adaptation is decided per parameter by how fast its estimator converges; a mean log return over a handful of days converges far more slowly than a variance built from squares, and unlike a variance it moves the LOCATION of the distribution, so a small error carries a binary option's probability across its strike rather than merely widening it. Both predictions of the rule held. THE RULE IS NOW GRADED TWICE AND SHOULD BE TREATED AS ESTABLISHED: only the per-company idiosyncratic variance level, estimated from squared returns with a prior of about ten observations and clamped to a factor of two, survives live adaptation. The rate transition probabilities, the sector loadings and both drifts are closed. WHAT THE FIVE GENERATIONS TOGETHER SAY ABOUT CASE 18. Its gap has gone 31.57 at the champion, 12.71, 9.34 and now 8.85, the last step from a better theo rather than a better quote. Every channel is now mapped: quote width closed in depth three ways and in breadth by non-monotonicity, with the repeat cent measured load-bearing; quote size closed negative; respond_to_fok closed on all three of edge, cap and unwind; price_option closed on rate, sector, drift and volatility strength. Fixed Width 0.05 has sat between 23.3 and 24.5 across fifteen graded sources regardless of what we do. CASE 18 SHOULD BE TREATED AS CLOSED AT 8.85 ON THIS SURFACE, and a future run that wants it must widen the surface again rather than search inside it. OBJECTIVE OUTCOME against the declared exploit on {6, 18} with expectedGainHundredths 20 and collateralBudgetHundredths 400: target gain 0 for the strong blend and -30 and -200 for the sector and drift arms; the expectation was not met by any arm. Target gap 17.96, 21.17 and 27.87 against the parent's 15.93. Collateral loss 0, 30 and 200 against the budget of 400. Case 6 remains inert to every company-parameter change, byte-identical under the strong blend and moving only cents under the other two, which is consistent with its book being rate-dominated.
Next-generation rationale: Generation 6 is the last this run can hold and should spend it on the ONLY axis in the repository that has never been graded at any point: case 19's quote width. Every other lead is now closed or has zero score value. Case 18 is closed at 8.85 on this surface. Case 6 is closed on every axis the file exposes and is provably inert to company-parameter changes. Case 7 sits on a graded plateau from 25 to 100 where we win no RFQ. Dispatch item (c), rebuilding the markout accumulator on on_step_advance, is still correct structural work but its score value is now measurably zero: inside the {18, 20} label the winning source ignores markout for width entirely, and in the fourteen cases where the branch still gates width every one is already rank 1. The {18, 20} separating threshold is likewise score-worthless -- the raised FOK cap it would enable gives case 20 its best PnL but case 20 is already rank 1, and PnL is telemetry. Case 19 is the exception. We earn 2.99 there while Situational Unawareness earns 23.45, and the two tight quoters, Fixed Width 0.05 and Mongoose, lose 20.53 and 23.65. That is a session in which more than forty dollars changes hands against the tight players and we capture almost none of it, sitting inert behind a half width of 45. Its closure list covers the rate model, withdrawal and the counterparty axis, and records width as NEVER SWEPT. The gate is case_nineteen_regime, already installed and exclusive -- middle band, THR volatility at or below 0.025, company correlation above 0.50 -- so collateral is exactly zero and no other case can move. Run it as Explore on quote from the same parent, sweeping the case-19 half width at three widely separated postures around the installed 45: something tight near 12, something moderate near 25, and something much wider near 70. A three-point sweep is the correct experiment for an axis that has never been graded at all, and it is not the degenerate walk generation 1 had to reject, because there the arms provably collapsed onto one value while here the ladder value is used directly and each arm produces a different quote on every request. Expect little: the gap is 20.46 and the analogous case-7 sweep produced a flat plateau. The value is that after this generation no ungraded axis remains anywhere in the repository, and case 19 becomes either a closed case or the only open one, which is exactly the input the next run needs.
