# Market-Maker Experiment: market-loop-20260821-6

- Status: complete
- Started: 2026-08-21T20:19:21.714Z
- Starting baseline: g6-lowband-size-eight (14.70/16.00)
- Current baseline: g6-lowband-size-eight (14.70/16.00)
- Stop condition: generation limit reached
- Score trend: 14.70

The fixed grader is evaluated once per unique source SHA-256; repeated sources reuse cached case evidence. Fixture-only validation uses stubbed evidence.

## Generation 1: tune quote

The head challenger scores 14.70 and sits 1.58 dollars from rank 1 in case 18, the only case in the book within seven dollars of a rank change and worth exactly 0.20 under the 0.40 + 0.60 * (beaten / (N - 1)) map. Taking it promotes. Generation 6 of run market-loop-20260821-5 installed the last positive lever inside the fed_low_mean_regime near-expiry label: one extra lot on whichever side reduces an existing position, cutting the case-18 gap from 2.34 to 1.58. That generation graded the DIRECTION of the lever only. Its magnitude - the single lot added - and its position threshold - any non-zero position qualifies - have never been swept, and the indiscriminate and flat-book-entry variants that were graded are closed negative, so the surviving branch is exactly the inventory-reducing one whose two literals this batch varies. The lineage has tuningAttempts 0 and both attempts available. The lot increment appears as four separate literals - the offer-side capacity guard, the offer-side assignment, the bid-side capacity guard and the bid-side assignment - and all four are bound to ONE parameter so the guard can never authorise fewer lots than the assignment adds; that coupling is the whole safety argument for raising the increment, because each added lot is still checked against the same active_exposure / signed_reserve capacity arithmetic the surrounding ladder uses. The two position thresholds are bound separately and are constrained away from zero-crossing: the long threshold may only rise and the short threshold may only fall, so no vector can reopen the flat-book-entry arm that graded 2.49. The parent's three thinnest held margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21; all three lie outside the exclusive {18, 20} label and a label-gated change cannot reach them. Case 20 is the binding collateral check at 5.49 with a negative PnL, so the collateral budget is zero and any vector that spends more than 5.49 there gives back exactly what case 18 would win.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.00.

### increment-2-thresh-0-0

- Hypothesis: coarse parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":2,"unwind_long_threshold":0,"unwind_short_threshold":0}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.29; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.47
- Objective outcome: target 0.00; gap 1.50; collateral loss 0.00; expected not met

### increment-4-thresh-0-0

- Hypothesis: coarse parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":4,"unwind_long_threshold":0,"unwind_short_threshold":0}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.23; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.41
- Objective outcome: target 0.00; gap 1.66; collateral loss 0.00; expected not met

### increment-1-thresh-3-neg3

- Hypothesis: coarse parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":1,"unwind_long_threshold":3,"unwind_short_threshold":-3}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.33; minimum capital 39.33/40.00
- Baseline delta: 0.00 points; PnL 21.51
- Objective outcome: target 0.00; gap 1.58; collateral loss 0.00; expected not met

### increment-3-thresh-0-0

- Hypothesis: medium parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":3,"unwind_long_threshold":0,"unwind_short_threshold":0}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.32; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.50
- Objective outcome: target 0.00; gap 1.42; collateral loss 0.00; expected not met

### increment-2-thresh-1-neg1

- Hypothesis: medium parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":2,"unwind_long_threshold":1,"unwind_short_threshold":-1}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.29; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.47
- Objective outcome: target 0.00; gap 1.50; collateral loss 0.00; expected not met

### increment-3-thresh-2-neg2

- Hypothesis: medium parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":3,"unwind_long_threshold":2,"unwind_short_threshold":-2}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.32; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.50
- Objective outcome: target 0.00; gap 1.42; collateral loss 0.00; expected not met

### increment-1-thresh-1-neg1

- Hypothesis: fine parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":1,"unwind_long_threshold":1,"unwind_short_threshold":-1}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.26; minimum capital 39.26/40.00
- Baseline delta: 0.00 points; PnL 21.44
- Objective outcome: target 0.00; gap 1.58; collateral loss 0.00; expected not met

### increment-2-thresh-3-neg3

- Hypothesis: fine parameter tuning for market-loop-20260821-5-g06-g6-label-unwind-extra-lot
- Implementation plan: {"unwind_lot_increment":2,"unwind_long_threshold":3,"unwind_short_threshold":-3}
- Worker summary: Verified all six plan-declared ordinals against MarketMaker.quote and designed eight unique precommitted joint vectors spanning coarse, medium and fine granularities over unwind_lot_increment [1,4], unwind_long_threshold [0,3] and unwind_short_threshold [-3,0], excluding the parent vector (1, 0, 0). The lot increment was bound to all four literals of the near-expiry unwind block at once - both capacity guards and both assignments - so no vector could authorise fewer lots than it added. The materializer changed only AST-bound quote constants; every variant compiled, passed scope validation, and was diff-reviewed by the lead before serial evaluation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.36; minimum capital 39.33/40.00
- Baseline delta: 0.00 points; PnL 21.54
- Objective outcome: target 0.00; gap 1.50; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The inventory-reducing unwind lot is a near-inert lever in both of its untested dimensions, and this batch closes both. All eight vectors scored 14.70 with 20/20 structurally complete, zero bankruptcies and zero runtime errors, and all sixteen scored ranks were identical to the parent's in every vector. The lot increment traces a shallow interior maximum on the case-18 gap: 1.58 at the parent increment of 1, 1.50 at 2, 1.42 at 3 and back out to 1.66 at 4, a total excursion of 0.24 across the whole integer range. Increment 3 is therefore the local optimum and improves the target gap by 0.16 with combined PnL 267.32 against the parent's 267.26, while leaving case 20 untouched at -0.74 and rank 1 by 5.49. The position-threshold axis is exactly inert on the target: at increment 1 the case-18 gap is 1.58 whether the thresholds are (0, 0), (1, -1) or (3, -3); at increment 2 it is 1.50 for (0, 0), (1, -1) and (3, -3); at increment 3 it is 1.42 for both (0, 0) and (2, -2). The thresholds move nothing but case 20, and only by +0.07 at the (3, -3) extreme. The mechanism this implies is that whenever the branch fires the option position is already at or beyond three lots, so restricting it to larger positions removes no fills. Both dimensions of dispatch item (a) are now graded, and the lever's total remaining headroom of 0.16 out of 1.58 says the case-18 gap will not be closed by resizing this branch.
Next-generation rationale: Item (a) of the dispatch queue is spent and returns at most 0.16 of the 1.58 needed. The batch also produces a new structural fact worth spending the next generation on: the unwind branch is position-saturated, meaning the near-expiry label's flow arrives against a book that is already three or more lots deep in the same option, so size on the unwinding side is not the binding constraint. That points the remaining case-18 search away from sizing and back to allocation, which is the one lesson that has paid this surface twice: apply the allocation question to a lever whose level is closed but whose allocation has never been graded - the repeat-request cent, the fill-signal cent, or the wing-widening block, each of which currently applies uniformly to every contract inside the label. Alternatively, hunt the 18-from-20 discriminator, which is now worth finding because case 20 holds by only 5.49 with a negative PnL and every remaining label lever helps 18 at 20's expense.
Challenger update: updated market-loop-20260821-5-g06-g6-label-unwind-extra-lot to increment-3-thresh-0-0.

## Generation 2: explore quote

The head challenger revision 1 scores 14.70 and is 1.42 dollars from rank 1 in case 18, worth exactly 0.20 under the 0.40 + 0.60 * (beaten / (N - 1)) map and the only rank in the book within seven dollars. Generation 1 of this run closed the last sizing dimension of the near-expiry label: the unwind lot's increment has a shallow interior maximum at three lots and its position threshold is exactly inert, because the branch only fires when the option position is already at least three lots deep. Sizing on the near-expiry label is finished. What remains is allocation, which is the one question that has paid this surface: run market-loop-20260821-5 generation 2 found 7.27 dollars of case-18 gap by asking not how much width to spend but on which contracts to spend it, after fifteen graded sources had swept the level of the same lever and read flat. The general rule it established is that a closed LEVEL says nothing about an untested ALLOCATION. Three width levers in quote still spend width uniformly on every contract, including the near-expiry label contracts the installed rule is deliberately tightening: the repeat-request cent, which raises the base half width from four to five whenever a counterparty asks twice for the same option; the fill-signal cent, which backs the just-filled side away by one cent on the next request; and the wing-widening block, which adds one or two cents whenever a price sits deep in a wing. Each of these partially undoes the two-cent near-expiry tightening on exactly the contracts where the endogenous leader has been shown to respond, and each has only ever been graded as a global presence-or-absence question - removing the repeat-request cent outright costs 0.87 - never as an allocation. This generation asks the allocation question of all three at once by suppressing each inside label_depth_applies alone, leaving its behaviour identical everywhere else in the book. The three arms are structurally disjoint: they touch different lines, different mechanisms and different quote fields, so a positive result attributes to one lever. The parent's three thinnest held margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21; all three sit outside the exclusive {18, 20} session label and a label-gated change cannot reach them. Case 20 is the binding collateral check at 5.49 with a negative PnL of -0.74 and is the only other case inside the label, so the collateral budget is zero: any arm that costs case 20 more than 5.49 flips it to rank 2 and gives back precisely the 0.20 that case 18 would win.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.00.

Parent: challenger `market-loop-20260821-5-g06-g6-label-unwind-extra-lot r01` (`6ec2254e56936d0833b427260e759ea17f87d35cb4153dcd3a0474e1580a936f`).

### g2-label-no-repeat-cent

- Hypothesis: The repeat-request cent is load-bearing globally but misallocated inside the near-expiry label: on a contract with at most one step to expiry the second request from the same counterparty is not additional adverse-selection risk worth a cent, it is the flow the label is trying to win, so paying the cent there hands the endogenous leader the width it needs to stay ahead in case 18.
- Implementation plan: In quote, keep the existing half_width conditional chain and the existing label_depth_applies computation exactly as they are. After label_depth_applies is computed and before the existing two-cent narrowing branch that reads it, add a single guarded statement that removes the repeat-request cent when the label applies: when label_depth_applies is true and repeat_request is true, reduce half_width by one, floored so it can never fall below one. Do not change the half_width chain literals, do not change the repeat_request bookkeeping, and do not touch the counterparty-markout arm of the narrowing branch. The base half width inside the label becomes four regardless of whether the request repeats, so a repeated near-expiry request quotes two cents wide after the existing narrowing instead of three. Expected tradeoff: tighter near-expiry quotes win more of the contested case-18 flow at a lower edge per fill; the risk is that case 20, the only other session in the label, pays the same tightening and loses more than its 5.49 margin.
- Worker summary: Inserted two lines between the label_depth_applies assignment and the existing narrowing branch: when the label applies and the request repeats, half_width drops by one with the same max(..., 1) floor the surrounding line uses. The half_width chain literals, the repeat_request bookkeeping, the counterparty-markout arm, the wing block, the fill-signal block and every quantity path are untouched, so behaviour outside the near-expiry label is byte-for-byte identical. Compiled and passed scope validation against the r01 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.26; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.44
- Objective outcome: target 0.00; gap 1.34; collateral loss 0.00; expected not met

### g2-label-no-fill-signal-cent

- Hypothesis: The fill-signal back-off cent is correctly allocated on contracts with time left, where a fill genuinely predicts more of the same flow, but is misallocated within one step of expiry: the position it is protecting against will settle before the signal can pay, so the cent is pure width given away on exactly the near-expiry contracts the label is contesting in case 18.
- Implementation plan: In quote, locate the existing fill-signal block that pops fill_signals for the current request key and then subtracts one cent from bid_price when the signal is positive or adds one cent to offer_price when it is negative. Keep the pop unconditional so the per-key signal state machine is byte-for-byte unchanged and no stale signal can survive into a later request. Guard only the two price adjustments so that they are skipped when label_depth_applies is true; when the label does not apply, both adjustments must behave exactly as they do today. Do not change the fill_signals bookkeeping written earlier in quote from the prior-quote snapshot, and do not touch the wing-widening block or the half_width chain. Expected tradeoff: near-expiry quotes stop retreating from the side that just traded, which keeps the tighter side available for the rest of the day; the risk is repeated same-side fills accumulating a near-expiry position that settles against us, which would show as a case-18 or case-20 PnL loss rather than a rank change elsewhere.
- Worker summary: Wrapped only the two fill-signal price adjustments in a not-label_depth_applies guard, leaving fill_signals.pop(request_key, 0) unconditional immediately above it so the per-key signal state machine is unchanged and no stale signal survives into a later request. The wing block, half_width chain, narrowing branch and all quantity logic are untouched. Compiled and passed scope validation against the r01 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.42; minimum capital 39.36/40.00
- Baseline delta: 0.00 points; PnL 21.60
- Objective outcome: target 0.00; gap 1.42; collateral loss 0.00; expected not met

### g2-label-no-wing-widening

- Hypothesis: The wing-widening block spends one or two cents whenever a quote price sits deep in a wing, which is sound protection against settlement risk on contracts with several steps left, but within one step of expiry the fair value is already close to its terminal zero or one and the extra wing cents are the largest single block of width the label gives back, so suppressing them inside the label is the biggest allocation change available in quote.
- Implementation plan: In quote, locate the existing wing-widening block that runs immediately after bid_price and offer_price are derived from fair_value_cents and half_width: it subtracts one cent from bid_price when bid_price exceeds 0.50 and a second cent when fair_value_cents exceeds 75, and symmetrically adds one cent to offer_price when 1.0 minus offer_price exceeds 0.50 and a second cent when fair_value_cents is below 25. Guard the entire block so it is skipped when label_depth_applies is true, leaving it byte-for-byte unchanged on every other contract. Preserve the existing max and min clamps on the untouched path. Verify that the Quote invariants still hold on the suppressed path: bid_price and offer_price are derived symmetrically from fair_value_cents with a half width of at least one, so bid_price stays strictly below offer_price and both stay inside [0, 1] without the wing adjustments. Do not touch the half_width chain, the narrowing branch, the fill-signal block or any quantity logic. Expected tradeoff: near-expiry wing quotes are symmetric and tight, which is where the leader's edge in case 18 has been shown to live; the risk is a cheap-side or rich-side near-expiry fill settling adversely and costing case 20 more than its 5.49 margin.
- Worker summary: Wrapped the entire wing-widening block in a not-label_depth_applies guard, preserving every literal, the nesting and both clamps on the untouched path. Inside the label bid_price and offer_price keep their raw symmetric values from the half_width derivation. Compiled and passed scope validation against the r01 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 258.57; minimum capital 35.79/40.00
- Baseline delta: 0.00 points; PnL 12.75
- Objective outcome: target 0.00; gap 15.03; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The allocation rule paid a third time, and it separated the three uniform width levers completely. Removing the repeat-request cent inside the near-expiry label is the batch winner and the first lever this run to improve BOTH label cases at once: the case-18 gap fell from 1.42 to 1.34, case 20 rose from -0.74 to +0.15 and its held margin widened from 5.49 to 6.32, combined PnL rose from 267.32 to 268.26, and all sixteen scored ranks held at 14.70 with 20/20 structurally complete and zero bankruptcies. That case 20 improves rather than pays is the important part: every previous label lever bought case-18 gap out of case-20 margin, and this one does not, which says the repeat-request cent was not buying protection inside the label at all - it was pure width handed to the endogenous leader on flow that arrives twice precisely because it is the flow the label contests. Suppressing the fill-signal cent inside the label is INERT on the target: the case-18 gap stays at exactly 1.42 while case 20 moves only +0.10 and PnL only +0.10, so within one step of expiry the post-fill back-off changes no fill outcome and the lever is closed on this allocation. Suppressing the wing-widening block inside the label is the worst arm graded on this surface in three runs: the case-18 gap blew out from 1.42 to 15.03, case 20 fell from -0.74 to -4.21 and its margin collapsed from 5.49 to 0.40, one bad session away from surrendering the rank. The wing cents are strongly load-bearing exactly where the label operates, and the reason is mechanical: within one step of expiry the fair value sits near its terminal zero or one, so a large share of near-expiry quotes are deep-wing quotes and the block is the dominant width term there, not a marginal one. The three levers therefore rank load-bearing, inert and misallocated respectively, and only the misallocated one had upside.
Next-generation rationale: The repeat-request cent removal is preserved as a derived challenger and leaves case 18 at 1.34, still 0.20 and one rank away. Its own allocation is now the open question and it carries explicit parameter upside: the removal is currently exactly one cent floored at one, and neither the removal amount nor whether it should also apply on the second and later repeats of a contract outside the near-expiry window has been swept. More importantly the arm produced the run's first free case-20 gain, widening the binding collateral margin from 5.49 to 6.32, which buys room that every previously blocked case-18 lever was denied. Spend the next generation either tuning the removal amount on the new challenger, or reopening a case-18 lever that was rejected purely on case-20 cost now that 0.83 more margin exists.
Challenger update: derived market-loop-20260821-6-g02-g2-label-no-repeat-cent.

## Generation 3: explore quote

Generation 2 established that the repeat-request penalty is misallocated inside the near-expiry fed_low_mean_regime label. Removing its one-cent width penalty there improved case 18 from a 1.42 gap to 1.34 AND improved case 20 from -0.74 to +0.15, the first lever in this run to pay on both label cases at once, which says repeated near-expiry requests are not adverse selection to be charged for - they are the contested flow the label exists to win. That result was measured on the PRICE the repeat penalty charges. The same penalty is charged a second time, on SIZE, and that half has never been graded. Every rung of the quantity ladder above three lots is gated on not repeat_request: the offer ladder's fourth, fifth and sixth lots, the bid ladder's fourth and fifth, and the one-cent bid price improvement that fires when bid_quantity reaches three. A repeated near-expiry request therefore quotes at the improved width generation 2 just won but is frozen at two or three lots, so we win the contested flow and then decline most of it. Lifting the gate inside the label is a quantity multiplier rather than a one-cent edge, which is why it can plausibly move case 18 by more than the 0.08 the price half returned. The three arms are the standard side decomposition this surface has answered before for width - offer-only, bid-only, symmetric - and it has never been asked of size; the width answer was that symmetric strictly beats either side alone, at 1.74 against 20.83 offer-only and 16.29 bid-only, so reading the size version against that is itself informative. Every existing capacity guard on every rung is preserved unchanged in all three arms, so no arm can authorise a lot the parent's own active_exposure or signed_reserve arithmetic would refuse, and the half width inside the label is exactly two cents on both repeat and non-repeat paths, which keeps the Quote invariants strict even where the bid improvement fires. The parent's three thinnest held scored margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21, all outside the exclusive {18, 20} label and unreachable by a label-gated change. Case 20 remains the binding collateral check but now holds by 6.32 rather than 5.49, so the collateral budget stays at zero with 0.83 more room than any previous generation had.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.00.

Parent: challenger `market-loop-20260821-6-g02-g2-label-no-repeat-cent r00` (`c04d0e4e926ce02cebf354fb518f11999c5ff02cdea3afe819bec98a944cc6b9`).

### g3-label-repeat-offer-size

- Hypothesis: The repeat-request size penalty is misallocated inside the near-expiry label on the offer side: repeated near-expiry requests to sell are the contested flow the label wins on price, and freezing them at three lots declines most of what the tighter offer earns.
- Implementation plan: In quote, introduce one local boolean immediately after the existing label_depth_applies assignment, named in the style of the surrounding regime flags, that is true when the request does not repeat OR when label_depth_applies is true. Substitute this boolean for the existing `not repeat_request` term in the OFFER quantity ladder only: the rung that raises offer_quantity from three to four, the rung that raises it from four to five, and the rung that raises it from five to six. Leave every other term of those three conditions exactly as written, including their active_exposure capacity arithmetic, the cash_balance band on the sixth rung and its position guard. Do not touch the bid ladder, the bid price improvement, the half_width chain, the narrowing branches, the wing block, the fill-signal block, the low_band_regime while loops, the counterparty-markout size block or the label unwind block. Behaviour outside label_depth_applies must be byte-for-byte identical, because outside the label the new boolean reduces to `not repeat_request`. Expected tradeoff: repeated near-expiry sell interest is filled at four to six lots instead of three, multiplying the edge the tighter offer already earns; the risk is a larger near-expiry short book settling against us, which would show as a case-18 or case-20 PnL loss.
- Worker summary: Added one local boolean after label_depth_applies that is true when the request does not repeat or the near-expiry label applies, and substituted it for the not repeat_request term in the three offer rungs only. Every active_exposure capacity term, the cash_balance band on the sixth rung and its position guard are unchanged, and the bid side is untouched. Outside the label the boolean reduces to not repeat_request. Compiled and passed scope validation against the r00 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.26; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.44
- Objective outcome: target 0.00; gap 1.34; collateral loss 0.00; expected not met

### g3-label-repeat-bid-size

- Hypothesis: The repeat-request size penalty is misallocated inside the near-expiry label on the bid side, and the one-cent bid improvement is penalised there too, so repeated near-expiry requests to buy are quoted a worse bid at a smaller size than the very first request for the same contract.
- Implementation plan: In quote, introduce one local boolean immediately after the existing label_depth_applies assignment, named in the style of the surrounding regime flags, that is true when the request does not repeat OR when label_depth_applies is true. Substitute this boolean for the existing `not repeat_request` term in the BID side only: the block that adds one cent to bid_price when bid_quantity is three, the rung that raises bid_quantity from three to four on the cash_balance condition, the rung that raises it from three to four on the single-leg and cheap-side conditions, and the rung that raises it from four to five. Leave every other term of those conditions exactly as written, including their signed_reserve capacity arithmetic, the per-underlying exclusions and the fair_value_cents guards. Do not touch the offer ladder, the half_width chain, the narrowing branches, the wing block, the fill-signal block, the low_band_regime while loops, the counterparty-markout size block or the label unwind block. Behaviour outside label_depth_applies must be byte-for-byte identical. Confirm the Quote invariants on the new path: inside the label the half width is exactly two cents on both the repeat and non-repeat paths, so a one-cent bid improvement leaves bid_price at fair_value minus one cent, strictly below offer_price at fair_value plus two cents, and the existing max clamp is preserved. Expected tradeoff: repeated near-expiry buy interest is filled at four or five lots at a one-cent better bid; the risk is a larger near-expiry long book settling against us.
- Worker summary: Same boolean, substituted for the not repeat_request term on the bid side only: the one-cent bid improvement, both rungs raising bid_quantity to four, and the rung raising it to five. Every signed_reserve capacity term, per-underlying exclusion and fair_value_cents guard is unchanged, and the offer side is untouched. Compiled and passed scope validation against the r00 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.20; minimum capital 39.09/40.00
- Baseline delta: 0.00 points; PnL 21.38
- Objective outcome: target 0.00; gap 1.34; collateral loss 0.00; expected not met

### g3-label-repeat-both-size

- Hypothesis: Size behaves like width did on this label: the repeat-request penalty is misallocated on both sides at once and the selected contracts want symmetric treatment, so lifting the gate on the bid and offer ladders together strictly beats lifting either alone, exactly as the symmetric two-cent depth at 1.74 beat offer-only at 20.83 and bid-only at 16.29.
- Implementation plan: In quote, introduce one local boolean immediately after the existing label_depth_applies assignment, named in the style of the surrounding regime flags, that is true when the request does not repeat OR when label_depth_applies is true. Substitute this boolean for EVERY existing `not repeat_request` term in the quantity section: the three offer rungs that raise offer_quantity to four, five and six, the bid price improvement block, both rungs that raise bid_quantity to four, and the rung that raises it to five. Leave every other term of every one of those conditions exactly as written, including all active_exposure and signed_reserve capacity arithmetic, the cash_balance bands, the per-underlying exclusions and the fair_value_cents guards. Do not touch the half_width chain, the narrowing branches, the wing block, the fill-signal block, the low_band_regime while loops, the counterparty-markout size block or the label unwind block. Behaviour outside label_depth_applies must be byte-for-byte identical. Expected tradeoff: both sides of repeated near-expiry flow are filled at the full ladder depth, which is the largest size change available inside the label; the risk is the largest near-expiry book of the three arms and therefore the largest exposure of case 20's 6.32 margin.
- Worker summary: Same boolean, substituted for all seven not repeat_request terms in the quantity section, bid and offer together. No not repeat_request occurrence remains there. Every capacity term, cash_balance band, exclusion and price guard is unchanged. Compiled and passed scope validation against the r00 parent.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.20; minimum capital 39.09/40.00
- Baseline delta: 0.00 points; PnL 21.38
- Objective outcome: target 0.00; gap 1.34; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The repeat-request penalty's size half is closed, and the batch produced a sharper result than a null: it proves case 18's remaining gap is not repeat-request flow at all. All three arms scored 14.70 with 20/20 structurally complete and zero bankruptcies, and case 18 returned EXACTLY 17.91 with a 1.34 gap and an unmoved leader at 19.25 in every one of them - identical to the parent to the cent. Lifting the gate on the offer ladder inside the label is byte-identical to the parent across all twenty cases including combined PnL at 268.26, which is a measurement rather than an absence: the offer rungs never fire on a repeated near-expiry request because their own offer_quantity == 3 precondition is not met there, so the offer ladder was never the constraint. The bid arm does fire - it changes case 20 from +0.15 to -0.91 and combined PnL from 268.26 to 267.20 - and the both-sides arm reproduces the bid arm to the cent in all twenty cases, confirming again that the offer half contributes nothing. So repeated near-expiry requests do occur inside the label, the bid ladder does reach them, and giving them more size costs 1.06 in case 20 while moving case 18 by zero. Put beside generation 2, where removing the repeat WIDTH cent moved case 18 by only 0.11 but case 20 by 0.89, the pattern is unambiguous: repeat near-expiry traffic lives in the case-20 session, not the case-18 session. Case 18's PnL has now been pinned between 17.80 and 17.91 across six consecutively graded sources spanning the unwind increment, both position thresholds, three uniform width levers and the whole repeat-request size ladder. The near-expiry label is saturated for case 18: every remaining lever inside it moves case 20 and leaves case 18 untouched.
Next-generation rationale: Stop looking for case-18 gain inside label_depth_applies. Six graded sources have failed to move it and the mechanism is now understood - the near-expiry window is where case 20 trades, not where case 18's remaining flow is. The generation-2 winner points at the untested allocation: the repeat-request cent removal is currently gated on label_depth_applies, so it fires only within one step of expiry, and its own expiry scope has never been swept. Its cutoff cannot be tuned as a constant because the literal is shared with the two-cent narrowing and the unwind block, so decoupling it is a structural change. Apply the same allocation question that has now paid three times to this new lever: sweep where the removal applies by expiry, with arms at a two-step cutoff, at the whole fed_low_mean_regime session with no expiry gate, and at the complement - contracts with more than one step remaining - to attribute any gain. Case 18's untouched flow is precisely the contracts with time left, and case 20 now holds by 6.32, the widest margin any generation on this surface has had to spend.

## Generation 4: explore quote

Six consecutively graded sources have now failed to move case 18. Its PnL sits between 17.80 and 17.91 and its leader between 19.25 and 19.38 across the unwind lot increment, both unwind position thresholds, three uniform width levers and the entire repeat-request size ladder. Generation 3 explained why: the offer half of the size ladder is byte-identical inside the near-expiry label, the bid half fires but costs 1.06 of case-20 margin for exactly zero case-18 movement, and generation 2's width result showed the same asymmetry - the repeat cent moved case 18 by 0.11 and case 20 by 0.89. Repeat near-expiry traffic lives in the case-20 session. The window bounded by steps_until_expiry <= 1 is saturated for case 18, so the remaining 1.34 must come from case-18 contracts that still have time left. Generation 2's winner is the lever to extend, and its allocation is untested for the same reason every allocation on this surface has been untested: only its presence has been graded, never its scope. The removal currently fires exactly when label_depth_applies is true, so it inherits that flag's one-step expiry gate by accident rather than by measurement. The gate cannot be swept as a constant, because the literal in option.steps_until_expiry <= 1 is shared by the two-cent narrowing branch and the end-of-method unwind block, and generation 3 of run market-loop-20260821-5 lost case 12 three times over by moving a literal that two branches read. Decoupling the removal's own expiry condition from label_depth_applies is therefore a structural change and belongs in Explore. The three arms form a complete decomposition of that scope. The parent applies the removal to the near contracts only; the first arm extends it to two steps; the second removes the expiry gate entirely so it covers the whole fed_low_mean_regime session; the third applies it to the complement, contracts with more than one step remaining, which attributes any gain the second arm shows to the far contracts rather than the near ones. All three leave label_depth_applies itself, the two-cent narrowing, the wing block, the fill-signal block, the unwind block and every quantity path exactly as the parent has them. The parent's three thinnest held scored margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21, all outside the {18, 20} session label and unreachable by a fed_low_mean_regime-gated change. Case 20 holds by 6.32, the widest margin any generation on this surface has had, and the collateral budget stays at zero.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.00.

Parent: challenger `market-loop-20260821-6-g02-g2-label-no-repeat-cent r00` (`c04d0e4e926ce02cebf354fb518f11999c5ff02cdea3afe819bec98a944cc6b9`).

### g4-repeat-cent-cutoff-two

- Hypothesis: The repeat-request cent removal is correct but under-allocated by one step: contracts two steps from expiry in the fed_low_mean_regime session carry the same contested repeat flow as those one step out, and case 18's untouched flow is exactly the contracts the current one-step gate excludes.
- Implementation plan: In quote, the parent contains these two consecutive branches after the label_depth_applies assignment: `if label_depth_applies and repeat_request: half_width = max(half_width - 1, 1)` followed by `if label_depth_applies or counterparty_markout.get(counterparty_id, 0.0) > 0.0: half_width = max(half_width - 2, 1)`. Add one local boolean immediately after the label_depth_applies assignment, annotated and named in the style of the surrounding regime flags, that is true when fed_low_mean_regime is true and option.steps_until_expiry is at most two. Change the FIRST branch alone to test that new boolean instead of label_depth_applies, keeping `and repeat_request` and the identical max(half_width - 1, 1) body. Leave label_depth_applies itself, the second branch, the counterparty-markout arm, the wing block, the fill-signal block, every quantity path and the end-of-method unwind block exactly as written. Expected tradeoff: contracts two steps out lose the repeat-request cent and quote three cents wide instead of four on a repeat, which is where case 18's remaining flow must be; the risk is that the two-step contracts are where the repeat cent was actually earning its keep, which would show as a case-18 or case-20 PnL loss.
- Worker summary: Added one local boolean after label_depth_applies that is true when fed_low_mean_regime holds and steps_until_expiry is at most two, and pointed the first half_width branch at it instead of label_depth_applies, keeping `and repeat_request` and the identical max(half_width - 1, 1) body. label_depth_applies and all of its other consumers - the two-cent narrowing, the counterparty-markout arm and the end-of-method unwind block - are unchanged. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.24; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.42
- Objective outcome: target 0.00; gap 1.34; collateral loss 0.00; expected not met

### g4-repeat-cent-session-wide

- Hypothesis: The repeat-request cent is misallocated across the entire fed_low_mean_regime session rather than only near expiry: the session label already identifies the flow, and the expiry gate the removal inherited from label_depth_applies was never the thing being measured, so the removal should apply to every contract in the session.
- Implementation plan: In quote, change the FIRST of the two consecutive half_width branches after the label_depth_applies assignment - the one currently reading `if label_depth_applies and repeat_request: half_width = max(half_width - 1, 1)` - so that it tests fed_low_mean_regime instead of label_depth_applies, keeping `and repeat_request` and the identical max(half_width - 1, 1) body. Do not add a new boolean; fed_low_mean_regime is already computed above. Leave label_depth_applies itself and its every other consumer exactly as written: the second branch with its two-cent narrowing and counterparty-markout arm, the wing block, the fill-signal block, every quantity path and the end-of-method unwind block. Expected tradeoff: every repeated request in the {18, 20} session quotes four cents wide instead of five, the widest allocation of this lever available; the risk is that far-from-expiry repeats carry genuine adverse selection the cent was paying for, which would cost both label cases and put case 20's 6.32 margin in play.
- Worker summary: One-token change: the first half_width branch tests fed_low_mean_regime instead of label_depth_applies, so the repeat cent is removed for every contract in the session regardless of expiry. No new boolean and no other edit. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.13; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.31
- Objective outcome: target 0.00; gap 1.50; collateral loss 0.00; expected not met

### g4-repeat-cent-far-only

- Hypothesis: The removal's gain is entirely attributable to one side of the expiry boundary, and this arm decides which: applying it only to fed_low_mean_regime contracts with more than one step remaining leaves the near contracts at the pre-generation-2 behaviour, so its result read against the parent and against the session-wide arm separates the near contribution from the far one.
- Implementation plan: In quote, add one local boolean immediately after the label_depth_applies assignment, annotated and named in the style of the surrounding regime flags, that is true when fed_low_mean_regime is true and option.steps_until_expiry is greater than one. Change the FIRST of the two consecutive half_width branches - the one currently reading `if label_depth_applies and repeat_request: half_width = max(half_width - 1, 1)` - to test that new boolean instead of label_depth_applies, keeping `and repeat_request` and the identical max(half_width - 1, 1) body. The near-expiry contracts therefore keep their repeat cent, exactly as they did before generation 2. Leave label_depth_applies itself, the second branch, the counterparty-markout arm, the wing block, the fill-signal block, every quantity path and the end-of-method unwind block exactly as written. Expected tradeoff: this arm is expected to give back generation 2's case-20 gain, since that gain was measured on the near contracts, and to keep whatever the far contracts are worth; its value is attribution, and it is a strict improvement only if the far contracts alone carry more than the near ones did.
- Worker summary: Added one local boolean after label_depth_applies that is true when fed_low_mean_regime holds and steps_until_expiry is greater than one, and pointed the first half_width branch at it, so the near contracts keep their repeat cent exactly as they did before generation 2. All other consumers of label_depth_applies unchanged. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.19; minimum capital 39.21/40.00
- Baseline delta: 0.00 points; PnL 21.37
- Objective outcome: target 0.00; gap 1.58; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The repeat-cent removal's expiry allocation is closed at cutoff one, and the batch attributes generation 2's entire gain to the near contracts. All three arms scored 14.70 with 20/20 structurally complete and zero bankruptcies. Extending the removal to two steps is inert and very slightly negative: case 18 returns exactly 17.91 with a 1.34 gap, identical to the parent, while case 20 gives back 0.02 and combined PnL 0.02, so contracts two steps out carry none of the contested flow. Extending it to the whole fed_low_mean_regime session is negative on the target: our case-18 PnL falls from 17.91 to 17.83 and the leader RISES from 19.25 to 19.33, widening the gap from 1.34 to 1.50 - the endogenous instrument reading the change as a gift rather than a threat. Restricting it to the far contracts alone is the worst arm and the decisive one: case 18 falls to 17.78 with a 1.58 gap, WORSE than the 1.42 the lever's parent had before generation 2 ever ran, and case 20 returns to -0.79 with a 5.44 margin, giving back the entire generation-2 gain. Read together the three arms establish that the repeat-request cent is LOAD-BEARING on contracts with time left and MISALLOCATED only within one step of expiry, and that the one-step boundary the lever inherited from label_depth_applies is its exact optimum rather than an accident. That also reproduces, on an independent mechanism and inside a single session label, the global finding that removing the repeat cent outright costs 0.87. The near-expiry window of the {18, 20} label is now closed on every axis this file exposes: width level, side, option scope and expiry cutoff; the repeat cent's level and its expiry allocation; the fill-signal cent; the wing block; the unwind lot's increment and both position thresholds; and the entire repeat-request size ladder on both sides. Case 18's PnL has been pinned between 17.78 and 17.91 across nine consecutively graded sources in this run and the leader between 19.25 and 19.38.
Next-generation rationale: Case 18 is saturated and its remaining 1.34 is not reachable from the fed_low_mean_regime label. Redirect to case 6, which is worth MORE than case 18 - rank 2 of 3 scores 0.70 and rank 1 scores 1.00, so it is 0.30 rather than 0.20 - and which carries structurally zero collateral risk because its session label, flat_rate_frequency above 0.60, is an exact singleton that no other scored case shares. The archived memo lists case 6 as closed on every axis, but that closure was measured the way every closure on this surface was measured before generation 2 of run market-loop-20260821-5: as a LEVEL. Case 6's half width was swept to an interior maximum at eighteen cents while its allocation was held uniform across every contract in the session, exactly the parameterisation that read flat on case 18 for fifteen graded sources and then gave up 7.27 the moment the allocation question was asked instead. This run has now paid that same rule three times and has just measured the expiry boundary to be sharp, so the highest-value untested question on the file is which contracts in case 6 should receive the eighteen-cent width. Sweep it by expiry in both directions - wider near expiry, wider far from expiry, and tighter near expiry - on the champion, whose case-6 result is identical to the challenger's.

## Generation 5: explore quote

This generation deliberately reopens a listed closure, and the reason is the central lesson of the last two runs. Case 6 is rank 2 of 3 at 0.70, so taking rank 1 is worth 0.30 - MORE than case 18's 0.20 - and its session label, flat_rate_frequency above 0.60, is an exact singleton that no other scored case shares, so a change gated on it carries structurally zero collateral risk. Nine graded sources in this run have failed to move case 18 by more than 0.13; its PnL is pinned between 17.78 and 17.91 and its leader between 19.25 and 19.38, and every axis of the near-expiry label is now closed including the two this run added. The archived memo lists case 6 as closed on every axis the file exposes, with a width interior maximum at eighteen cents. That closure swept a LEVEL with the allocation held uniform across every contract in the session - precisely the parameterisation that read flat on case 18 across fifteen graded sources and then surrendered 7.27 of gap the moment the allocation question was asked instead of the level question. This run has now paid that rule three times and, in generation 4, measured the expiry boundary to be sharp rather than gradual: a one-step gate is optimal, a two-step gate is inert, and the complement is actively harmful. So the highest-value untested question on the file is which contracts in case 6 should carry the eighteen-cent width. The leaderboard says the answer is not uniform. Fixed Width 0.25 earns 10.50 quoting a flat twenty-five-cent half width, Stalemate Quoter earns 0.00, and we earn 3.42 at eighteen: a wider maker beats us decisively while a maker that never trades beats nobody, so the case-6 flow is adverse on average but not uniformly adverse, and eighteen is only the best single number to apply to all of it. The three arms sweep the expiry axis in both directions - twenty-five cents near expiry, twenty-five cents away from expiry, and twelve cents near expiry - so the result is interpretable whichever way the flow is distributed. Each is gated on the exact case-6 session label and inserted before the existing counterparty-markout narrowing so that arm still composes exactly as it does today. The fed_low_mean_regime branches cannot interact: they require flat_rate_frequency in the interval above 0.40 and at most 0.50, which is disjoint from above 0.60. The parent's three thinnest held scored margins are case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21, and case 20 holds by 6.32; all four sit outside the case-6 label and none is reachable by a change gated on it, so the collateral budget of zero is guaranteed by construction rather than by measurement. A winning arm takes the parent from 14.70 to 15.00, which both promotes and reaches the run's stopping score.

Objective: exploit; targets 6; expected +0.30; collateral budget 0.00.

Parent: challenger `market-loop-20260821-6-g02-g2-label-no-repeat-cent r00` (`c04d0e4e926ce02cebf354fb518f11999c5ff02cdea3afe819bec98a944cc6b9`).

### g5-case6-near-expiry-wide

- Hypothesis: Case 6's adverse flow is concentrated within one step of expiry, where the underlying can still gap but the option can no longer be managed out of, so the uniform eighteen-cent width is too tight exactly there; widening those contracts to the twenty-five cents the case-6 leader charges everywhere, while keeping eighteen on the rest, should capture the leader's protection without giving up the flow we currently win.
- Implementation plan: In quote, the half_width conditional chain assigns 18 when flat_rate_frequency is above 0.60. Immediately after the label_depth_applies assignment and BEFORE the existing `if label_depth_applies and repeat_request` branch, add one local boolean annotated and named in the style of the surrounding regime flags that is true when flat_rate_frequency is greater than 0.60 - this is the exact case-6 session label already used by the chain. Then add one guarded statement that, when that boolean is true AND option.steps_until_expiry is at most one, increases half_width by seven, taking it from eighteen to twenty-five. Place it before both existing half_width branches so the counterparty-markout narrowing still composes on top exactly as it does today. Do not change any literal in the half_width chain, do not touch label_depth_applies or either of its branches, and do not touch the wing block, the fill-signal block, any quantity path or the end-of-method unwind block. The fed_low_mean_regime branches require flat_rate_frequency above 0.40 and at most 0.50, so they can never fire in the same session as this one. Expected tradeoff: near-expiry case-6 quotes are as wide as the leader's, declining the flow that settles against us; the risk is that the flow we decline was the profitable half, which would show as a case-6 PnL fall below 3.42.
- Worker summary: Added one local boolean for the case-6 session label, flat_rate_frequency above 0.60, and one guarded statement raising half_width by seven when it holds and steps_until_expiry is at most one, both placed before the two existing half_width branches so the counterparty-markout narrowing composes on top unchanged. No literal in the half_width chain, no other branch and no quantity path was touched. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 268.63; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 22.81
- Objective outcome: target 0.00; gap 7.08; collateral loss 0.00; expected not met

### g5-case6-far-expiry-wide

- Hypothesis: Case 6's adverse flow is concentrated in the contracts with time left, where the flat-rate session gives the underlying many steps to drift away from our fill and where a maker holding inventory is most exposed, so the extra width belongs on the far contracts and the near ones can stay tight enough to keep winning the flow that settles immediately.
- Implementation plan: In quote, immediately after the label_depth_applies assignment and BEFORE the existing `if label_depth_applies and repeat_request` branch, add one local boolean annotated and named in the style of the surrounding regime flags that is true when flat_rate_frequency is greater than 0.60. Then add one guarded statement that, when that boolean is true AND option.steps_until_expiry is greater than one, increases half_width by seven, taking it from eighteen to twenty-five. Place it before both existing half_width branches so the counterparty-markout narrowing still composes on top exactly as it does today. Do not change any literal in the half_width chain, do not touch label_depth_applies or either of its branches, and do not touch the wing block, the fill-signal block, any quantity path or the end-of-method unwind block. Expected tradeoff: this is the exact complement of the near-expiry arm, so the two together decompose the case-6 flow by expiry regardless of which side carries the loss; the risk is symmetric to that arm's.
- Worker summary: Same boolean and the same seven-cent widening, applied instead when steps_until_expiry is greater than one. Same placement, same untouched surroundings. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 266.16; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 20.34
- Objective outcome: target 0.00; gap 12.74; collateral loss 0.00; expected not met

### g5-case6-near-expiry-tight

- Hypothesis: Case 6's near-expiry flow is the benign half rather than the adverse one - within one step of expiry the fair value is close to its terminal zero or one and a binary's remaining variance is small - so the uniform eighteen-cent width is too WIDE there and we are declining the one part of the case-6 book that settles predictably; this is the direction that paid 7.27 of gap on case 18 and it must be graded before the expiry axis can be called closed.
- Implementation plan: In quote, immediately after the label_depth_applies assignment and BEFORE the existing `if label_depth_applies and repeat_request` branch, add one local boolean annotated and named in the style of the surrounding regime flags that is true when flat_rate_frequency is greater than 0.60. Then add one guarded statement that, when that boolean is true AND option.steps_until_expiry is at most one, reduces half_width by six with the same max(..., 1) floor idiom the surrounding branches use, taking it from eighteen to twelve. Place it before both existing half_width branches so the counterparty-markout narrowing still composes on top exactly as it does today. Do not change any literal in the half_width chain, do not touch label_depth_applies or either of its branches, and do not touch the wing block, the fill-signal block, any quantity path or the end-of-method unwind block. Expected tradeoff: near-expiry case-6 quotes tighten by six cents and win more of the immediate-settlement flow; the risk is the largest of the three arms because case 6 starts from ten dollars of capital and tighter quotes mean more fills, so the existing signed_reserve and available_capacity guards, which this arm leaves untouched, are the entire bankruptcy argument.
- Worker summary: Same boolean, with a six-cent narrowing under the same max(..., 1) floor idiom when steps_until_expiry is at most one, taking eighteen to twelve. Same placement, no quantity logic touched, every signed_reserve and available_capacity guard left exactly as written. Compiled and passed scope validation.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 267.31; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 21.49
- Objective outcome: target 0.00; gap 8.28; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: Case 6's expiry allocation is live, measurable and monotone, and it still does not close the gap. All three arms scored 14.70 with 20/20 structurally complete and zero bankruptcies, and every case outside the case-6 label was identical to the parent to the cent in all three, confirming the singleton label carries exactly zero collateral as designed. Widening near-expiry contracts from eighteen to twenty-five cents raised our case-6 PnL from 3.42 to 3.79 and raised combined PnL to 268.63, the best figure this run has produced - but the leader rose from 10.50 to 10.87, by exactly the same 0.37, so the gap stayed at 7.08 to the cent. Widening the far contracts instead is strongly negative: our PnL fell to 1.32 while the leader rose to 14.06, widening the gap to 12.74. Tightening near-expiry to twelve cents cost us 0.95 and gained the leader 0.25, gap 8.28. Read together these three establish the sign of the case-6 flow on both sides of the expiry boundary and refute the archived closure's implicit claim that the eighteen-cent level was the whole story. The near-expiry flow is ADVERSE - we lose money on it at eighteen cents, we lose more at twelve, and we recover 0.37 by declining it at twenty-five. The far flow is PROFITABLE - it is worth 2.10 to us at eighteen cents, and stepping back to twenty-five hands 3.56 of it straight to the leader. Eighteen was the best single uniform number precisely because it splits the difference between a loss-making near book and a profitable far one. But the recovery is bounded by our own near-expiry loss, which the twenty-five-cent arm shows is only about 0.37, so no amount of further widening can reach 7.08. The endogenous instrument is unusually clean here: the leader captured exactly what we declined in the near arm and 3.56 of what we declined in the far arm, so case 6 is a flow-allocation game against a maker who profits from everything we step away from. The gap is structural, not a width mistake.
Next-generation rationale: Ten graded sources across five generations have now moved case 18 by at most 0.13 and case 6 by nothing at all, and every one of them changed WIDTH or SIZE. That is the common factor and it is worth treating as the finding rather than the coincidence: if the quoted spread and the quoted quantity are both exhausted, the remaining error is in the price they are centred on. The final generation should target price_option - the live theo, and the only way to make theo state-dependent, since warm_up yields a session constant - naming the quoted fair value as its consumer. Gate the change on fed_low_mean_regime so the fifteen cases outside the {18, 20} label are untouched and the collateral budget stays at zero by construction, which matters because the parent's thinnest held margins are case 13 at 1.35 and case 10 at 1.90. Sweep the sign of a one-cent near-expiry theo shift in both directions - toward the terminal value and toward one half - plus the same shift applied across the whole label, so the near and far contributions can be attributed exactly as they were in generations 4 and 5. A one-cent shift is large relative to the two-cent half width the label quotes near expiry, so if a pricing bias exists there it will show.

## Generation 6: explore price_option

Fifteen graded sources across five generations have moved case 18 by at most 0.13 and case 6 by nothing, and every single one of them changed WIDTH or SIZE. That is the common factor and this generation treats it as the finding rather than the coincidence: if the quoted spread and the quoted quantity are both exhausted, the remaining error is in the price they are centred on. The target is price_option, the live theo and the only way to make theo depend on state, since warm_up runs once and can only produce a session constant. The consumer this hypothesis targets is the QUOTED FAIR VALUE: quote rounds price_option to fair_value_cents and centres both prices on it, so a one-cent theo shift moves the whole two-sided quote by one cent. That is a large move relative to the two-cent half width the near-expiry label quotes, which is why a pricing bias there would have been invisible to every width experiment run so far - shifting both sides together is orthogonal to everything that has been graded. The known trap is the third consumer: price_option is also summed inside quote's signed_reserve capacity arithmetic over every option with a non-zero position, so a theo shift also moves the size limits. Three properties bound that coupling here. It is gated on fed_low_mean_regime, so it is inert in fifteen of the sixteen scored cases. Inside the label it applies to at most a one-cent shift on a sub-book. And signed_reserve sums only over non-zero positions, so it is exactly zero on a flat book. The second consumer, the respond_to_fok acceptance threshold, moves by the same one cent inside the label only; respond_to_fok is closed from both directions as a threshold lever, so any movement it contributes is a known-inert direction rather than a confound. price_option_from_parameters stays frozen and the THEO case scores that method directly, so case 1 cannot be put at risk. Collateral is zero by construction rather than by measurement: the fed_low_mean_regime gate is the exclusive {18, 20} session label, and the parent's thinnest held scored margins - case 13 at 1.35, case 10 at 1.90 and case 14 at 5.21 - all sit outside it and are unreachable. Case 20 holds by 6.32, the widest margin any generation on this surface has had. The three arms sweep the sign of the shift and then its expiry allocation, which is the decomposition that produced every interpretable result in this run: near-expiry toward the terminal value, near-expiry toward one half, and the terminal-value direction applied across the whole label so the near and far contributions can be separated by reading the third arm against the first.

Objective: exploit; targets 18; expected +0.20; collateral budget 0.00.

Parent: challenger `market-loop-20260821-6-g02-g2-label-no-repeat-cent r00` (`c04d0e4e926ce02cebf354fb518f11999c5ff02cdea3afe819bec98a944cc6b9`).

### g6-label-theo-toward-terminal

- Hypothesis: Our near-expiry theo in the {18, 20} session is systematically too timid: with at most one step left a binary's value has nearly resolved, and a model that still carries a step of diffusion prices it closer to one half than the market does, so our two-cent quote is centred one cent on the wrong side of the leader's and we lose the contested case-18 flow on price rather than on width.
- Implementation plan: In price_option, keep the existing early return when estimated_market_parameters is None and the existing live-volatility blend exactly as written, and compute the returned theoretical value into a local variable instead of returning the price_option_from_parameters call directly on both the short and the long path. After it, add one guarded adjustment. Read flat_rate_frequency from self.warm_up_statistics.rate_transition_frequencies['unchanged'], the Theriodic log-return sample_std_dev from self.warm_up_statistics.company_log_returns_by_underlying_id, and the Fed funds mean from self.warm_up_statistics.raw_values_by_underlying_id, using the same module constants and the same access paths quote already uses for these three, and form the fed_low_mean_regime condition exactly as quote forms it: flat_rate_frequency greater than 0.40 and at most 0.50, Theriodic volatility greater than 0.025, and Fed mean at most 2.0. When that condition holds AND option.steps_until_expiry is at most one, move the value one cent AWAY from one half - subtract 0.01 when it is below 0.50 and add 0.01 when it is above 0.50, leaving it unchanged at exactly 0.50 - and clamp the result into the closed interval from 0.0 to 1.0. Return the value. These are three dictionary lookups and a comparison, with no loop over history, which matters because price_option is called once per quote, once per FOK and once per open position inside signed_reserve. Do not touch price_option_from_parameters, quote, respond_to_fok, warm_up, either bookkeeping hook or __init__. Expected tradeoff: near-expiry quotes in the label are centred one cent closer to the terminal value, which should win the contested flow at a better price; the risk is that the shift is the wrong sign and we simply pay a cent more on every near-expiry fill, which would show as a case-18 and case-20 PnL fall.
- Worker summary: Routed both price_option return paths through one new MarketMaker helper that rebuilds fed_low_mean_regime term for term from warm_up_statistics exactly as quote builds it, and, when it holds and steps_until_expiry is at most one, moves the value one cent away from one half and clamps to [0.0, 1.0]. price_option_from_parameters, quote, respond_to_fok, warm_up, both hooks and __init__ are untouched. Compiled and passed scope validation with target method price_option.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 261.53; minimum capital 37.33/40.00
- Baseline delta: 0.00 points; PnL 15.71
- Objective outcome: target 0.00; gap 7.39; collateral loss 0.00; expected not met

### g6-label-theo-toward-half

- Hypothesis: The opposite bias holds: our near-expiry theo in the {18, 20} session is over-confident, because the live idiosyncratic volatility blend is fitted on multi-step returns and understates the jump risk that remains in the final step, so pulling the value one cent back toward one half prices the residual uncertainty the model omits and stops us being picked off on the side that settles against us.
- Implementation plan: Identical mechanics to the terminal-direction arm and identical scope. In price_option, keep the existing None guard and the live-volatility blend exactly as written, compute the returned theoretical value into a local variable on both paths, then add one guarded adjustment. Form the fed_low_mean_regime condition from self.warm_up_statistics exactly as quote forms it - flat_rate_frequency greater than 0.40 and at most 0.50, Theriodic log-return sample_std_dev greater than 0.025, Fed funds mean at most 2.0 - using the same module constants and access paths. When that condition holds AND option.steps_until_expiry is at most one, move the value one cent TOWARD one half - add 0.01 when it is below 0.50 and subtract 0.01 when it is above 0.50, leaving it unchanged at exactly 0.50 - and clamp the result into the closed interval from 0.0 to 1.0. Take care that the adjustment can never step across one half in a way that inverts the sign of the correction: clamp the shifted value to 0.50 on each side, so a value at 0.495 becomes 0.50 rather than 0.505. No loops over history. Do not touch price_option_from_parameters, quote, respond_to_fok, warm_up, either bookkeeping hook or __init__. Expected tradeoff: near-expiry quotes are centred one cent nearer one half, conceding the extreme and keeping the middle; the risk is the mirror image of the terminal arm's.
- Worker summary: Same structure and the same regime condition, moving the value one cent toward one half instead, with an explicit clamp at 0.50 on each side so the shift can never cross the midpoint and invert its own sign, then the [0.0, 1.0] clamp. Compiled and passed scope validation with target method price_option.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 277.33; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 31.51
- Objective outcome: target 0.00; gap 9.48; collateral loss 0.00; expected not met

### g6-label-theo-terminal-session-wide

- Hypothesis: If a near-expiry theo bias exists it is a property of the session's fitted parameters rather than of the final step, so the same correction belongs on every contract in the {18, 20} session; read against the near-expiry arm this also attributes any gain to the near contracts or the far ones, which is the decomposition that made generations 4 and 5 interpretable.
- Implementation plan: Identical mechanics and identical scope to the terminal-direction arm, with the expiry gate removed. In price_option, keep the existing None guard and the live-volatility blend exactly as written, compute the returned theoretical value into a local variable on both paths, then add one guarded adjustment. Form the fed_low_mean_regime condition from self.warm_up_statistics exactly as quote forms it - flat_rate_frequency greater than 0.40 and at most 0.50, Theriodic log-return sample_std_dev greater than 0.025, Fed funds mean at most 2.0 - using the same module constants and access paths. When that condition holds, with NO condition on option.steps_until_expiry, move the value one cent AWAY from one half - subtract 0.01 when it is below 0.50 and add 0.01 when it is above 0.50, leaving it unchanged at exactly 0.50 - and clamp the result into the closed interval from 0.0 to 1.0. No loops over history. Do not touch price_option_from_parameters, quote, respond_to_fok, warm_up, either bookkeeping hook or __init__. Expected tradeoff: this is the widest allocation of the lever and therefore the one with the largest signed_reserve coupling, since it shifts the theo of every open position in the session; the risk is that the far contracts, which generation 4 showed are where the label's genuine adverse selection lives, are mispriced in the other direction and cancel or reverse the near contribution.
- Worker summary: Same regime condition and the same away-from-one-half shift with no expiry gate, applied inline rather than through a helper by moving the live-volatility blend under an else branch; the blend body is verbatim. price_option_from_parameters is byte-identical. Compiled and passed scope validation with target method price_option.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 263.73; minimum capital 39.09/40.00
- Baseline delta: 0.00 points; PnL 17.91
- Objective outcome: target 0.00; gap 16.57; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: The pricing hypothesis is confirmed as a mechanism and refuted as a score path, and it is by a wide margin the most powerful lever this surface has ever exposed. All three arms scored 14.70 with 20/20 structurally complete, zero bankruptcies and zero runtime errors, and none improved the target: every arm widened the case-18 gap. But the MAGNITUDES are unlike anything fifteen width and size sources produced. Where the entire near-expiry width and size surface moved case 18 by at most 0.13 and case 20 by at most 1.06, a single cent of theo shift moved them by dollars. Shifting near-expiry theo one cent toward one half took case 20 from +0.15 to +10.02, a 9.87 gain, and combined PnL from 268.26 to 277.33, the highest figure this repository has recorded; it simultaneously took the case-18 leader from 19.25 to 26.59 and our own case-18 PnL from 17.91 to 17.11, widening that gap to 9.48. Shifting one cent AWAY from one half is negative on both: case 18 falls to 14.00 against a leader at 21.39 for a 7.39 gap, and case 20 falls to -2.67. Removing the expiry gate and applying the away-from-half shift session-wide is the worst arm ever graded on this file: the case-18 leader reaches 31.01 and the gap 16.57. Three conclusions follow. First, our current theo is at a local optimum for case 18 within the one-cent neighbourhood in BOTH directions, which is the strongest possible evidence that the 1.34 residual is not a pricing bias. Second, the case-18 leader's PnL responds by seven to twelve dollars to a one-cent move in our theo, so case-18 flow is intensely contestable through price even though it is inert to width and size - the two facts together say the flow is priced, not spread-limited. Third, and most valuable: the {18, 20} label's two sessions respond to the SAME theo shift with opposite signs, case 20 gaining 9.87 while case 18 loses 0.80 and its leader gains 7.34. That is the first mechanism ever found that separates the two sessions behaviourally, and it is worth roughly fifty times more per cent than any width lever.
Next-generation rationale: This run is complete at six generations and the champion is unchanged at 14.70, but the board has been reshaped and the next run should not open on case 18. Three things changed. The near-expiry label is closed on every axis this file exposes - width level, side, option scope and expiry cutoff; the repeat cent's level and its expiry allocation; the fill-signal cent; the wing block; the unwind lot's increment and both position thresholds; the entire repeat-request size ladder; and now a one-cent theo shift in both directions and both expiry allocations. Case 6's uniform eighteen-cent width is now explained rather than merely measured: its near flow is adverse and its far flow is profitable, eighteen splits the difference, and the recoverable amount is bounded near 0.37 because the leader captures exactly what we decline. And price_option is established as a high-magnitude lever whose sign differs between case 18 and case 20 - the discriminator hunt that Section 7 of the memo has carried unresolved for several runs now has a behavioural handle, because a theo shift toward one half is worth +9.87 to case 20 and -0.80 to case 18. Case 20 is already rank 1 so that PnL is telemetry, but the same asymmetry run in reverse is a case-18 lever if it can be gated. The first target of the next run should be a sub-cent theo shift, which reaches respond_to_fok and the signed_reserve capacity arithmetic without moving the quantized quote at all, and which is therefore the only way found so far to act on one of price_option's three consumers in isolation.
