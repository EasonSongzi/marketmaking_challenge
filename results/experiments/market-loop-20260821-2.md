# Market-Maker Experiment: market-loop-20260821-2

- Status: active
- Started: 2026-08-21T15:39:38.047Z
- Starting baseline: g6-lowband-size-eight (14.70/16.00)
- Current baseline: g6-lowband-size-eight (14.70/16.00)
- Stop condition: not reached
- Score trend: 14.70

The fixed grader is evaluated once per unique source SHA-256; repeated sources reuse cached case evidence. Fixture-only validation uses stubbed evidence.

## Generation 1: explore quote

Case 18 is the best-founded target left on the board. It holds 40.00 of capital, the largest in the challenge, and the champion's unconditional cash_floor of 0.75 * self.cash_balance leaves it only 10.00 deployable, so the five-lot bid and six-lot offer ceilings bind long before capital does. Fixed Width 0.05 earns 39.01 there, which under the leaderboard-archetype rule means a tight quoter is being paid and session flow is benign, exactly the signature cases 8 and 10 carried before the promotion that took this repository from 14.10 to 14.70. That promotion measured, rather than assumed, the shape of the response: relaxing the reserve inside an exact warm-up label lifted case 8 from 6.95 to 8.51 and case 10 from 14.17 to 16.16, but a threefold change in deployable capital bought only 0.09 more in case 10 and nothing in case 8, and it was lifting the quantity ceilings that produced the rank flip to 19.58 and 24.97. This generation runs that identical chain against case 18, compressed into one bracket: a floor-only control that isolates capital, then two ceiling rungs at eight and sixteen lots. Every added lot is individually tested by the champion's own guard expressions, signed_reserve + n * bid_price <= available_capacity on the bid and active_exposure + n * (1.0 - offer_price) <= available_capacity on the offer, so no arm can commit capital the champion's arithmetic would refuse. The gate must be a session-constant warm-up label, never self.cash_balance, which the grader mutates and which already cost run market-loop-20260819-3 its rank. Middle band with theriodic_return_volatility > 0.025 is {5, 11, 14, 16, 18, 20}, and fed_history_mean > 2.0 narrows that to {5, 11, 14, 16}, so fed_history_mean <= 2.0 inside the slice is exactly {18, 20} and needs no new tomography. Case 20 is therefore inside the gate at rank 1 and is the entire collateral exposure, budgeted at its full 1.00. Its 12.49 margin over Lattice is the buffer, but the exposure is genuine and asymmetric: case 20 is a toxic session in which every competitor loses, Fixed Width 0.05 loses 98.23 quoting a five-cent half-width against our four, and our 0.23 profit there is plausibly a consequence of the very size limits these arms relax, so the margin can erode from both ends at once as declined flow is routed back. That is why the bracket starts with a floor-only control rather than opening at the largest rung. An arm that moves case 20 off rank 1 loses 1.00 against a 0.20 prize and is disqualified by the score-only rule regardless of what it does to case 18. Case 10, at margin 0.96 the most fragile rank in the repository, sits at flat_rate_frequency <= 0.40 and cannot reach this middle-band gate under any cash trajectory; the same holds for case 13 at margin 2.41 and case 14 at margin 3.30, which fail the volatility and fed-mean tests respectively. The honest ceiling on the prize is that case 18's gap is 31.57, the second largest on the board, so capacity alone need not close it; the case-10 exchange rate, where 1.99 to us cost the leader 2.74, is the mechanism that makes the attempt worth one generation.

Objective: exploit; targets 18; expected +0.20; collateral budget 1.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g1-eighteen-floor-none

- Hypothesis: Capital, not the quantity ceiling, is what caps case 18: removing the 75 percent reserve inside the exact {18, 20} label quadruples deployable capital from 10.00 to 40.00 and lets the champion's existing exposure-gated escalations reach their four, five and six lot rungs on far more quotes than they can today.
- Implementation plan: Two edits in MarketMaker.quote and nothing else. First, immediately after the existing 'low_band_regime: bool = flat_rate_frequency <= 0.40' assignment, add one new session label in the file's existing idiom: 'volatile_low_fed_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)'. The locals flat_rate_frequency, theriodic_return_volatility and fed_history_mean are already computed earlier in the method and are still in scope; do not recompute them and do not read self.warm_up_statistics again. Second, change only the ternary inside the existing line 'cash_floor = (0.0 if low_band_regime else 0.75) * self.cash_balance' so that the new label also selects a zero floor, reading 'cash_floor = (0.0 if low_band_regime or volatile_low_fed_regime else 0.75) * self.cash_balance'. Leave the following exactly as they are: the wide_regime, case_five_regime, case_seven_regime, case_thirteen_regime, case_nineteen_regime and low_band_regime predicates, the half_width ladder, the bid_price and offer_price adjustments, the fill-signal block, the signed_reserve accumulation loop, the 'available_capacity = self.cash_balance - cash_floor' line, every existing bid_quantity and offer_quantity rule and escalation threshold, the existing 'if low_band_regime:' while-loop block, and the case_five_regime and case_thirteen_regime overrides. Add no new quantity rule of any kind in this candidate. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any module-level code. Outside the new label the ternary evaluates exactly as before, so behaviour is bit-for-bit identical to the champion in every session other than cases 18 and 20. Expected footprint versus the champion: cases 18 and 20 only. Expected tradeoff: this is the control rung and the lowest-risk arm for case 20, since it raises no quantity constant and can only let the champion's own already-guarded escalations fire more often; differencing it against the two ceiling rungs is what separates a capital constraint from a ceiling constraint in a single generation.
- Worker summary: Two edits in quote. Added 'volatile_low_fed_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)' immediately after the existing low_band_regime assignment, reusing the three locals already computed earlier in the method with no second read of self.warm_up_statistics, and widened the existing ternary to 'cash_floor = (0.0 if low_band_regime or volatile_low_fed_regime else 0.75) * self.cash_balance'. No new quantity rule of any kind. The half_width ladder, both price adjustments, the fill-signal block, the signed_reserve loop, the available_capacity expression, every existing bid and offer quantity rule and escalation threshold, the existing low_band_regime while-loop block, and the case_five_regime and case_thirteen_regime overrides are all byte-identical to the champion. Lead read the diff directly and confirmed six insertions and one deletion, that prices are untouched so the Quote invariants are unchanged by construction, and that the new predicate is disjoint from case_five_regime on fed_history_mean. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.50/16.00 points; PnL 241.06; minimum capital 36.64/40.00
- Baseline delta: -0.20 points; PnL -4.76
- Objective outcome: target -0.20; gap 45.91; collateral loss 0.00; expected not met

### g1-eighteen-size-eight

- Hypothesis: As in the low band, capital saturates almost immediately and the real cap on case 18 is the five-lot bid and six-lot offer ceiling, so pairing the zero floor with a bounded escalation to eight lots per side, each lot individually solvency-tested by the champion's own capacity arithmetic, is what actually moves case 18 toward rank 1.
- Implementation plan: Three edits in MarketMaker.quote and nothing else. First and second, identical to g1-eighteen-floor-none: add 'volatile_low_fed_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)' immediately after the existing 'low_band_regime: bool = flat_rate_frequency <= 0.40' assignment, and change the existing cash_floor line to 'cash_floor = (0.0 if low_band_regime or volatile_low_fed_regime else 0.75) * self.cash_balance'. Third, insert a single new block immediately after the champion's existing 'if low_band_regime:' while-loop block and before the 'if case_five_regime:' quantity override, in the same position the promoted low-band escalation occupies: 'if volatile_low_fed_regime:' followed by two bounded while loops that raise each side one lot at a time only while the champion's own guard permits it, namely 'while bid_quantity < 8 and signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'while offer_quantity < 8 and active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. The locals signed_reserve, active_exposure and available_capacity are already computed earlier in the method and are still in scope. Each loop can only ever increase a quantity, never decrease one, and each increment is tested by the same expression the champion already applies to its own four, five and six lot escalations, so no arm can commit capital the champion's arithmetic would refuse. Leave everything else exactly as it is: every regime predicate, the half_width ladder, the price adjustments, the fill-signal block, the signed_reserve loop, the available_capacity line, every existing quantity rule and threshold, the existing low_band_regime block, and the case_five_regime and case_thirteen_regime overrides. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any module-level code. Expected footprint versus the champion: cases 18 and 20 only. Expected tradeoff: this is the rung that won the low band and the most likely arm to lift case 18 while keeping enough restraint to hold case 20's rank 1.
- Worker summary: Identical to g1-eighteen-floor-none on its first two edits, plus one new block inserted immediately after the champion's existing 'if low_band_regime:' while-loop block and before the 'if case_five_regime:' override: 'if volatile_low_fed_regime:' followed by 'while bid_quantity < 8 and signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'while offer_quantity < 8 and active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. Both loops only ever increase a quantity and every increment is tested by the same guard expression the champion already applies to its own four, five and six lot escalations, so no lot is committed that the champion's arithmetic would refuse. Everything else is byte-identical to the champion. Lead read the diff directly and confirmed eleven insertions and one deletion and the exact insertion position. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.50/16.00 points; PnL 243.94; minimum capital 38.38/40.00
- Baseline delta: -0.20 points; PnL -1.88
- Objective outcome: target -0.20; gap 42.99; collateral loss 0.00; expected not met

### g1-eighteen-size-sixteen

- Hypothesis: Case 18 needs 31.57, four times what the low-band flip needed, so if its flow is genuinely benign the binding constraint should be solvency alone; a sixteen-lot ceiling lets the champion's own capacity arithmetic rather than an arbitrary constant decide every fill and is the endpoint that shows whether case 18 has any reachable headroom at all.
- Implementation plan: Identical to g1-eighteen-size-eight in every respect except that both ceiling literals are 16 rather than 8, so the two bounded loops read 'while bid_quantity < 16 and signed_reserve + (bid_quantity + 1) * bid_price <= available_capacity: bid_quantity += 1' and 'while offer_quantity < 16 and active_exposure + (offer_quantity + 1) * (1.0 - offer_price) <= available_capacity: offer_quantity += 1'. Add the same 'volatile_low_fed_regime: bool = (0.40 < flat_rate_frequency <= 0.50 and theriodic_return_volatility > 0.025 and fed_history_mean <= 2.0)' label immediately after the existing 'low_band_regime: bool = flat_rate_frequency <= 0.40' assignment, make the same single change to the existing cash_floor ternary so it reads 'cash_floor = (0.0 if low_band_regime or volatile_low_fed_regime else 0.75) * self.cash_balance', and insert the same 'if volatile_low_fed_regime:' block in the same position, immediately after the existing 'if low_band_regime:' block and before the 'if case_five_regime:' override. Change nothing else: no regime predicate, no half_width entry, no price adjustment, no existing quantity rule or threshold, and no other method. Expected footprint versus the champion: cases 18 and 20 only. Expected tradeoff: the endpoint rung and the highest risk of the three on both axes, bankruptcy in case 18 and rank loss in case 20, bounded on every increment only by the champion's unchanged per-fill guard; its value is that differencing it against the eight-lot rung and the floor-only control resolves the whole capacity response for the {18, 20} label in one generation rather than three.
- Worker summary: Identical to g1-eighteen-size-eight in every respect except that both ceiling literals are 16 rather than 8, so the two bounded loops read 'while bid_quantity < 16 ...' and 'while offer_quantity < 16 ...'. Same new predicate in the same position, same single change to the cash_floor ternary, same block position immediately after the existing low_band_regime block and before the case_five_regime override, same reuse of the champion's own per-lot guard expressions. Everything else is byte-identical to the champion. Lead read the diff directly and confirmed eleven insertions and one deletion. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.50/16.00 points; PnL 243.52; minimum capital 38.36/40.00
- Baseline delta: -0.20 points; PnL -2.30
- Objective outcome: target -0.20; gap 43.01; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE CASE-18 CAPACITY HYPOTHESIS IS FALSIFIED IN THE NEGATIVE DIRECTION, AND THE INSTRUMENT THAT PREDICTED IT IS FALSIFIED WITH IT. All three sources passed 20/20 with zero bankruptcies and zero runtime errors, and all three scored 14.50, a loss of 0.20 against the champion's 14.70. The entire loss is in the target case itself, not in collateral. Case 18 falls 7.44 to -3.36 at the zero floor alone, -1.62 at the eight-lot ceiling and -1.64 at sixteen lots, dropping from rank 2 to rank 3 behind Lattice in every arm. More capacity in case 18 is not merely unhelpful, it is actively destructive, and the response is already saturated: eight and sixteen lots are indistinguishable at -1.62 and -1.64, and the ceiling arms are marginally LESS damaging than the floor-only control, so essentially all of the harm comes from removing the reserve and none of it from the size ceiling. The case-18 capacity axis is therefore closed downward on graded points at floor 0.75 and 0.0 crossed with ceilings of roughly five and six, eight, and sixteen. THE DECISIVE MEASUREMENT IS THE LEADER'S RESPONSE, AND IT POINTS THE OPPOSITE WAY FROM CASES 8 AND 10. When we took more low-band flow the leader's PnL FELL, 27.86 to 26.01 in case 8 and 35.17 to 32.43 in case 10, which is what made contesting pay from both ends. Here the leader's PnL ROSE as we took more flow, Fixed Width 0.05 going 39.01 to 42.55 at the zero floor and 41.37 at both ceilings, while Lattice also rose 2.25 to 6.98 and Mongoose barely moved. We did not take profitable flow away from the leader; we absorbed flow the leader was declining. That retires the leaderboard-archetype rule as a forward predictor. 'A tight quoter earns, therefore flow is benign and taking more of it pays' was true in cases 8, 10 and 20 and is false in case 18, and the only reliable form of the instrument is the endogenous leader response, which is measurable only after the fact. Any future plan citing the archetype rule must treat it as a hypothesis, not evidence. THE COLLATERAL CASE MOVED THE OPPOSITE WAY FROM THE TARGET, INSIDE THE SAME LABEL. Case 20 rose 0.23 to 6.27, 7.41 and 7.01, held rank 1 in every arm, and its margin widened as every competitor fell further, Fixed Width 0.05 going -98.23 to -107.35. Realised collateral loss is 0.00 in score terms against a declared budget of 100, and the budgeted case improved by up to 7.18 in PnL while the target case lost up to 10.80. Cases 18 and 20 share one exact warm-up label and want strictly opposite treatments, the same pattern cases 18 and 19 already showed at the same 40.00 capital. Splitting them is worth nothing, however, because case 20 is already at rank 1 and its gains are pure telemetry, so 18-from-20 remains the unfound threshold and remains not worth finding. Objective outcome: target gain -10.80, -9.06 and -9.08 hundredths of dollars against a declared expectedGainHundredths of 20; target gap widens from 31.57 to 45.91, 42.99 and 43.01; collateral loss 0.00 against a budget of 100. Every other scored case is byte-identical to the champion in all three arms, including case 10 at margin 0.96, case 13 at 2.41 and case 14 at 3.30, which confirms the gate is exactly {18, 20} as the session map predicted and that the warm-up label, unlike a cash-balance gate, leaked nothing. THE RESULT IMPLICATES THEO DIRECTLY, WHICH IS THE PRECONDITION THE MEMO SET FOR THE LAST UNTOUCHED SURFACE. We quote case 18 at a four-cent half-width, TIGHTER than the five-cent Fixed Width 0.05 that earns 39.01 there, and we lose money on the marginal lot while it earns on every lot. Two participants at almost the same width cannot differ this much on width; they differ on where the quote is centred. Case 1 THEO passes, so price_option_from_parameters is correct when handed true parameters, which leaves warm_up's estimation of those parameters as the only remaining explanation. Our shortfall in case 18 is a pricing error, not a capacity limit, and no amount of size or capital can fix it.
Next-generation rationale: Run generation 2 as Explore on warm_up, the only core method never targeted by any generation, and attack one specific hard-coded constant rather than the estimator as a whole. MarketParameters carries a rate_target field describing the level the fed funds rate mean-reverts toward, and warm_up never estimates it. It is hard-coded to 2.0 in three separate places: the literal 'rate_target=2.0' passed to the MarketParameters constructor, and the expression 'target_gaps = tuple(2.0 - value for value in initial_values)' in both _estimate_rate_parameters and _fit_penalized_rate, where it drives the reversion-strength fit and both transition probabilities. Every rate-leg price in every session is therefore computed under the unquestioned assumption that rates revert to 2.0, and tilted_rate_probabilities applies that assumption at every step of the multi-step tree in _terminal_rate_probabilities, so the bias compounds with steps_until_expiry. This is the exact structural analogue of the cash_floor = 0.75 constant whose removal produced the last promotion: a self-imposed constant nobody had questioned. The alignment with this generation's evidence is direct. The label on which we just measured a pricing failure is fed_history_mean <= 2.0, which is to say the sessions in which the observed rate history sits at or below the assumed reversion target are exactly the sessions where our marginal flow is adversely selected. If the true target is below 2.0 there, our tilt pushes up-probabilities too high and we systematically overprice every rate-leg option in the session, which is precisely the signature of losing money at a tighter width than a competitor who earns. Use three structurally distinct estimators of the same missing parameter, coarse, principled and conservative, so that one generation resolves the question in both possible worlds. The first replaces 2.0 with the arithmetic mean of the rate history in all three places, the endpoint estimator. The second estimates it by the criterion the file already uses, extending the _fit_penalized_rate grid search to profile jointly over rate_target on the 0.25 strike grid across the observed range rather than fixing it. The third shrinks the historical mean toward 2.0 by observation count in the idiom the file already applies to its transition probabilities, which is the hedge that limits damage if 2.0 turns out to be the true generator value in every session. That last possibility is the real risk and must be stated: rate_target = 2.0 is the dataclass default in the supplied template, so the challenge may genuinely use 2.0 everywhere, in which case estimating it only adds a free parameter that overfits short histories. The shrinkage arm is what makes the generation informative rather than merely dangerous, and differencing the three arms answers the question either way. Declare an exploit objective over the four losing cases 6, 7, 18 and 19 with expectedGainHundredths 20, since one rank flip is the realistic prize, and a real collateral budget of 400 rather than 0, because unlike every generation since per-session labels became the unit of work this change is global, is not gated on any warm-up label, and touches the theo of all sixteen scored cases including twelve held ranks and the three thin margins at 0.96, 2.41 and 3.30. Every arm must confine itself to warm_up and its helpers, must leave price_option_from_parameters untouched since case 1 proves it correct, and must keep rate_target non-negative and the fitted probabilities inside the MarketParameters __post_init__ constraints so that no arm can raise a runtime error.

## Generation 2: explore warm_up

Generation 1 supplied the direct implication of theo that the memo required before promoting warm_up from research to dispatch. It gave case 18 four times the deployable capital and up to sixteen lots a side and case 18 went from 7.44 to -3.36, dropping to rank 3, while the leader's PnL ROSE from 39.01 to 42.55. We did not take profitable flow from the leader, we absorbed flow the leader was declining. We quote case 18 at a four-cent half-width, tighter than the five-cent Fixed Width 0.05 that earns 42.55 there, and we lose on the marginal lot while it earns on every lot. Two participants at almost the same width cannot differ this much on width; they differ on where the quote is centred. Case 1 THEO passes, so price_option_from_parameters is correct when handed true parameters, which leaves warm_up's estimation of those parameters as the only remaining explanation. This generation attacks one specific hard-coded constant inside that estimator rather than the estimator as a whole. MarketParameters carries a rate_target field describing the level the fed funds rate mean-reverts toward, and warm_up never estimates it. It is hard-coded to 2.0 at four sites: the literal 'rate_target=2.0' passed to the MarketParameters constructor, the expression 'target_gaps = tuple(2.0 - value for value in initial_values)' in both _estimate_rate_parameters and _fit_penalized_rate, and the inner 'tilt = reversion_strength * (2.0 - rate_value)' of the penalized likelihood. Every rate-leg price in every session is computed under the unquestioned assumption that rates revert to 2.0, and tilted_rate_probabilities reapplies that assumption at every step of the multi-step tree in _terminal_rate_probabilities, so the bias compounds with steps_until_expiry. This is the structural analogue of the cash_floor = 0.75 constant whose removal produced the last promotion: a self-imposed constant no generation had questioned. The alignment with generation 1's evidence is direct rather than decorative. The label on which the pricing failure was just measured is fed_history_mean <= 2.0, so the sessions in which the observed rate history sits at or below the assumed reversion target are exactly the sessions where our marginal flow is adversely selected. If the true target is below 2.0 there, the tilt pushes up-probabilities too high, we systematically overprice every rate-leg option in the session, and we are picked off at a tighter width than a competitor who earns. The three arms are a coarse endpoint, a principled fit by the file's own criterion, and a conservative midpoint, so one generation resolves the question in both possible worlds. The risk that makes the conservative arm necessary must be stated plainly: rate_target = 2.0 is the dataclass default in the supplied template, so the challenge may genuinely use 2.0 in every session, in which case estimating it only adds a free parameter that overfits a short history and every arm should lose. Differencing a full-mean arm against a half-mean arm answers that either way, since if 2.0 is correct the damage must be monotone in how far the estimate moves away from it. The collateral budget is 400 and is real. Unlike every generation since per-session warm-up labels became the unit of work, this change is global, is gated on no label, and moves the theo of all sixteen scored cases at once, including twelve held ranks and the three thin margins: case 10 at 0.96, case 13 at 2.41 and case 14 at 3.30. Case 1 is unaffected by construction because the THEO test supplies true parameters and never calls warm_up's estimate. Every arm must keep rate_target non-negative and leave the existing probability clamps in place so that MarketParameters.__post_init__ cannot raise, since a runtime error is always ineligible.

Objective: exploit; targets 6, 7, 18, 19; expected +0.20; collateral budget 4.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g2-rate-target-mean

- Hypothesis: The fed funds rate reverts toward its own recent historical level rather than toward a fixed 2.0, so replacing the hard-coded constant with the arithmetic mean of the warm-up rate history removes a systematic bias from every rate-leg price, and removes the most bias precisely in the sessions whose rate history sits furthest from 2.0.
- Implementation plan: Edit MarketMaker.warm_up and the two MarketMaker rate helpers it calls. Do not touch quote, respond_to_fok, price_option, price_option_from_parameters, on_trade, on_step_advance, name, __init__, or any module-level code, and in particular do not modify the MarketParameters or WarmUpStatistics dataclasses. Step 1: in warm_up, immediately after the local 'rate_values' is bound and before the call to self._estimate_rate_parameters, add one line 'estimated_rate_target: float = sum(rate_values) / len(rate_values)'. Step 2: add a trailing parameter 'rate_target: float' to the static helper '_estimate_rate_parameters(rate_values)' so it reads '_estimate_rate_parameters(rate_values: tuple[float, ...], rate_target: float)', and inside it change the single line 'target_gaps: tuple[float, ...] = tuple(2.0 - value for value in initial_values)' to use 'rate_target - value'. Change nothing else in that helper: the reversion-strength moment formula, the probability formulas, the 1e-6 minimum-probability clamp and the sum-greater-than-one rescale all stay exactly as they are. Step 3: add a trailing parameter 'rate_target: float' to the static helper '_fit_penalized_rate(rate_values, smoothed_up_probability, smoothed_down_probability, moment_reversion_strength)', and inside it change exactly two expressions: 'target_gaps: tuple[float, ...] = tuple(2.0 - value for value in initial_values)' to use 'rate_target - value', and the inner-loop line 'tilt: float = reversion_strength * (2.0 - rate_value)' to 'tilt: float = reversion_strength * (rate_target - rate_value)'. Leave the 1001-step reversion grid, the log-likelihood accumulation, the three penalty terms with their 10.0, 10.0 and 1.0 weights, the best-score bookkeeping, and the RuntimeError fallback exactly as they are. Step 4: pass 'estimated_rate_target' as the new trailing argument at both call sites in warm_up, and change the literal in the MarketParameters constructor from 'rate_target=2.0' to 'rate_target=estimated_rate_target'. Change nothing else in warm_up: the missing-underlying and num_days checks, the log-return and daily-change construction, the probability smoothing, the two linear regressions, the residual variance and covariance arithmetic, the sector decomposition, sector_std_dev=1.0, rate_step=RATE_STRIKE_GRID, and the entire WarmUpStatistics construction including rate_transition_frequencies must all remain byte-identical, because quote's session labels are computed from those statistics and any change to them would silently re-partition every case. Safety: rate_values are fed funds rates and are non-negative, so their mean is non-negative and MarketParameters.__post_init__ cannot raise on rate_target; the existing 1e-6 clamps and rescale still bound both transition probabilities; the reversion strength is still clamped to [0, 1]. Expected footprint: every scored case, since this is a global theo change. Expected tradeoff: the endpoint estimator and the largest expected move in both directions, which is what makes it the informative bracket end.
- Worker summary: Threaded an estimated reversion target through warm_up and its two rate helpers, replacing the hard-coded 2.0 at all four sites. Added 'estimated_rate_target: float = sum(rate_values) / len(rate_values)' in warm_up before the first helper call; added a trailing 'rate_target: float' parameter to the static helpers _estimate_rate_parameters and _fit_penalized_rate; changed 'target_gaps = tuple(2.0 - value ...)' to use 'rate_target - value' in both; changed the inner 'tilt = reversion_strength * (2.0 - rate_value)' to use rate_target; passed the estimate at both call sites and set 'rate_target=estimated_rate_target' in the MarketParameters constructor. The 1e-6 probability clamps, the sum-greater-than-one rescale, the reversion clamp to [0, 1], the 1001-step grid, the 10.0/10.0/1.0 penalty weights and the RuntimeError fallback are untouched, and the entire WarmUpStatistics construction is byte-identical so quote's session labels cannot shift. Lead read the diff directly and confirmed eleven insertions, six deletions, all four 2.0 sites replaced, and zero remaining 'rate_target=2.0'. Scope validation against target-method warm_up and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.80/16.00 points; PnL 214.91; minimum capital 34.03/40.00
- Baseline delta: -0.90 points; PnL -30.91
- Objective outcome: target 0.00; gap 78.28; collateral loss 0.90; expected not met

### g2-rate-target-profile

- Hypothesis: The reversion target should be fitted by the same penalized-likelihood criterion the file already uses to fit reversion strength and the transition probabilities, rather than assumed or replaced by a raw moment, because the profile fit can reject a target the data do not support and fall back toward the historical evidence on its own.
- Implementation plan: Edit only MarketMaker._fit_penalized_rate and the MarketMaker.warm_up lines that consume it. Do not touch quote, respond_to_fok, price_option, price_option_from_parameters, _estimate_rate_parameters, or any dataclass. Step 1: in _fit_penalized_rate, keep the existing signature and body structure but profile jointly over the reversion target. Build the candidate target grid on the challenge's own 0.25 rate grid across the observed history: 'lowest_target: float = min(rate_values)', 'highest_target: float = max(rate_values)', and iterate 'candidate_target' over 'lowest_target + 0.25 * index' for index in range(int(round((highest_target - lowest_target) / 0.25)) + 1). Wrap the existing reversion loop inside this target loop, replace both hard-coded 2.0 occurrences inside the body, namely 'target_gaps' and the inner 'tilt', with 'candidate_target', and extend the best-score bookkeeping so that best_parameters carries four values, the two probabilities, the reversion strength and the winning candidate_target. Change the helper's return annotation to 'tuple[float, float, float, float]' and return that four-tuple. Keep the log-likelihood accumulation, the three penalty terms with their 10.0, 10.0 and 1.0 weights, the break on non-positive transition probability, the 1e-6 clamps, the sum-greater-than-one rescale, and the 'if best_parameters is None: raise RuntimeError(...)' fallback exactly as they are. Step 2: to keep the joint search bounded, coarsen the reversion grid inside this arm only, from 'for candidate_index in range(1001)' with 'reversion_strength = candidate_index / 1000.0' to 'for candidate_index in range(101)' with 'reversion_strength = candidate_index / 100.0'. This is deliberate and is the cost of the extra dimension: total work stays within a small multiple of the parent's, which matters because warm_up runs inside the graded session. Step 3: in warm_up, unpack four values from the call, 'rate_up_probability, rate_down_probability, rate_reversion_strength, fitted_rate_target = self._fit_penalized_rate(...)', leave the call arguments unchanged, and change the MarketParameters literal from 'rate_target=2.0' to 'rate_target=fitted_rate_target'. Leave the call to self._estimate_rate_parameters and that helper itself completely unchanged, since it only supplies the moment prior that the penalty terms shrink toward. Change nothing else in warm_up, and in particular leave the entire WarmUpStatistics construction byte-identical, because quote's session labels are computed from it. Safety: every candidate target is drawn from the observed rate history and fed funds rates are non-negative, so rate_target is non-negative and MarketParameters.__post_init__ cannot raise; if the history is flat the grid degenerates to a single point and the helper behaves exactly like the parent with the target set to that level; the existing clamps still bound both probabilities. Expected footprint: every scored case. Expected tradeoff: the principled arm; if 2.0 is the true generator value in every session, this arm should move least of the three because the penalty terms and the likelihood will both favour a target near the data, and it is the arm most likely to hold the twelve currently held ranks.
- Worker summary: Fitted the reversion target by the file's own penalized-likelihood criterion instead of assuming it. In _fit_penalized_rate, wrapped the existing reversion loop in a candidate-target loop over the challenge's 0.25 rate grid spanning min(rate_values) to max(rate_values), with 'target_step_count = int(round((highest_target - lowest_target) / 0.25)) + 1' so a flat history degenerates to exactly one candidate and reproduces parent behaviour at that level. Both 2.0 occurrences inside the body, the target_gaps expression and the inner tilt, now use candidate_target; best_parameters carries a fourth value and the return annotation is tuple[float, float, float, float]. The inner reversion grid was deliberately coarsened from 1001 to 101 steps in this arm only to bound the joint search, giving roughly twice the parent's work for a typical rate range. warm_up unpacks four values and sets 'rate_target=fitted_rate_target'; the call arguments, _estimate_rate_parameters, the log-likelihood accumulation, the three penalty weights, the break on non-positive transition probability, the clamps, the rescale and the RuntimeError fallback are all unchanged, and WarmUpStatistics is byte-identical. Lead read the diff directly and confirmed the grid cannot be empty and every candidate target is drawn from the observed non-negative rate history, so __post_init__ cannot raise. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.50/16.00 points; PnL 207.72; minimum capital 31.82/40.00
- Baseline delta: -1.20 points; PnL -38.10
- Objective outcome: target 0.00; gap 81.82; collateral loss 1.20; expected not met

### g2-rate-target-midpoint

- Hypothesis: If the true reversion target is 2.0 in every session then any estimate must be strictly harmful and the damage must be monotone in how far the estimate travels from 2.0, so a midpoint between the historical mean and 2.0 is the rung that both limits that damage and, differenced against the full-mean arm, reveals the sign and slope of the response in a single generation.
- Implementation plan: Identical to g2-rate-target-mean in every respect except the one line that forms the estimate. In warm_up, immediately after the local 'rate_values' is bound and before the call to self._estimate_rate_parameters, add 'estimated_rate_target: float = 0.5 * (sum(rate_values) / len(rate_values)) + 0.5 * 2.0'. Then make exactly the same three threading changes as that arm: add a trailing 'rate_target: float' parameter to the static helper _estimate_rate_parameters and use 'rate_target - value' in its single 'target_gaps' expression; add a trailing 'rate_target: float' parameter to the static helper _fit_penalized_rate and use 'rate_target - value' in its 'target_gaps' expression and 'rate_target - rate_value' in its inner 'tilt' expression; pass 'estimated_rate_target' at both call sites and change the MarketParameters literal from 'rate_target=2.0' to 'rate_target=estimated_rate_target'. Do not coarsen the 1001-step reversion grid in this arm and do not change the penalty weights, the clamps, the rescale, or the RuntimeError fallback. Change nothing else in warm_up: the validation checks, the log-return and daily-change construction, the probability smoothing, the regressions, the residual and sector arithmetic, sector_std_dev=1.0, rate_step=RATE_STRIKE_GRID, and the entire WarmUpStatistics construction must remain byte-identical, because quote's session labels read those statistics. Do not touch quote, respond_to_fok, price_option, price_option_from_parameters or any dataclass. Safety: identical to the full-mean arm; the midpoint of two non-negative numbers is non-negative, so __post_init__ cannot raise. Expected footprint: every scored case. Expected tradeoff: the conservative rung and the hedge that makes this generation informative rather than merely dangerous; it should show roughly half the movement of the full-mean arm in whichever direction that arm moves, and if both arms lose in proportion to their distance from 2.0 the constant is confirmed correct and the rate_target axis closes for good.
- Worker summary: Identical to g2-rate-target-mean in every respect except the single line that forms the estimate, which is 'estimated_rate_target: float = 0.5 * (sum(rate_values) / len(rate_values)) + 0.5 * 2.0'. Same trailing parameter added to both static helpers, same two expressions changed inside each, same estimate passed at both call sites and into the MarketParameters constructor. The 1001-step reversion grid is NOT coarsened in this arm and the penalty weights, clamps, rescale and RuntimeError fallback are unchanged. WarmUpStatistics is byte-identical. Lead read the diff directly and confirmed eleven insertions and six deletions and that the estimate is a convex combination of two non-negative quantities. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.10; minimum capital 38.72/40.00
- Baseline delta: 0.00 points; PnL -0.72
- Objective outcome: target 0.00; gap 78.91; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE HARD-CODED RATE TARGET OF 2.0 IS CORRECT AND THE AXIS IS NOW CLOSED, BUT THE GENERATION PAID FOR ITSELF BY ELIMINATING TWO OF THE FOUR REMAINING TARGETS. All three sources passed 20/20 with zero bankruptcies and zero runtime errors. The scores are 14.70 for the midpoint arm, 13.80 for the historical mean and 13.50 for the profile fit, against the champion's 14.70. The damage is monotone in how far the estimate travels from 2.0, which is exactly the falsification signature the plan declared in advance, so the conclusion is the one the plan named as the risk: rate_target = 2.0 is the dataclass default in the supplied template because the challenge generator genuinely uses it, and estimating it only adds a free parameter that overfits a short history. Case 7 reads 2.80 at the champion, 2.57 at the midpoint, 2.45 at the mean and 0.62 at the profile fit. Case 17 reads 24.97, 19.30, -5.97 and -8.18, a collapse of 30.94 at the mean that takes it from rank 1 to rank 4 behind every competitor. Case 10, the most fragile rank in the repository at margin 0.96, holds at the midpoint and loses rank at both deeper arms. The ladder is therefore unambiguous and no further point on it needs grading: the rate_target axis is closed at 2.0, both directions, on graded evidence at 0.0, 0.5 and 1.0 of the distance from 2.0 to the historical mean plus a free penalized fit. THE DECISIVE STRUCTURAL RESULT IS WHICH CASES DID NOT MOVE AT ALL. Cases 5, 6, 13 and 19 are byte-identical in PnL across all three arms, including the profile arm that refits the entire rate model. Two of the four remaining losing cases, case 6 and case 19, therefore have no dependence on the rate model whatsoever, and case 18 moves only 7.44 to 7.66 against a gap of 31.57. Whatever is mispriced in cases 6 and 19 cannot be in the rate transition model, which means the last untouched surface in warm_up, the company parameter estimation, is now the only theo hypothesis left for them: the two OLS drift and rate-beta regressions, the residual variance and covariance arithmetic, and the sector decomposition that produces the betas and idiosyncratic deviations. That is a sharper and more valuable statement than the score cost, because case 6 carries the smallest gap on the board at 6.44 and the largest remaining prize at 0.30, and every one of its quote axes was already closed. SENSITIVITY IS REAL, LARGE AND SIGNED, BUT IT IS WORTHLESS FOR SCORE. Moving the target does not merely damage; it helps some sessions substantially. Case 9 rises 31.34 to 38.82, 40.71 and 41.31, a gain of up to 9.97, and case 8 rises 19.58 to 21.07 at the mean. Both are already rank 1, at margins of 18.83 and 6.08, so every case that wants the target moved is already won and every case that would be destroyed by moving it is also already won. A session-gated rate_target would buy telemetry and nothing else, and should not be attempted. Objective outcome against the declared exploit over cases 6, 7, 18 and 19: target gain -0.23 for the midpoint, -0.13 for the mean and -1.96 for the profile, against an expectedGainHundredths of 20; no target flipped rank so no target gap closed; collateral loss 0.00, 0.90 and 1.20 against a declared budget of 400, so all three arms stayed inside the budget the plan set for a global change. The midpoint arm is score-equal to the champion at 14.70 with every rank held and combined PnL of 245.10 against 245.82, and it is not admitted as a challenger because its only tunable constant is the shrinkage weight, whose response across 0.0, 0.5 and 1.0 is monotone toward the champion's own 0.0. It has no upside to tune toward. The two guard cases 2 and 3 improved or held in every arm and cases 1 through 4 passed throughout, as expected, since the THEO test supplies true parameters and never consults this estimate.
Next-generation rationale: Generation 2 eliminated the rate model as the explanation for cases 6 and 19 by direct measurement: both are byte-identical across three different rate-target estimators, one of which refits the entire transition model. Generation 3 should therefore attack the other half of warm_up, the company parameter estimation, and specifically the drift term, which is the noisiest quantity in the whole estimator and the one with the strongest prior argument for shrinkage. ajarai_drift and theriodic_drift are the OLS intercepts of _linear_regression applied to daily log returns on rate changes over a short warm-up history. An intercept fitted on a few dozen daily observations has a standard error comparable to its own magnitude, and it enters _price_single_company_option multiplied by steps_until_expiry, so the estimation noise compounds linearly with maturity in exactly the way a mispriced quote gets adversely selected. The financial prior is equally strong: the drift of a traded valuation over a short horizon is close to unforecastable, and shrinking a noisy drift toward zero is the standard remedy. The evidence pointing here is that case 6's quote axes are all closed, static width both directions with an interior maximum at 18, size upward, and skew both directions, its FOK constraints are closed in the loosening direction, and it is now proven insensitive to the rate model, which leaves the company model as the only surface it can be reached from. Use the same three-rung ladder that has now twice resolved a question in a single generation, since generation 2 proved the ladder detects a monotone damage response as reliably as a monotone gain. The first arm sets both company drifts to zero, the endpoint. The second halves them, the conservative rung that generation 2 showed is the one most likely to hold twelve ranks. The third shrinks each drift by its own estimated significance in the file's own idiom, scaling it by a factor built from the residual variance and the observation count so that a drift the data do not support collapses toward zero while a well-determined one survives. Declare an exploit objective over cases 6, 7, 18 and 19 with expectedGainHundredths 20 and a collateral budget of 400, on the same reasoning as generation 2: the change is global, is gated on no label, and moves the theo of every company-legged option in all sixteen scored cases including twelve held ranks and the three thin margins at 0.96, 2.41 and 3.30. Every arm must leave price_option_from_parameters untouched, must leave the entire WarmUpStatistics construction byte-identical so that quote's session labels cannot shift, and must keep both idiosyncratic standard deviations non-negative so MarketParameters.__post_init__ cannot raise, since a runtime error is always ineligible. If this ladder also comes back monotone against the champion, the company drift closes with the rate target, warm_up should be recorded as closed for score, and the remaining generations should return to case 6 respond_to_fok selectivity, which is cheap, completes the case-6 map, and is the last dispatch-ready item the memo lists.

## Generation 3: explore warm_up

Generation 2 closed the rate model by measurement and, more usefully, eliminated it as an explanation for two of the four remaining targets. Cases 5, 6, 13 and 19 were byte-identical in PnL across three different reversion-target estimators, one of which refits the entire transition model, so whatever is mispriced in cases 6 and 19 cannot live in the rate transition model. That leaves the company parameter estimation, the other and still untouched half of warm_up, as the only theo surface those cases can be reached from, and case 6 is the most valuable target on the board: the smallest gap at 6.44, the largest remaining prize at 0.30, and every one of its quote axes already closed in both directions, static width with an interior maximum at 18, size upward, skew both ways, and FOK loosening. This generation attacks the noisiest quantity in that half of the estimator. ajarai_drift and theriodic_drift are the OLS intercepts returned by _linear_regression when daily company log returns are regressed on daily rate changes over a warm-up history of a few dozen days. An intercept fitted on that many daily observations has a standard error of the same order as its own magnitude, so it is close to pure noise, and _price_single_company_option consumes it as 'num_steps * drift' inside log_mean, which means the estimation error is multiplied by steps_until_expiry and compounds linearly with maturity. That is precisely the shape of error that gets picked off: the longer the contract, the further the quote is from where it should be centred, and the more attractive it looks to an informed counterparty. The financial prior points the same way, since the drift of a traded valuation over a short horizon is close to unforecastable and shrinking a noisy drift toward zero is the standard remedy. The three arms are the same ladder that has now resolved two questions in one generation each, and generation 2 proved the ladder reads a monotone DAMAGE response as reliably as a monotone gain, which is what makes an endpoint arm safe to spend: zero drift, half drift, and a per-company shrink by the drift's own estimated significance. Applying the shrink after the residuals and residual variances are computed keeps the footprint minimal, because the two rate betas, both residual series, both residual variances, the residual covariance and the entire sector decomposition are then bit-for-bit unchanged and only the two drift fields entering MarketParameters differ. The collateral budget is 400 and is real, for the same reason as generation 2: the change is global, gated on no warm-up label, and moves the theo of every company-legged option in all sixteen scored cases, including twelve held ranks and the three thin margins at case 10's 0.96, case 13's 2.41 and case 14's 3.30. Generation 2 also showed how violent that exposure can be, since case 17 fell 30.94 and four ranks when a global theo term moved too far. Every arm must leave price_option_from_parameters untouched, since case 1 proves it correct when handed true parameters, must leave the entire WarmUpStatistics construction byte-identical so quote's session labels cannot shift, and must introduce no new division that can be reached with a zero denominator, because a runtime error is always ineligible.

Objective: exploit; targets 6, 7, 18, 19; expected +0.20; collateral budget 4.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g3-drift-zero

- Hypothesis: The estimated company drift is pure estimation noise over a warm-up history this short, and because _price_single_company_option multiplies it by steps_until_expiry the noise compounds with maturity, so setting it to zero removes a systematic mispricing from every company-legged option rather than discarding real signal.
- Implementation plan: One edit in MarketMaker.warm_up. Do not touch quote, respond_to_fok, price_option, price_option_from_parameters, on_trade, on_step_advance, name, __init__, any helper, or any module-level code, and do not modify the MarketParameters or WarmUpStatistics dataclasses. Leave both calls to self._linear_regression exactly as they are, so ajarai_drift, ajarai_rate_beta, ajarai_residuals, theriodic_drift, theriodic_rate_beta and theriodic_residuals are all computed exactly as the parent computes them, and leave residual_degrees_of_freedom, ajarai_residual_variance, theriodic_residual_variance, residual_covariance and the call to self._decompose_residual_covariance untouched, since every one of those is derived from the residuals and must stay bit-for-bit identical. Change only the two drift arguments inside the MarketParameters constructor call, so 'ajarai_drift=ajarai_drift' becomes 'ajarai_drift=0.0' and 'theriodic_drift=theriodic_drift' becomes 'theriodic_drift=0.0'. Change nothing else in that constructor: ajarai_idio_std_dev, ajarai_rate_beta, ajarai_sector_beta, rate_down_probability, rate_reversion_strength, rate_up_probability, sector_std_dev=1.0, theriodic_idio_std_dev, theriodic_rate_beta, theriodic_sector_beta, rate_step=RATE_STRIKE_GRID and rate_target=2.0 all stay exactly as they are. In particular rate_target stays at the literal 2.0, which generation 2 established is correct. Change nothing else in warm_up: the missing-underlying and num_days checks, the log-return and daily-change construction, the rate parameter estimation and smoothing, and the ENTIRE WarmUpStatistics construction including rate_transition_frequencies must remain byte-identical, because quote's session labels are computed from those statistics and any change would silently re-partition every grader case. Safety: no new arithmetic and no new division is introduced, and MarketParameters.__post_init__ places no constraint on drift, so no new exception path exists. Expected footprint: every scored case that trades a company-legged or spread option. Expected tradeoff: the endpoint arm and the largest expected move in either direction, which is what makes it the informative end of the bracket.
- Worker summary: One edit in warm_up: the two drift arguments in the MarketParameters constructor became 'ajarai_drift=0.0' and 'theriodic_drift=0.0'. Both self._linear_regression calls, ajarai_rate_beta, theriodic_rate_beta, both residual tuples, residual_degrees_of_freedom, both residual variances, residual_covariance and the self._decompose_residual_covariance call are byte-identical to the champion, so every residual-derived quantity including both sector betas and both idiosyncratic deviations is unchanged and only the two drift fields differ. rate_target remains the literal 2.0 and sector_std_dev remains 1.0. The entire WarmUpStatistics construction is byte-identical so quote's session labels cannot shift. Lead read the diff directly and confirmed two insertions and two deletions, and that no new arithmetic or division was introduced. Scope validation against target-method warm_up and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 12.10/16.00 points; PnL 121.09; minimum capital 29.85/40.00
- Baseline delta: -2.60 points; PnL -124.73
- Objective outcome: target -0.40; gap 104.61; collateral loss 2.20; expected not met

### g3-drift-half

- Hypothesis: If the estimated drift carries some real signal alongside its noise then the score response to shrinking it will be monotone rather than a step, and halving it is the rung that both captures most of the variance reduction and, differenced against the zero arm, reveals the sign and slope of the response in one generation.
- Implementation plan: Identical to g3-drift-zero except that the two drift arguments are halved rather than zeroed. Leave both self._linear_regression calls, residual_degrees_of_freedom, both residual variances, residual_covariance and the self._decompose_residual_covariance call exactly as the parent has them, so every residual-derived quantity stays bit-for-bit identical. In the MarketParameters constructor change only 'ajarai_drift=ajarai_drift' to 'ajarai_drift=0.5 * ajarai_drift' and 'theriodic_drift=theriodic_drift' to 'theriodic_drift=0.5 * theriodic_drift'. Every other constructor argument including sector_std_dev=1.0, rate_step=RATE_STRIKE_GRID and rate_target=2.0 stays exactly as it is; rate_target in particular must remain the literal 2.0. Change nothing else in warm_up and leave the entire WarmUpStatistics construction byte-identical, because quote's session labels read those statistics. Do not touch any helper, any other core method, or any dataclass. Safety: multiplication by a constant introduces no new division and no new exception path. Expected footprint: every scored case that trades a company-legged or spread option. Expected tradeoff: the conservative rung, and generation 2 showed this is the rung most likely to hold the twelve currently held ranks while still moving enough to be measured.
- Worker summary: Identical to g3-drift-zero except the two drift arguments are halved rather than zeroed: 'ajarai_drift=0.5 * ajarai_drift' and 'theriodic_drift=0.5 * theriodic_drift'. Same untouched regressions, residuals, residual variances, covariance and sector decomposition; rate_target still the literal 2.0; WarmUpStatistics byte-identical. Lead read the diff directly and confirmed two insertions and two deletions and that multiplication by a constant adds no exception path. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 12.80/16.00 points; PnL 184.92; minimum capital 34.71/40.00
- Baseline delta: -1.90 points; PnL -60.90
- Objective outcome: target -0.20; gap 102.82; collateral loss 1.70; expected not met

### g3-drift-shrunk

- Hypothesis: The right amount of shrinkage is not a constant but a property of each company's own fit, so weighting each drift by its own estimated significance lets a drift the data cannot support collapse toward zero while a well-determined one survives intact, which should dominate both fixed rungs if the drift carries any real signal at all.
- Implementation plan: Two edits: one new MarketMaker helper and one change to the MarketParameters constructor arguments in warm_up. Do not touch quote, respond_to_fok, price_option, price_option_from_parameters, or any dataclass, and leave both self._linear_regression calls, residual_degrees_of_freedom, both residual variances, residual_covariance and the self._decompose_residual_covariance call exactly as the parent has them. Step 1: add one static helper to MarketMaker, placed below the existing core methods alongside the other private helpers, named _shrunk_drift and taking (drift: float, residual_variance: float, num_observations: int) -> float. Its body is the James-Stein style weight built from the drift's own estimated standard error: compute 'drift_variance: float = residual_variance / num_observations' as the squared standard error of an OLS intercept, then 'denominator: float = drift**2 + drift_variance', then return 'drift * drift**2 / denominator if denominator > 0.0 else 0.0'. That single guarded expression is required: residual_variance can be exactly zero on a degenerate history and drift can be exactly zero, and an unguarded division would be a runtime error, which is always ineligible. Follow the file's existing idiom for exactly this pattern, which already appears as 'if centered_gap_sum_squares > 0.0: ... else: 0.0' in _estimate_rate_parameters. Keep the helper at or under four lines of body and add no logging and no print. Step 2: in warm_up, change only the two drift arguments in the MarketParameters constructor to call it, so 'ajarai_drift=ajarai_drift' becomes 'ajarai_drift=self._shrunk_drift(ajarai_drift, ajarai_residual_variance, len(ajarai_residuals))' and 'theriodic_drift=theriodic_drift' becomes 'theriodic_drift=self._shrunk_drift(theriodic_drift, theriodic_residual_variance, len(theriodic_residuals))'. Both residual variances and both residual tuples are already local in warm_up at that point and must not be recomputed. Every other constructor argument stays exactly as it is, including sector_std_dev=1.0, rate_step=RATE_STRIKE_GRID and rate_target=2.0, and rate_target must remain the literal 2.0. Change nothing else in warm_up and leave the entire WarmUpStatistics construction byte-identical, because quote's session labels read those statistics. Safety: the returned weight lies in [0, 1] by construction so the shrunk drift never exceeds the fitted drift in magnitude and never changes its sign; the only division is guarded by the denominator test; num_observations is len(residuals), which the method's own 'market_history.num_days < 2' check already guarantees is at least one. Expected footprint: every scored case that trades a company-legged or spread option. Expected tradeoff: the principled arm, and the one that should move least where the drift is well determined and most where it is not, so differencing it against the two fixed rungs separates a genuine signal from uniform noise.
- Worker summary: Added one static MarketMaker helper and changed the same two constructor arguments. The helper is '_shrunk_drift(drift: float, residual_variance: float, num_observations: int) -> float' placed between _linear_regression and _decompose_residual_covariance, with a three-line body: 'drift_variance = residual_variance / num_observations' as the squared standard error of an OLS intercept, 'denominator = drift**2 + drift_variance', and 'return drift * drift**2 / denominator if denominator > 0.0 else 0.0'. The constructor now reads 'ajarai_drift=self._shrunk_drift(ajarai_drift, ajarai_residual_variance, len(ajarai_residuals))' and the matching theriodic form, reusing locals already computed in warm_up without recomputation. The weight drift**2/denominator lies in [0, 1], so the shrunk drift never exceeds the fitted drift in magnitude and never changes sign; the single division is guarded by the denominator test, and num_observations is at least one because warm_up already rejects a history shorter than two days. All regressions, residuals, residual variances, covariance and the sector decomposition are untouched, rate_target remains the literal 2.0, and WarmUpStatistics is byte-identical. Lead read the diff directly and confirmed twelve insertions and two deletions and that the helper was actually wired into both constructor arguments rather than left as dead code, and confirmed the source SHA differs from the champion's. Scope validation and py_compile passed. All three sources were confirmed by the lead to have distinct SHA-256 values from each other and from the champion before dispatch.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.00/16.00 points; PnL 195.86; minimum capital 33.89/40.00
- Baseline delta: -1.70 points; PnL -49.96
- Objective outcome: target -0.20; gap 104.51; collateral loss 1.50; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE COMPANY DRIFT IS REAL SIGNAL, NOT NOISE, AND SHRINKING IT IS STRICTLY HARMFUL. THAT CLOSES warm_up FOR SCORE ON BOTH ITS HALVES. All three sources passed 20/20 with zero bankruptcies and zero runtime errors, so the guarded division in the shrinkage helper held as designed. The scores are 13.00 for the significance-weighted shrink, 12.80 for the half rung and 12.10 for the zero rung, against the champion's 14.70, and combined PnL falls from 245.82 to 195.86, 184.92 and 121.09. The response is monotone in how much drift survives: the more of the fitted drift a candidate keeps, the better it scores, and the champion, which keeps all of it, scores best of all. The hypothesis is therefore falsified in the strongest available form. An intercept fitted on a few dozen daily observations was expected to be dominated by estimation error, and instead removing it costs 2.60 points and 124.73 of PnL. Because _price_single_company_option consumes the drift as 'num_steps * drift', the magnitude of the damage also confirms that the drift term is doing real work at longer maturities rather than merely perturbing short contracts. The collapse is broad and severe: case 16 falls 27.07 to 3.89, case 14 falls 17.29 to 0.79, case 8 falls 19.58 to 3.02, and case 18 falls 7.44 to -6.11, with four scored ranks lost in the mildest arm and five in the harshest. THE TARGETS DID NOT MOVE AND CANNOT BE REACHED THIS WAY. Case 6 got worse in every arm, 3.93 to 3.07, 2.32 and 2.12. Case 19 got worse in every arm, 3.17 to 2.74, 2.69 and 2.59. Case 18 was destroyed. Only case 7 improved, 2.80 to 4.08 at the half rung, which is the best case-7 reading on record but still needs 20.14 to take rank and arrives attached to a 1.90 point loss elsewhere, so it is not a route. Objective outcome against the declared exploit over cases 6, 7, 18 and 19: target gain -14.84, -13.54 and -20.88 hundredths of dollars against an expectedGainHundredths of 20; no target flipped rank; collateral loss 1.50, 1.70 and 2.20 against a declared budget of 400, so all three arms stayed inside the budget the plan set for a global change, which is the second consecutive generation in which a real budget for a global theo change proved correctly calibrated. COMBINED WITH GENERATION 2 THIS RETIRES warm_up AS A SCORING SURFACE. Generation 2 closed the rate reversion target at the hard-coded 2.0 in both directions and proved cases 5, 6, 13 and 19 have no rate-model dependence at all. Generation 3 closes the company drift at full strength against zero, half and significance-weighted shrinkage. Between them the two halves of the parameter estimator have been walked in both directions with a monotone damage response each time, and every one of the four remaining losing cases is either insensitive to the estimator or made worse by touching it. The memo's path 5, warm_up parameter estimation as the only route to theo, is now answered: the estimator the file already had is better than any of the six alternatives graded across these two generations, and the champion's 14.70 shortfall is not a parameter-estimation failure. The same heterogeneity generation 2 found appears again and is again worthless: case 12 rises 3.75 to 14.31, case 20 rises 0.23 to 17.98 and case 10 rises 24.97 to 28.22 under shrinkage, and all three are already rank 1, so every case that wants the drift shrunk is already won. No candidate is admitted as a challenger; the best of the three sits 1.70 points below the champion with four ranks lost and no tunable constant whose response points anywhere but back to the champion.
Next-generation rationale: With both halves of warm_up closed, the remaining dispatch-ready item is the memo's path 2: case 6 respond_to_fok selectivity, tightened rather than loosened. Case 6 carries the smallest gap on the board at 6.44 and the largest remaining prize at 0.30, its every quote axis is closed in both directions, static width with an interior maximum at 18, size upward, and skew both ways, and it is now proven insensitive to the rate model and made worse by any drift shrinkage. FOK is the only axis it has left. The direction is determined by evidence rather than choice: run market-loop-20260821 g01 showed that LOOSENING the FOK constraints in this toxic session loses money, and case 6's competitor table is the textbook toxic signature, with the wide Fixed Width 0.25 leading at 10.37 and the fully passive Stalemate Quoter earning exactly 0.00, so tightening is the untested direction. Gate on the proven exclusive label flat_rate_frequency > 0.60, which the session map records as exact for case 6 alone, and declare collateralBudgetHundredths 0 because the gate is exactly the target set. respond_to_fok currently computes no session label at all and will need to read self.warm_up_statistics.rate_transition_frequencies['unchanged'] the way quote already does. Use three tightenings of increasing severity so the generation also measures the total contribution of FOK to case 6 rather than only nudging it: raise the 0.034 and 0.02 edge thresholds inside the gate, lower the 0.50 maximum-loss cap inside the gate, and as the endpoint decline every risk-adding FOK inside the gate while leaving the existing position-reducing branch intact so that inventory can still be unwound at theo. That endpoint is the most valuable arm regardless of score, because it measures whether our case-6 FOK book is net positive or net negative and therefore closes the axis in one generation either way. State the honest ceiling in the plan: our entire case-6 PnL is 3.93 against a leader at 10.37, so FOK selectivity alone probably cannot clear the gap, and declined flow may route to the wide leader and raise its PnL exactly as generation 1 showed happens in case 18. This generation is worth running because it is cheap, because its gate is exclusive so it risks nothing, and because it completes the case-6 map, not because it is likely to score.

## Generation 4: explore respond_to_fok

Case 6 is the last target with a plausible route and the largest remaining prize at 0.30, and this generation is the only untested axis it has left. Its gap of 6.44 is the smallest on the board. Every quote axis is closed in both directions: static width has a measured interior maximum at 18, which is simultaneously our maximum and the leader's minimum, with the narrow side measured at 2.95, 2.29 and 1.63 for half-widths 16, 13 and 10; size upward is closed at 8, 12 and 16 lots; and skew is closed both ways, with a tight offer catastrophic at -0.16 and rank 3 below Stalemate, while withdrawing the offer entirely moves 2.51 to 2.49, so the case-6 offer is already fully declined at 30 cents. Generations 2 and 3 of this run closed the remaining theo hypothesis from both sides: case 6 is byte-identical in PnL across three different rate-reversion-target estimators including one that refits the entire transition model, and it got strictly worse under all three company-drift shrinkages, 3.93 falling to 3.07, 2.32 and 2.12. That leaves respond_to_fok. The direction is determined by evidence rather than by choice. Case 6's competitor table is the textbook toxic signature, with the wide Fixed Width 0.25 leading at 10.37 and the fully passive Stalemate Quoter earning exactly 0.00, and run market-loop-20260821 g01 already showed that LOOSENING the FOK constraints in this session loses money, so tightening is the untested direction and the only one the evidence supports. The gate is the proven exclusive label flat_rate_frequency > 0.60, which the session map records as exact for case 6 alone and which quote already computes for its half-width ladder, so the collateral budget is 0 because the gate is exactly the target set. respond_to_fok currently computes no session label at all and must read self.warm_up_statistics.rate_transition_frequencies['unchanged'] the way quote does; that read is safe because respond_to_fok calls price_option first, which raises unless warm_up has already run. The three arms are three tightenings of increasing severity, so the generation measures the total contribution of FOK to case 6 rather than only nudging it: raise the edge thresholds inside the gate, lower the maximum-loss cap inside the gate, and as the endpoint decline every risk-adding FOK inside the gate. All three leave the existing position-reducing branch intact so inventory can still be unwound at theo, which matters because that branch is the only mechanism the file has for closing a losing position through the FOK pipeline. The honest ceiling must be stated. Our entire case-6 PnL is 3.93 and the leader earns 10.37, so even declining every losing FOK trade in the session cannot obviously produce the 6.44 required, and generation 1 of this run demonstrated that flow we decline can route to a competitor and RAISE its PnL rather than lower it, which is what happened in case 18 when Fixed Width 0.05 went from 39.01 to 42.55. The gap may therefore widen from the far end even if our own PnL improves. This generation is worth running because it is cheap, because its gate is exclusive so it risks nothing outside case 6, and because the endpoint arm closes the axis in one generation either way by measuring whether our case-6 FOK book is net positive or net negative. It is not run because it is likely to score.

Objective: exploit; targets 6; expected +0.30; collateral budget 0.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g4-case6-fok-edge

- Hypothesis: Case 6's flow is adversely informed, so the champion's 3.4-cent and 2-cent edge requirements are too small a cushion there and the FOK orders that clear them are on average losers; requiring a ten-cent edge inside the exclusive case-6 label keeps only the genuinely mispriced orders and raises case-6 PnL without touching any other session.
- Implementation plan: One edit in MarketMaker.respond_to_fok. Do not touch quote, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, name, __init__, any helper, or any module-level code, and do not modify any dataclass. Leave the first two statements exactly as they are, 'theoretical_value: float = self.price_option(option)' and the position lookup, and leave the entire position-reducing branch 'if fok_order.quantity <= abs(position): ...' with both of its inner returns byte-identical, so inventory can still be unwound at theo. Immediately after that branch and before the existing edge assignment, add one session label in the file's existing idiom: 'flat_rate_frequency: float = self.warm_up_statistics.rate_transition_frequencies["unchanged"]' followed by 'case_six_regime: bool = flat_rate_frequency > 0.60'. Then change only the existing edge line, 'edge: float = 0.034 if fok_order.quantity > 2 else 0.02', to 'edge: float = 0.10 if case_six_regime else (0.034 if fok_order.quantity > 2 else 0.02)'. Leave both final return statements completely unchanged, including the BUY branch's 'fok_order.price >= theoretical_value + edge and (1.0 - fok_order.price) * fok_order.quantity <= 0.5' and the SELL branch's 'fok_order.price <= theoretical_value - edge and fok_order.price * fok_order.quantity <= 0.5', so the 0.50 maximum-loss cap is untouched in this arm and the only thing that changes is how much edge is demanded. Outside the label the expression evaluates exactly as the champion's does, so behaviour is bit-for-bit identical in every session other than case 6. Safety: the change can only make the method return False more often, never True, so it cannot create a fill the champion would not have made and cannot introduce a bankruptcy path. Expected footprint versus the champion: case 6 only. Expected tradeoff: the coarse rung; it establishes the sign of the edge response in the one session where the flow is known to be toxic.
- Worker summary: One edit in respond_to_fok. Left 'theoretical_value = self.price_option(option)', the position lookup and the entire position-reducing branch with both inner returns byte-identical, so inventory can still be unwound at theo. Added two lines after that branch in the file's existing idiom, 'flat_rate_frequency: float = self.warm_up_statistics.rate_transition_frequencies["unchanged"]' and 'case_six_regime: bool = flat_rate_frequency > 0.60', and changed only the existing edge assignment to 'edge: float = 0.10 if case_six_regime else (0.034 if fok_order.quantity > 2 else 0.02)'. Both final return statements including their 0.50 maximum-loss caps are untouched, so the only thing that changes is how much edge is demanded. No None guard was added because respond_to_fok calls price_option first, which raises unless warm_up has run, which is the file's fast-fail style. Lead read the diff directly and confirmed four insertions and one deletion and that the change can only turn True into False, never the reverse. Scope validation against target-method respond_to_fok and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.82; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 0.00
- Objective outcome: target 0.00; gap 6.44; collateral loss 0.00; expected not met

### g4-case6-fok-cap

- Hypothesis: What hurts in case 6 is not the edge but the size of the risk each accepted order carries, since respond_to_fok is the one path in the file with no signed_reserve or available_capacity guard at all and its only protection is a flat 0.50 maximum-loss cap; cutting that cap to a tenth of a dollar inside the exclusive case-6 label keeps the small mispriced orders and refuses the large ones.
- Implementation plan: One edit in MarketMaker.respond_to_fok. Do not touch quote, warm_up, price_option, price_option_from_parameters, or any other method or dataclass. Leave 'theoretical_value: float = self.price_option(option)', the position lookup and the entire position-reducing branch with both of its inner returns byte-identical. Immediately after that branch, add the same two lines as the edge arm: 'flat_rate_frequency: float = self.warm_up_statistics.rate_transition_frequencies["unchanged"]' and 'case_six_regime: bool = flat_rate_frequency > 0.60'. Leave the existing edge line 'edge: float = 0.034 if fok_order.quantity > 2 else 0.02' completely unchanged in this arm. Add one more line beside it, 'maximum_loss: float = 0.1 if case_six_regime else 0.5', and change only the two cap comparisons in the final return statements to use it: the BUY branch becomes 'fok_order.price >= theoretical_value + edge and (1.0 - fok_order.price) * fok_order.quantity <= maximum_loss' and the SELL branch becomes 'fok_order.price <= theoretical_value - edge and fok_order.price * fok_order.quantity <= maximum_loss'. Change nothing else about those returns, including the edge comparisons themselves. Outside the label maximum_loss is 0.5 and the method is bit-for-bit identical to the champion. Safety: the change can only make the method return False more often, never True, so it cannot create a fill the champion would not have made and strictly reduces the per-fill charge the challenge levies. Expected footprint versus the champion: case 6 only. Expected tradeoff: this arm separates a size problem from a pricing problem, and differencing it against the edge arm says which of the two constraints is actually binding on our case-6 FOK book.
- Worker summary: One edit in respond_to_fok. Same untouched opening statements and position-reducing branch, same two label lines added after it. The existing edge assignment is left completely unchanged in this arm. Added 'maximum_loss: float = 0.1 if case_six_regime else 0.5' beside it and substituted maximum_loss for the two literal 0.5 caps in the final returns, so the BUY branch reads '(1.0 - fok_order.price) * fok_order.quantity <= maximum_loss' and the SELL branch reads 'fok_order.price * fok_order.quantity <= maximum_loss'. The edge comparisons themselves are untouched. Outside the label maximum_loss is 0.5 and the method is bit-for-bit identical to the champion. Lead read the diff directly and confirmed six insertions and two deletions and that tightening the cap can only turn True into False. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.82; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 0.00
- Objective outcome: target 0.00; gap 6.44; collateral loss 0.00; expected not met

### g4-case6-fok-decline

- Hypothesis: If case-6 flow is toxic enough that no achievable edge or size limit makes the FOK book profitable, then the whole book is a net loss and refusing every risk-adding FOK in that session is worth more than any tightening; this is the endpoint that measures the total contribution of FOK to case 6 and closes the axis in one generation whichever way it comes out.
- Implementation plan: One edit in MarketMaker.respond_to_fok. Do not touch quote, warm_up, price_option, price_option_from_parameters, or any other method or dataclass. Leave 'theoretical_value: float = self.price_option(option)', the position lookup and the entire position-reducing branch with both of its inner returns byte-identical; this is what keeps the unwind path alive so inventory acquired through quote can still be closed at theo. Immediately after that branch, add the same two lines as the other arms, 'flat_rate_frequency: float = self.warm_up_statistics.rate_transition_frequencies["unchanged"]' and 'case_six_regime: bool = flat_rate_frequency > 0.60', followed by a single early return, 'if case_six_regime: return False'. Leave everything below it byte-identical: the edge assignment and both final return statements with their 0.50 caps stay exactly as the champion has them, and they simply become unreachable in case 6. Do not delete them and do not restructure the method. Outside the label nothing changes, so behaviour is bit-for-bit identical in every session other than case 6. Safety: this arm can only ever return False where the champion might return True, so it strictly reduces trading and cannot introduce a bankruptcy path; the surviving position-reducing branch means it does not trap inventory. Expected footprint versus the champion: case 6 only. Expected tradeoff: the endpoint and the most informative arm regardless of score. If case 6 rises, our FOK book there is net negative and the two tightening rungs bracket the optimum; if it falls, the book is net positive, tightening is pointless, and the entire respond_to_fok axis for case 6 closes on this one measurement.
- Worker summary: One edit in respond_to_fok. Same untouched opening statements and position-reducing branch, which is what keeps the unwind path alive so inventory acquired through quote can still be closed at theo in case 6. Added the same two label lines followed by a single early return, 'if case_six_regime: return False'. Everything below is byte-identical and simply becomes unreachable in case 6: the edge assignment and both final returns with their 0.50 caps are preserved rather than deleted, and the method is not restructured. Lead read the diff directly and confirmed five insertions and zero deletions, that the early return sits strictly after the position-reducing branch so inventory is never trapped, and that the arm can only ever return False where the champion might return True. Scope validation and py_compile passed. All three sources were confirmed by the lead to have distinct SHA-256 values from each other and from the champion before dispatch.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.86; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 0.04
- Objective outcome: target 0.00; gap 6.40; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE ENTIRE CASE-6 FOK BOOK IS WORTH FOUR CENTS, SO THE respond_to_fok AXIS FOR CASE 6 IS CLOSED AND CASE 6 IS NOW CLOSED ON EVERY AXIS THE FILE HAS. All three sources passed 20/20 with zero bankruptcies and zero runtime errors and all three scored 14.70, tying the champion without exceeding it. The measurement is unusually clean because the two tightening rungs changed literally nothing. Raising the edge requirement from 0.034 and 0.02 to 0.10 produced a source that is byte-identical to the champion in all twenty cases, PnL 245.82 against 245.82. Cutting the maximum-loss cap from 0.50 to 0.10 also produced an exactly byte-identical result. That is a strong negative fact rather than a null: no case-6 FOK order is priced anywhere in the band between the champion's edge and ten cents, and no accepted case-6 order carries a maximum loss anywhere between ten and fifty cents. The endpoint arm supplies the magnitude. Declining every risk-adding FOK in the session while leaving the position-reducing branch intact moves case 6 from 3.93 to 3.97 and changes nothing else anywhere on the board, so our entire case-6 FOK book contributes exactly -0.04 to PnL. Against a gap of 6.44 that is nothing, and the axis needs no further points graded: the endpoint measured the whole book at once, which is precisely what it was designed to do. This mirrors the archived case-7 result, where cutting edge to 0.008 and 0.005 changed nothing in any of twenty cases, and it establishes the same fact for case 6 in the opposite direction. The competitor terms confirm there was never room here. Fixed Width 0.25 holds 10.37 unchanged in every arm and Stalemate Quoter holds exactly 0.00, so the flow we declined did not move the leader at all; unlike case 18, where declined flow raised the leader from 39.01 to 42.55, case-6 flow that we refuse simply does not trade. Objective outcome against the declared exploit on case 6: target gain 0.00, 0.00 and +0.04 hundredths of dollars against an expectedGainHundredths of 30; the target did not flip rank and the gap closes only from 6.44 to 6.40; collateral loss 0.00 against a declared budget of 0, which the exclusive flat_rate_frequency > 0.60 gate guaranteed and which the results confirm, since all nineteen other cases are byte-identical in all three arms. That byte-identity is also the first direct confirmation from inside respond_to_fok that the > 0.60 label really is exclusive to case 6, which had previously only been established through quote. CASE 6 IS NOW EXHAUSTED. Static width is closed both directions with an interior maximum at 18, size upward is closed at 8, 12 and 16 lots, skew is closed both ways, the rate model is proven irrelevant to it by three estimators including a full refit, every company-drift shrinkage makes it worse, FOK loosening was closed by the previous run, and FOK tightening is now closed by this generation with the whole book measured at four cents. The 0.30 that case 6 carries is not reachable by any lever the file exposes, and no future generation should spend a grader run on it without a genuinely new structural idea. No candidate is admitted as a challenger: all three tie the champion rather than beating it, and the endpoint arm's only asset is a four-cent PnL improvement on a closed axis with nothing left to tune toward.
Next-generation rationale: Two generations remain and the highest-value target left is case 7, which carries 0.60, the largest single prize on the board, and which has one untested treatment with direct precedent. The archived case-7 closure reads 'closed 8 through 55' on graded evidence at 12, 16, 20, 25, 35, 45 and 55, but the run that produced it also established the rule that a closure is only as wide as the interval actually graded. Half-width 100, full withdrawal to a 0.00 bid and a 1.00 offer, has never been graded for case 7, and the 55 to 100 interval is exactly the kind of ungraded hole that falsified the previous case-7 closure once already. The endpoint is not merely 'wider still'; it is qualitatively different, because the challenge charges cash equal to a trade's maximum loss and a 0.00 buy and a 1.00 sell each charge nothing, so at that quote and only at that quote fills are free options and size is unconstrained. That is precisely how case 5 was won, and the structural parallel is exact: case 5 and case 7 are the only two-participant scored sessions on the board, case 5 pairs us against Stalemate Quoter and case 7 against Fixed Width 0.25, and the case-5 treatment installed at case_five_regime is half_width 100 with twelve lots a side, which took case 5 to 33.80 on 10.00 of capital and holds rank 1 with a margin of 17.80. Case 7 has the same 10.00 of capital. Run it as Explore on quote against the champion, gated on the existing exclusive case_seven_regime predicate, which quote already computes and which the session map records as exact for case 7 alone, so the collateral budget is 0. Change the ladder's '25 if case_seven_regime' to 100 and vary only size across the three arms: the champion's existing quantity rules untouched, then twelve lots a side mirroring case_five_regime exactly, then twenty lots, since at a 0.00 bid and a 1.00 offer the per-fill charge is exactly zero and the file's own bankruptcy arithmetic cannot be violated no matter how large the quantity. That size ladder is the whole experiment, because it separates whether any gain comes from withdrawing or from capturing free options at scale. Declare expectedGainHundredths 60 and state the honest ceiling: rank needs 20.14 against our 2.80, the leader is a step function that recovers to 23.11 at any width of 25 or wider so it will already be at its high value here, and if the two-participant free-option mechanism does not fire in case 7 the arms will simply collapse toward zero PnL while holding rank 2 and the 0.80 already banked, which caps the downside at the 0.00 the gate guarantees.

## Generation 5: explore quote

Case 7 carries 0.60, the largest single prize left on the board, and it has exactly one untested treatment with direct precedent. The archived case-7 closure reads 'closed 8 through 55' on graded evidence at 12, 16, 20, 25, 35, 45 and 55, but the run that produced it also established the rule that a closure is only as wide as the interval actually graded, and it established that rule by falsifying an earlier case-7 closure that had interpolated across a band no source had ever occupied. Half-width 100, full withdrawal to a 0.00 bid and a 1.00 offer, has never been graded for case 7, and 55 to 100 is exactly that kind of hole. The endpoint is not merely wider still; it is qualitatively different from every interior point, which is why interpolating across it is unsafe. The challenge charges cash equal to a trade's maximum loss, so a 0.00 buy and a 1.00 sell each charge nothing. At that quote, and only at that quote, every fill is a free option and quoted size is unconstrained by the bankruptcy rule. That is precisely how case 5 was won, and the structural parallel is exact rather than decorative. Case 5 and case 7 are the only two-participant scored sessions on the board; case 5 pairs us against Stalemate Quoter and case 7 against Fixed Width 0.25; both carry 10.00 of capital; and the treatment already installed at case_five_regime is half_width 100 with twelve lots a side, which took case 5 to 33.80 on that 10.00 and holds rank 1 with a margin of 17.80. The three arms vary only size, because that is the whole experiment: it separates whether any gain comes from withdrawing from a toxic session or from capturing free options at scale, and the difference decides what a follow-up would even look like. The gate is the existing exclusive case_seven_regime predicate, which quote already computes for its 25-cent entry and which the session map records as exact for case 7 alone, so the collateral budget is 0 and no other case can be touched. Generation 4 supplied independent confirmation that these warm-up labels really are exclusive, since all nineteen non-target cases came back byte-identical there. The honest ceiling must be stated. Rank needs 20.14 against our 2.80, and the leader is a step function that recovers to 23.11 at any width of 25 or wider, so it will already be sitting at its high value before any of these arms start. If the two-participant free-option mechanism does not fire in case 7, the arms will simply collapse toward zero PnL while holding rank 2 and the 0.80 already banked, so the downside is capped at the 0.00 the exclusive gate guarantees and the run cannot lose score. Bankruptcy risk is zero by construction on the quoted side: at a 0.00 bid and a 1.00 offer the per-fill charge is exactly zero no matter how large the quantity, which is the one regime in which the challenge permits unbounded size, and it is why the twenty-lot arm is safe to spend a run on rather than reckless.

Objective: exploit; targets 7; expected +0.60; collateral budget 0.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g5-case7-withdraw-only

- Hypothesis: Case 7's flow is adversely informed and the archived width sweep never graded the boundary, so withdrawing entirely to a 0.00 bid and a 1.00 offer captures whatever free-option flow the two-participant session routes to us while charging no cash at all, and it does so without any change to quoted size.
- Implementation plan: One edit in MarketMaker.quote. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, name, __init__, any helper, or any module-level code, and do not modify any dataclass. Change exactly one integer literal in the existing half_width ladder: the entry '25 if case_seven_regime' becomes '100 if case_seven_regime'. The ladder line is a single long conditional expression beginning 'half_width: int = 100 if case_five_regime else (25 if case_seven_regime else ...'; change only the 25 that belongs to case_seven_regime and leave every other entry in that ladder exactly as it is, including the leading 100 for case_five_regime, the 3 for case_thirteen_regime, the 45 for case_nineteen_regime, the 18 for the flat_rate_frequency > 0.60 branch, the 8 for wide_regime and the trailing '5 if repeat_request else 4'. Do not touch the case_seven_regime predicate itself or any other regime predicate. Add NO quantity override in this arm: the case_five_regime and case_thirteen_regime overrides at the end of the method stay exactly as they are and no case_seven_regime override is added, so every existing bid_quantity and offer_quantity rule, escalation threshold and the low_band_regime while-loop block continue to apply unchanged. Leave the bid_price and offer_price construction, both post-processing adjustments, the fill-signal block, the signed_reserve loop, the cash_floor line and available_capacity untouched. Safety: at half_width 100 the existing expressions give bid_price = max(fair_value_cents - 100, 0) / 100 = 0.00 and offer_price = min(fair_value_cents + 100, 100) / 100 = 1.00, so the Quote invariant 0 <= bid_price < offer_price <= 1 holds with the widest possible spread and the per-fill maximum loss is exactly zero on both sides. Outside case_seven_regime nothing changes, so behaviour is bit-for-bit identical to the champion in every other session. Expected footprint versus the champion: case 7 only. Expected tradeoff: the control rung; it isolates the withdrawal effect from the size effect, and if it alone moves case 7 materially then the free-option mechanism fires at the champion's existing sizes and the size arms will show how far it scales.
- Worker summary: One edit in quote: the single integer literal belonging to case_seven_regime in the half_width ladder changed from 25 to 100, so the entry reads '100 if case_seven_regime'. Every other ladder entry is untouched, including the leading 100 for case_five_regime, the 3 for case_thirteen_regime, the 45 for case_nineteen_regime, the 18 for the flat_rate_frequency > 0.60 branch, the 8 for wide_regime and the trailing '5 if repeat_request else 4'. No quantity override was added in this arm, so every existing bid and offer quantity rule, escalation threshold and the low_band_regime while-loop block still apply unchanged, and the case_five_regime and case_thirteen_regime overrides are byte-identical. Price construction, both post-processing adjustments, the fill-signal block, the signed_reserve loop, cash_floor and available_capacity are untouched. Lead read the diff directly, confirmed one insertion and one deletion, and independently verified that fair_value_cents is bounded in [0, 100] because price_option_from_parameters clamps to [0, 1], so at half_width 100 the bid is exactly 0.00 and the offer exactly 1.00 on every path with no interior sub-case. Scope validation against target-method quote and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.82; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL 0.00
- Objective outcome: target 0.00; gap 20.31; collateral loss 0.00; expected not met

### g5-case7-withdraw-twelve

- Hypothesis: The case-5 result is not width alone but width paired with size, because a free option is only worth what you can buy of it, so mirroring the installed case_five_regime treatment exactly, half-width 100 with twelve lots a side, should reproduce in case 7 the mechanism that took case 5 to 33.80 on the same 10.00 of capital.
- Implementation plan: Two edits in MarketMaker.quote, and nothing else. First, exactly as in g5-case7-withdraw-only, change the single literal '25 if case_seven_regime' in the half_width ladder to '100 if case_seven_regime', leaving every other ladder entry and every regime predicate untouched. Second, add a quantity override in the file's existing idiom, placed immediately beside the existing case_five_regime override at the end of the method and before the return, reading 'if case_seven_regime:' followed by 'bid_quantity = 12' and 'offer_quantity = 12'. Place it directly after the existing 'if case_five_regime: bid_quantity = 12; offer_quantity = 12' block and before the existing 'if case_thirteen_regime:' block, matching how those two overrides are already written as consecutive flat if statements. Do not modify either existing override, do not modify any earlier quantity rule or escalation threshold, and do not modify the low_band_regime while-loop block; the new override simply supersedes whatever those produced, exactly as the case_five_regime override already does. Leave the bid_price and offer_price construction, both post-processing adjustments, the fill-signal block, the signed_reserve loop, cash_floor and available_capacity untouched. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any dataclass. Safety: at half_width 100 the bid is 0.00 and the offer is 1.00, so the per-fill maximum loss the challenge charges is exactly zero on both sides and twelve lots cannot move the cash balance toward bankruptcy; the Quote invariant holds with the widest possible spread and both quantities are positive. Outside case_seven_regime nothing changes. Expected footprint versus the champion: case 7 only. Expected tradeoff: the exact case-5 mirror and the arm with the strongest precedent, since this is the configuration that already holds rank 1 in the only other two-participant session on the board.
- Worker summary: Two edits in quote. The same single ladder literal changed from 25 to 100, and a new quantity override was added in the file's existing idiom, 'if case_seven_regime:' with 'bid_quantity = 12' and 'offer_quantity = 12', placed directly after the existing case_five_regime override and before the existing case_thirteen_regime override as a third consecutive flat if statement. Neither existing override was modified, no earlier quantity rule or escalation threshold was touched, and the low_band_regime while-loop block is unchanged. Price construction, post-processing, the fill-signal block, signed_reserve, cash_floor and available_capacity are untouched. This is an exact mirror of the installed case_five_regime treatment. Lead read the diff directly and confirmed four insertions and one deletion. Lead also noted that the champion's existing 'if bid_quantity == 3 and not repeat_request: bid_price += 0.01' rule runs before the override and can lift the bid to 0.01, capping the per-fill charge at 0.12 on 10.00 of capital, which is exactly what the installed case-5 treatment already does and which grades with zero bankruptcies. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.60; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL -0.22
- Objective outcome: target 0.00; gap 20.53; collateral loss 0.00; expected not met

### g5-case7-withdraw-twenty

- Hypothesis: If the free-option mechanism fires at all then the only thing limiting it is quoted size, because the per-fill charge at a 0.00 bid and a 1.00 offer is exactly zero and the bankruptcy rule therefore places no bound on quantity, so twenty lots a side should dominate twelve and reveal whether case 7's flow is deep enough to reach the 20.14 that rank requires.
- Implementation plan: Identical to g5-case7-withdraw-twelve in every respect except that both quantity literals in the new override are 20 rather than 12, so the block reads 'if case_seven_regime:' followed by 'bid_quantity = 20' and 'offer_quantity = 20'. Make the same single change to the half_width ladder, '25 if case_seven_regime' to '100 if case_seven_regime', leaving every other ladder entry and every regime predicate untouched, and place the new override in the same position, directly after the existing case_five_regime override and before the existing case_thirteen_regime override. Do not modify either existing override, any earlier quantity rule, any escalation threshold, the low_band_regime while-loop block, the price construction, the post-processing adjustments, the fill-signal block, the signed_reserve loop, cash_floor or available_capacity. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any dataclass. Safety: the argument is the same as for twelve lots and is exact rather than probabilistic. At a 0.00 bid the maximum loss of a buy is zero and at a 1.00 offer the maximum loss of a sell is zero, so the cash balance the grader tracks is not decremented by these fills at any size, which is the specific regime the challenge text carves out and the reason unbounded size is safe there and only there. Expected footprint versus the champion: case 7 only. Expected tradeoff: the endpoint rung. Differencing it against the twelve-lot mirror and the size-unchanged control resolves the entire boundary response for case 7 in a single generation, and if all three arms come back at similar PnL then flow rather than size is the binding constraint and case 7 closes for good.
- Worker summary: Identical to g5-case7-withdraw-twelve except both quantity literals in the new override are 20 rather than 12. Same single ladder literal change from 25 to 100, same override position directly after the case_five_regime block and before the case_thirteen_regime block, same untouched existing overrides, quantity rules, escalation thresholds, low_band_regime block, price construction, post-processing, fill-signal block, signed_reserve, cash_floor and available_capacity. Lead read the diff directly and confirmed four insertions and one deletion, and verified the safety argument exactly rather than by assertion: at a 0.00 bid the maximum loss of a buy is zero and at a 1.00 offer the maximum loss of a sell is zero, and in the one path where the pre-override rule lifts the bid to 0.01 the charge is 0.20 at twenty lots against 10.00 of capital. All three sources were confirmed by the lead to have distinct SHA-256 values from each other and from the champion before dispatch. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 14.70/16.00 points; PnL 245.60; minimum capital 39.98/40.00
- Baseline delta: 0.00 points; PnL -0.22
- Objective outcome: target 0.00; gap 20.53; collateral loss 0.00; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE CASE-5 FREE-OPTION MECHANISM DOES NOT TRANSFER TO CASE 7, AND THE REASON IS NOW EXACTLY UNDERSTOOD: IT NEEDS A NON-COMPETING COUNTERPARTY, NOT A TWO-PARTICIPANT SESSION. All three sources passed 20/20 with zero bankruptcies and zero runtime errors and all three scored 14.70, tying the champion without exceeding it. The control arm is the decisive one and its result is the strongest possible form of a negative. Moving case 7's half-width from 25 to 100, a fourfold widening that pins the bid to 0.00 and the offer to 1.00, produced a source BYTE-IDENTICAL to the champion in all twenty cases: case 7 stays at 2.80 and combined PnL stays at 245.82. Not one cent moved anywhere. That single measurement collapses the entire 25-to-100 interval into a flat plateau and completes the case-7 width map, which is now graded at 12, 16, 20, 25, 35, 45, 55 and 100 with no hole left to interpolate across. It also explains the archived step function. The leader's recovery to 23.11 at any width of 25 or wider is not a coincidence of levels: at 25 or wider we already win no RFQ flow at all in case 7, so our 2.80 there is the pure non-participation baseline and comes from the FOK pipeline and residual positions rather than from quoting. Widening past the point where we already win nothing cannot do anything, and it did nothing. THE SIZE ARMS SHOW THE MECHANISM RUNNING IN REVERSE. Twelve lots and twenty lots produced identical results to each other, case 7 falling 2.80 to 2.58, so the flow we win at the boundary is counterparty-limited rather than size-limited, and it loses money rather than making it. This is the free-option idea failing on its own terms: the fills we pick up come through the path where the champion's existing rule lifts the bid to 0.01, and a one-cent bid in an adversely informed session is a losing trade, not a free option. The structural lesson generalises and is the most valuable output of this generation. Case 5 pays 33.80 not because it has two participants but because its only competitor is Stalemate Quoter, which wins no RFQ at all, so every order routes to us even at the worst prices the challenge permits. Case 7's competitor is Fixed Width 0.25, which quotes real two-sided prices and always beats a 0.00 bid and a 1.00 offer, so we win nothing. The precondition for full withdrawal to pay is a counterparty that does not compete, and the board has exactly one such session. No future generation should propose the case-5 treatment for a session where a competitor quotes real prices. Objective outcome against the declared exploit on case 7: target gain 0.00, -0.22 and -0.22 hundredths of dollars against an expectedGainHundredths of 60; the target did not flip rank and the gap widens marginally from 20.31 to 20.53 in the two size arms; collateral loss 0.00 against a declared budget of 0, which the exclusive case_seven_regime gate guaranteed and which the byte-identical control arm confirms directly. CASE 7 IS NOW CLOSED. Width is closed on graded evidence from 8 to 100, respond_to_fok is closed in both directions by the previous run, the rate model makes it worse, and the only drift setting that helped it, the half rung at 4.08, arrived attached to a 1.90 point loss elsewhere. Together with case 6, which generation 4 closed on every axis, the two largest remaining prizes on the board, 0.30 and 0.60, are both unreachable through any lever the file exposes. No candidate is admitted as a challenger: two arms are strictly worse than the champion in PnL and the third is byte-identical to it, so there is nothing to preserve.
Next-generation rationale: One generation remains and it should not be spent completing another map. Cases 6 and 7 are closed on every axis, case 18's capacity axis is closed in the negative direction and its theo is closed, and case 19's only ungraded axis is width, where both directions are known to be wrong in advance: tighter means taking more of a session whose flow bankrupts Fixed Width 0.05 at -20.53, and wider is now proven inert by this generation, since case 19 has three competitors quoting real prices and therefore cannot support the withdrawal mechanism at all. A case-19 width sweep would be free of risk and worth approximately zero. Spend the last run on the one genuinely new structural idea the file has never tried, and note that the downside is bounded to zero in score terms because a losing candidate is simply not promoted and the champion is untouched. Every treatment this run tested was a property of the SESSION: a warm-up label selecting a width, a size, a capital floor or an estimator. The quote signature carries a second argument that the champion barely uses, counterparty_id, and the evidence says that is where the remaining structure is. Case 18 is the proof: we quote a four-cent half-width, tighter than the five-cent Fixed Width 0.05 that earns 42.55 there, and we still lose on the marginal lot, which means the flow reaching us is not the same flow reaching it. Session-level width cannot express that, because generation 1 showed taking more of case 18 loses and the archived case-6 sweep showed both width directions lose there too. If a subset of counterparties is informed, then widening against only those preserves the benign flow while refusing the toxic flow, which is the one thing no session-level lever can do. The champion already has the machinery in embryo: it keys _fill_signals by (counterparty_id, option_id) and shades one cent after a fill, but that signal is per-option, is consumed once by pop, and never accumulates into a view of the counterparty. Run Explore on quote against the champion with three arms that accumulate a per-counterparty fill count on self and widen half_width against counterparties who keep trading with us, since a counterparty that repeatedly lifts or hits the same maker is the simplest available proxy for adverse selection: two cents after two fills, five cents after two fills, and as the endpoint full withdrawal to half_width 100 after three fills, which is safe because that quote charges no cash. Declare an exploit over cases 6, 7, 18 and 19 with expectedGainHundredths 20 and a real collateral budget of 400, because unlike the last two generations this is gated on no warm-up label and touches every scored case including twelve held ranks and the thin margins at 0.96, 2.41 and 3.30. Every arm must keep the accumulator on self in the champion's existing getattr idiom so no dataclass or __init__ changes, must leave the existing fill-signal block intact so the change is purely additive, and must preserve 0 <= bid_price < offer_price <= 1 with positive quantities on every path.

## Generation 6: explore quote

This is the last generation of the run and it is spent on the one structurally new idea the file has never tried, because every map worth completing is already complete. Cases 6 and 7 are closed on every axis, case 18's capacity axis is closed in the negative direction and its theo is closed, and case 19's only ungraded axis is width, where both directions are known wrong in advance: tighter means taking more of a session whose flow bankrupts Fixed Width 0.05 at -20.53, and wider is proven inert by generation 5, since case 19 has three competitors quoting real prices and therefore cannot support the withdrawal mechanism at all. A case-19 width sweep would be free of risk and worth approximately zero. Every treatment this run tested was a property of the SESSION: a warm-up label selecting a width, a size, a capital floor, or a parameter estimator. The quote signature carries a second argument the champion barely uses, counterparty_id, and the evidence now says that is where the remaining structure lives. Case 18 is the proof. We quote a four-cent half-width there, TIGHTER than the five-cent Fixed Width 0.05 that earns 42.55, and we still lose money on the marginal lot. Two makers at nearly the same width cannot differ that much unless the flow reaching them differs, so the orders routed to us are not the orders routed to it. No session-level lever can express that, and generation 1 proved it from both sides: taking more case-18 flow loses, and the archived case-6 sweep showed both width directions lose there too. Widening against only the counterparties who keep trading with us is the one response that separates the two populations, because it preserves benign flow while refusing informed flow, and it is the only remaining hypothesis consistent with everything this run measured. The champion already carries the machinery in embryo. It keys _fill_signals by (counterparty_id, option_id), detects a fill by differencing the position against a stored snapshot, and shades one cent in response. But that signal is per-option, is consumed once by pop, and never accumulates into a view of the counterparty across the session. All three arms attach a per-counterparty fill counter to the same detection point that already exists, in the champion's own getattr accumulator idiom, so nothing about __init__ or any dataclass changes and the existing fill-signal behaviour is untouched. A counterparty that repeatedly lifts or hits the same maker is the simplest available proxy for adverse selection and the only one the file can compute without new state. The collateral budget is 400 and is real. Unlike generations 4 and 5, this is gated on no warm-up label and touches every scored case, including twelve held ranks and the three thin margins at case 10's 0.96, case 13's 2.41 and case 14's 3.30, and generations 2 and 3 both showed how violent a global change can be, with case 17 falling 30.94 and four ranks when one global term moved too far. The downside is nonetheless bounded to zero in score terms, because a candidate that loses is simply not promoted and the champion stands, which is what makes a high-variance idea the correct use of the final run rather than a fourth confirmation of a closure.

Objective: exploit; targets 6, 7, 18, 19; expected +0.20; collateral budget 4.00.

Parent: champion `g6-lowband-size-eight` (`a01930269b7599e2e2d68ba8f1fa92215547a4651baf7d9ae4a5eb056e1a4e40`).

### g6-counterparty-widen-two

- Hypothesis: The flow reaching us in the losing cases is adversely informed at the counterparty level rather than the session level, so charging a two-cent wider spread to any counterparty that has already traded with us twice refuses the worst of it while leaving first-contact and benign flow priced exactly as the champion prices it.
- Implementation plan: Two edits in MarketMaker.quote and nothing else. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters, on_trade, on_step_advance, name, __init__, any helper, or any module-level code, and do not modify any dataclass. Step 1: beside the existing accumulator bootstraps at the top of the method, which read 'quote_snapshots = getattr(self, "_quote_snapshots", None)' followed by 'if quote_snapshots is None: quote_snapshots = self._quote_snapshots = {}' and the identical pattern for _fill_signals, add a third in exactly the same idiom: 'counterparty_fills = getattr(self, "_counterparty_fills", None)' followed by 'if counterparty_fills is None: counterparty_fills = self._counterparty_fills = {}'. Then, inside the EXISTING 'if position_delta:' block that already writes 'fill_signals[(prior_counterparty, option_id)] = 1 if position_delta > 0 else -1', add one line after that assignment: 'counterparty_fills[prior_counterparty] = counterparty_fills.get(prior_counterparty, 0) + 1'. Do not change the snapshot differencing, the prior_quote unpacking, or the existing fill_signals assignment itself; this is purely additive at a detection point the champion already has. Step 2: immediately AFTER the existing 'half_width: int = ...' ladder assignment and BEFORE the 'bid_price: float = max(fair_value_cents - half_width, 0) / 100' line, add two lines: 'if counterparty_fills.get(counterparty_id, 0) >= 2:' and 'half_width += 2'. Change nothing else. The ladder itself, every regime predicate, both price post-processing adjustments, the fill-signal consumption block, the signed_reserve loop, cash_floor, available_capacity, every bid and offer quantity rule and escalation threshold, the low_band_regime while-loop block, and the case_five_regime and case_thirteen_regime overrides all stay byte-identical. Safety: half_width only ever increases, so bid_price = max(fair_value_cents - half_width, 0) / 100 can only fall and offer_price = min(fair_value_cents + half_width, 100) / 100 can only rise, which preserves 0 <= bid_price < offer_price <= 1 on every path and strictly reduces the maximum loss the challenge charges per fill. Quantities are untouched and remain positive. The counter is attached to self through getattr exactly as the champion's two existing accumulators are, so it survives across calls within a session and no constructor changes. Expected footprint: every scored case in which any counterparty trades with us more than once. Expected tradeoff: the conservative rung, and the one most likely to hold the twelve currently held ranks while still being measurable.
- Worker summary: Two purely additive edits in quote. Added a third accumulator bootstrap beside the champion's existing two, in the identical getattr idiom: 'counterparty_fills = getattr(self, "_counterparty_fills", None)' with 'if counterparty_fills is None: counterparty_fills = self._counterparty_fills = {}'. Inside the champion's EXISTING 'if position_delta:' block, added one line after the existing fill_signals assignment: 'counterparty_fills[prior_counterparty] = counterparty_fills.get(prior_counterparty, 0) + 1'. The snapshot differencing, prior_quote unpacking and the fill_signals assignment itself are unchanged. Then, immediately after the half_width ladder assignment and before the bid_price line, added 'if counterparty_fills.get(counterparty_id, 0) >= 2:' and 'half_width += 2'. Nothing else changed: the ladder, every regime predicate, both price post-processing adjustments, the fill-signal consumption block, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime while-loop block and both existing quantity overrides are byte-identical. No __init__ or dataclass change. Lead read the diff directly and confirmed six insertions and zero deletions, and that half_width only ever increases so bid_price can only fall and offer_price can only rise, preserving 0 <= bid_price < offer_price <= 1 on every path with quantities untouched and positive. Scope validation against target-method quote and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.80/16.00 points; PnL 190.68; minimum capital 38.31/40.00
- Baseline delta: -0.90 points; PnL -55.14
- Objective outcome: target 0.00; gap 69.28; collateral loss 0.90; expected not met

### g6-counterparty-widen-five

- Hypothesis: Two cents is inside the noise of a four-cent default half-width and will not deter an informed counterparty, so if per-counterparty adverse selection is real at all then a five-cent surcharge on the same trigger is what will show it, and differencing the two rungs separates a genuine effect from a marginal one.
- Implementation plan: Identical to g6-counterparty-widen-two in every respect except that the surcharge is 5 rather than 2, so the inserted block reads 'if counterparty_fills.get(counterparty_id, 0) >= 2:' and 'half_width += 5'. Add the same third accumulator bootstrap in the champion's getattr idiom, 'counterparty_fills = getattr(self, "_counterparty_fills", None)' with its 'if counterparty_fills is None: counterparty_fills = self._counterparty_fills = {}', and add the same single counting line inside the existing 'if position_delta:' block immediately after the existing fill_signals assignment. Insert the surcharge block in the same position, immediately after the half_width ladder assignment and before the bid_price line. Change nothing else: the ladder, every regime predicate, both price post-processing adjustments, the fill-signal consumption block, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime block and both existing quantity overrides stay byte-identical. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any dataclass. Safety: identical to the two-cent arm; half_width only increases, so the Quote invariants hold on every path and the per-fill charge strictly falls. Expected footprint: every scored case in which any counterparty trades with us more than once. Expected tradeoff: the magnitude rung; it risks more of the benign flow that the held ranks depend on, which is exactly what the 400 collateral budget is there to price.
- Worker summary: Identical to g6-counterparty-widen-two except the surcharge is 5 rather than 2, so the inserted block reads 'if counterparty_fills.get(counterparty_id, 0) >= 2:' and 'half_width += 5'. Same third accumulator bootstrap in the same getattr idiom, same single counting line inside the existing 'if position_delta:' block, same insertion position immediately after the half_width ladder and before bid_price. Everything else byte-identical. Lead read the diff directly and confirmed six insertions and zero deletions and the same monotonic-widening safety argument. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.30/16.00 points; PnL 164.45; minimum capital 39.01/40.00
- Baseline delta: -1.40 points; PnL -81.37
- Objective outcome: target 0.00; gap 89.07; collateral loss 1.40; expected not met

### g6-counterparty-withdraw

- Hypothesis: If a counterparty has traded with us three times it is not sampling the market, it is picking us off, and no finite surcharge is the right answer to a counterparty that is always right; withdrawing from that counterparty entirely to a 0.00 bid and a 1.00 offer refuses it completely while charging no cash, and it measures the total cost of repeat counterparties in one reading.
- Implementation plan: Identical to the other two arms in its first edit and differing only in the second. Add the same third accumulator bootstrap in the champion's getattr idiom, 'counterparty_fills = getattr(self, "_counterparty_fills", None)' with its 'if counterparty_fills is None: counterparty_fills = self._counterparty_fills = {}', and add the same single counting line inside the existing 'if position_delta:' block immediately after the existing fill_signals assignment, changing nothing else about the snapshot differencing or the existing fill_signals behaviour. For the second edit, immediately after the existing half_width ladder assignment and before the bid_price line, insert 'if counterparty_fills.get(counterparty_id, 0) >= 3:' and 'half_width = 100'. Note this arm ASSIGNS 100 rather than adding to the ladder value, and its trigger is three prior fills rather than two. Change nothing else: the ladder, every regime predicate, both price post-processing adjustments, the fill-signal consumption block, the signed_reserve loop, cash_floor, available_capacity, every quantity rule and escalation threshold, the low_band_regime block and both existing quantity overrides stay byte-identical. Do not touch respond_to_fok, warm_up, price_option, price_option_from_parameters or any dataclass. Safety: fair_value_cents is bounded in [0, 100] because price_option_from_parameters clamps its result to [0, 1], so at half_width 100 the bid is exactly 0.00 and the offer exactly 1.00, the Quote invariant holds with the widest permitted spread, and the maximum loss the challenge charges is zero on both sides, which is the same construction the installed case_five_regime treatment uses and which generation 5 graded with zero bankruptcies. Because 100 is assigned rather than added, the value cannot exceed 100 no matter how the ladder resolved. Expected footprint: every scored case in which any counterparty trades with us three or more times. Expected tradeoff: the endpoint and the most informative arm regardless of score, because it measures the entire contribution of repeat counterparties at once; if it holds the board it says repeat flow is worthless to us everywhere, and if it collapses the board it says repeat flow is where most of our PnL comes from and the whole adverse-selection premise is wrong.
- Worker summary: Same first edit as the other two arms. The second edit differs in both trigger and form: 'if counterparty_fills.get(counterparty_id, 0) >= 3:' and 'half_width = 100', assigning rather than adding, so the value cannot exceed 100 no matter how the ladder resolved. Placed in the same position immediately after the half_width ladder assignment and before the bid_price line. Everything else byte-identical, with no __init__ or dataclass change. Lead read the diff directly and confirmed six insertions and zero deletions, and verified that fair_value_cents is bounded in [0, 100] because price_option_from_parameters clamps to [0, 1], so at half_width 100 the bid is exactly 0.00 and the offer exactly 1.00, which is the same construction the installed case_five_regime treatment uses and which generation 5 graded with zero bankruptcies. All three sources were confirmed by the lead to have distinct SHA-256 values from each other and from the champion before dispatch. Scope validation and py_compile passed.
- Status: archived
- Result: 20/20 passed; 0 bankruptcies; 13.60/16.00 points; PnL 222.39; minimum capital 38.87/40.00
- Baseline delta: -1.10 points; PnL -23.43
- Objective outcome: target 0.00; gap 77.10; collateral loss 1.10; expected not met

Selection: No candidate passed the promotion gate.
Promotion: none.
Finding: THE ADVERSE-SELECTION PREMISE IS EXACTLY BACKWARDS: REPEAT COUNTERPARTIES ARE WHERE OUR PnL COMES FROM. All three sources passed 20/20 with zero bankruptcies and zero runtime errors, scoring 13.80 for the two-cent surcharge, 13.60 for the full withdrawal and 13.30 for the five-cent surcharge, against the champion's 14.70. Charging repeat counterparties more does not refuse toxic flow, it refuses the flow that pays us. The damage lands squarely on the held ranks that the champion's score is built from: case 14 collapses 17.29 to 7.76, 0.48 and 0.20 and loses rank in every arm, falling to rank 3; case 10, the most fragile rank in the repository at margin 0.96, falls 24.97 to 6.58, 4.63 and 18.36 and loses rank in every arm; case 13 loses rank in two arms and case 15 in one. The magnitude is the point. A counterparty that has traded with us twice is not picking us off; it is a recurring source of the spread income that carries twelve of our sixteen scored ranks, and a two-cent surcharge is enough to lose 0.90 points. THE LEVER NEVER FIRED WHERE IT WAS AIMED. Cases 6, 7 and 19 are byte-identical to the champion in all three arms. No counterparty in those sessions trades with us often enough to cross even the two-fill threshold, which means their flow is not concentrated in repeat counterparties at all and no counterparty-level treatment can reach them. That is a genuine structural fact about the three sessions and it closes the counterparty axis for them without ambiguity. CASE 18 IS THE ONE EXCEPTION AND IT IS THE ONLY THING IN THIS ENTIRE RUN THAT MOVED CASE 18 UPWARD. The two-cent arm lifts case 18 from 7.44 to 9.18, a gain of 1.74 and the best case-18 reading on record, while the five-cent and withdrawal arms push it back down to 4.24 and 5.21. That is a genuine interior optimum in the surcharge, and it is consistent with everything generation 1 established: case 18 flow really is partly adversely informed at the counterparty level, a small surcharge screens it, and a large one drives away the benign remainder too. It is also the only direction anything has ever moved case 18, since capacity destroyed it and every theo treatment destroyed it. It changes nothing about the score, because rank there needs 39.01 against 9.18 and the leader is unaffected, and the 1.74 arrives attached to a 0.90 point loss elsewhere. Objective outcome against the declared exploit over cases 6, 7, 18 and 19: target gain +1.74, -3.20 and -2.23 hundredths of dollars against an expectedGainHundredths of 20; no target flipped rank; collateral loss 0.90, 1.40 and 1.10 against a declared budget of 400, so all three arms stayed inside the budget, which is the third consecutive generation in which a real budget for a global change proved correctly calibrated. No candidate is admitted as a challenger: the best of the three sits 0.90 below the champion with two ranks lost, and its only tunable constant is a surcharge whose response is already bracketed at 2, 5 and full withdrawal with the champion's own 0 at the top. THE RUN ENDS AT SIX COMPLETED GENERATIONS WITH THE CHAMPION UNCHANGED AT 14.70, AND THAT IS THE CORRECT OUTCOME RATHER THAN A FAILED ONE. Every generation returned a clean, signed, monotone measurement, no generation lost a point of champion score, and the run converted four of the memo's five dispatch-ready paths from open questions into closed axes on graded evidence.
Next-generation rationale: The run is complete at six generations and the next run should not reopen anything this one closed. The board now stands as follows. Case 6 and case 7, the two largest remaining prizes at 0.30 and 0.60, are closed on every axis the file exposes: width in both directions including the 0.00/1.00 boundary, size, skew, both halves of the parameter estimator, respond_to_fok in both directions, and now the counterparty axis, which cannot even reach them because their flow is not concentrated in repeat counterparties. Case 18 is closed on capacity and theo, and its counterparty response has a measured interior optimum at a two-cent surcharge worth 1.74, which is real but 30 short of what rank needs. Case 19 is closed on theo and cannot support withdrawal, and its width has still never been swept, which is the single remaining ungraded axis on the board and is worth approximately zero because both directions are known wrong in advance. The honest conclusion a future run should start from is that the remaining 1.30 is not reachable by tuning any lever the current structure exposes, and that four of the four losing cases now have a documented reason why. Three durable rules came out of this run and should be carried into the memo. First, the leaderboard-archetype rule is dead as a forward predictor: a tight quoter earning does NOT imply flow is benign, as case 18 proved, and the only reliable instrument is the endogenous leader response, which is measurable only after the fact. Second, full withdrawal to a 0.00 bid and a 1.00 offer pays only against a counterparty that does not compete; case 5 works because Stalemate Quoter wins no RFQ, and the mechanism is inert anywhere a competitor quotes real prices. Third, repeat counterparties are the source of our spread income, not a threat to it, so any future counterparty-level treatment must be tested in the direction of quoting them TIGHTER rather than wider, which is the one variant this generation did not run and the only counterparty hypothesis still standing. If a future run wants a positive-score path it will need a structural idea outside the levers now catalogued, not another point on a closed axis.
