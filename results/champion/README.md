# Champion

`champion.json` is the machine-readable active champion record, the canonical
source is the repository-root `Market_making_binary_option.py`, and `result.md`
is the champion's fixed-grader evidence. These files are replaced together on
promotion.

`results/baselines/best.*` exists only for compatibility with older commands.
